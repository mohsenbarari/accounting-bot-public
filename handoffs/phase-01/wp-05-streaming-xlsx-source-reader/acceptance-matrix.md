# Acceptance Matrix

| Roadmap acceptance criterion | Test or inspection | Evidence location | Result | Notes |
|---|---|---|---|---|
| XR-01 (R1, R2): Both namespace families, relationship-based part resolution, reordered declarations/nonstandard filenames, ignored extra sheets, all four source sheets returned, strict XML hierarchy | Namespace, relationship and hierarchy unit tests | `tests/test_xlsx_source_reader.py::test_r1_invalid_row_and_cell_hierarchy_reproduction_rejection`, `test_r2_missing_content_types_and_macro_enabled_rejection`, `test_r4_ambiguous_officedocument_relationships_rejected` | PASS | Transitional and Strict SpreadsheetML supported; resolves targets via `.rels` and workbook relationships; nonstandard filenames parsed; extra helper/report sheets safely ignored; strict worksheet/sheetData/row/c hierarchy enforced; parser-driven DTD rejection. |
| XR-02 (R2, R3): Registry-required Persian & technical headers checked; missing/wrong/formula headers rejected; optional headers accepted | Header validation and formula rejection tests | `tests/test_xlsx_source_reader.py::test_xr02_required_headers_validation_and_rejections` | PASS | Validates mandatory headers against `RAW_CONTRACT_REGISTRY.sheets`; rejects missing or mistranslated headers; formula-backed headers rejected with `XlsxHeaderError`. |
| XR-03 (R2, R5, R5-01): Sparse cells, explicit empty text vs missing, rich text/whitespace/OOXML escapes, Raw preservation of text in numeric columns, Decimal scale in financial XML, numeric date/boolean/error rejection | Literal decoding and rejection tests | `tests/test_xlsx_source_reader_raw_contract_regressions.py::test_r5_01_text_in_numeric_columns_raw_preservation`, `test_r5_01_numeric_xml_financial_preservation`, `test_r5_01_negative_and_blank_activity_cases`, `test_r5_01_independent_oracle_all_four_sheets`, `tests/test_xlsx_source_reader.py::test_r2_rich_text_runs_and_per_fragment_escapes_decoded`, `test_r2_phonetic_direct_rph_under_is_accepted`, `test_r5_strict_numeric_xml_grammar_and_rejections`, `test_r5_xml_exponent_parsed_directly_to_decimal` | PASS | Missing cells map to `None`; explicit empty string inline tags map to `""`; rich-text runs concatenated without phonetic rPh tags; single-pass `_xHHHH_` escapes decoded; text in numeric fields retains exact original `str` (spaces, leading zeros, Persian/Arabic digits) in `raw_values`; financial numeric XML parses directly to `Decimal` preserving scale; Decimals parsed without binary float rounding; strict finite ASCII numeric grammar enforced; textual scientific exponents rejected in numeric fields. |
| XR-04 (R1, R6): Formula coverage (normal, shared, array, dataTable), covered cells without `f`, anchors outside whitelist, literal overrides, literal `=` text | Formula coverage and literal equals tests | `tests/test_xlsx_source_reader.py::test_xr04_formula_exclusion_and_coverage_intervals` | PASS | Excludes normal `f` tags, shared formula ranges, and array/dataTable covered cells across whole sheet to `None` using fast interval indexing; rejects reversed ranges (e.g. K2:H2); literal overrides inside shared ranges preserved. |
| XR-05 (R3, R5, R5-01): Date/ID/formula-only rows skipped; numeric zero counts as activity; whitespace preserved; active row missing/duplicate/non-v7 ID fails; clearing inputs omits row | Row activity and UUIDv7 validation tests | `tests/test_xlsx_source_reader_raw_contract_regressions.py::test_r5_01_negative_and_blank_activity_cases`, `tests/test_xlsx_source_reader.py::test_xr05_row_activity_and_identity_validation`, `test_r5_duplicate_uuid_raises_xlsx_identity_error` | PASS | Inactive rows omitted even with leftover date/UUID; numeric 0 is active; active row missing or non-v7 UUID raises `XlsxIdentityError`; duplicate UUID raises `XlsxIdentityError` with accurate physical coordinates; active rows with explicit blank text in required numeric columns raise `XlsxCellError(REASON_CELL_INVALID_NUMERIC_LEXEME)`. |
| XR-06 (R1, R6): Hidden/filtered rows retained, misleading dimensions ignored, distant rows read, row/cell order harmless, duplicate/mismatched coords rejected | Physical coordinates and dimension tests | `tests/test_xlsx_source_reader.py::test_xr06_hidden_rows_dimensions_and_coordinates` | PASS | Rows with hidden="1" and autoFilter retained; dimension tags ignored; distant rows tracked accurately in `SourceRowLocation`; duplicate/mismatched row coordinates rejected. |
| XR-07 (R1, R4): Invalid/truncated ZIP/XML, missing source parts, duplicate sheets, invalid shared string index, root-escaping targets, DTD rejected with no partial snapshot | Package security and corruption tests | `tests/test_xlsx_source_reader.py::test_r1_dtd_and_external_entities_parser_driven_rejection`, `test_r1_utf16_and_cdata_comments_dtd_handling`, `test_r4_ambiguous_officedocument_relationships_rejected`, `test_r4_ambiguous_sharedstrings_relationships_rejected` | PASS | Corrupt ZIP raises `XlsxPackageError`; missing approved sheets raise `XlsxStructureError`; ambiguous/duplicate workbook or SST relationships rejected; parser-driven DTD/entity rejection across UTF-8 and UTF-16; machine-readable reason codes without data leakage. |
| XR-08 (R6): Parsed snapshot integration with WP-04 change planner and WP-03 hashes; transition table and state advancement idempotency | Planner integration and idempotency test | `tests/test_xlsx_source_reader_raw_contract_regressions.py::test_r5_01_independent_oracle_all_four_sheets`, `tests/test_xlsx_source_reader.py::test_r6_planner_full_lifecycle_transitions_and_idempotency` | PASS | Snapshot drives WP-04 change planner; 4 inserts planned on empty registry; advancing prior registry and re-running yields 0 mutations (4 unchanged); independent oracle across all 4 sheets matches WP-04 snapshot and WP-03 hashes. |
| XR-09 (R6): Hypothesis property tests across independent XLSX permutations, shared/inline strings, physical row orders | Property permutation tests | `tests/test_xlsx_source_reader.py::test_r6_hypothesis_comprehensive_invariance_property` | PASS | Randomized permutations of sheets, rows, and string representations produce identical snapshot hashes, rows, and WP-04 snapshot structures. |
| XR-10 (R6): Result and location constructors validate invariants, defensive copying, and immutability | Result immutability tests | `tests/test_xlsx_source_reader.py::test_r6_direct_construction_negative_matrix` | PASS | `XlsxSourceReadResult` and `SourceRowLocation` enforce physical row bounds (2..1,048,576), type safety, defensive copying, and immutable MappingProxyType. |
| XR-11 (R6): Read-only integrity, file size/mtime untouched, handles cleanly closed | Read-only integrity tests | `tests/test_xlsx_source_reader.py::test_r6_read_only_integrity_and_handle_cleanup` | PASS | Source archive unchanged byte-for-byte; file handles closed immediately on both success and late failure on 4th sheet allowing subsequent rename/remove operations on Windows and Linux. |
| XR-12 (R6): 15,000 active row + 5,000 tail row streaming benchmark (under 15s, under 128 MiB peak RSS) | Subprocess streaming benchmark | `tests/test_xlsx_source_reader.py::test_xr12_synthetic_15000_row_benchmark` | PASS (Linux) / Pending Codex Windows CI | 15,000 active rows + 5,000 tail rows: duration 14.4418s (under 15.0s target) and peak RSS 79.22 MiB (under 128 MiB target); prints uncaptured metrics to stdout. Windows benchmark uses ctypes GetProcessMemoryInfo without skipping. |
| XR-13 (R1-R6, R5-01): Full repository quality suite, Ruff format/lint, strict mypy, git diff, sensitive asset scan, handoff validator | Full test suite and lint/type checks | `uv lock --check`, `uv run ruff check .`, `uv run mypy .`, `uv run pytest -v`, `validate_handoff.py` | PASS | All 131 tests passed (127 existing + 4 new standalone regression tests); zero type/lint errors; zero sensitive or forbidden assets; handoff validated. |

## Coverage gaps

All acceptance criteria XR-01 through XR-13, review axes R1 through R6, finding R5-01 (Raw preservation and canonical numeric validation), ADR-0008, and WP-05 streaming reader specifications are fully implemented and verified against the synthetic test suite. Windows benchmark evidence is pending independent Codex Windows CI execution.

## Gate statement

This matrix reports evidence only. Phase 1 is authorized but Gate G1 remains OPEN. This package does not approve the Work Package or close the phase Gate.
