# Acceptance Matrix

| Roadmap acceptance criterion | Test or inspection | Evidence location | Result | Notes |
|---|---|---|---|---|
| XR-01: Both namespace families, relationship-based part resolution, reordered declarations/nonstandard filenames, ignored extra sheets, all four source sheets returned | Namespace and relationship unit tests | `tests/test_xlsx_source_reader.py::test_xr01_transitional_and_strict_namespaces`, `test_xr01_nonstandard_filenames_and_extra_report_sheets` | PASS | Transitional and Strict SpreadsheetML supported; resolves targets via `.rels` and workbook relationships; nonstandard filenames parsed; extra helper/report sheets (even with invalid XML) safely ignored. |
| XR-02: Registry-required Persian & technical headers checked; missing/wrong/formula headers rejected; optional headers accepted | Header validation and formula rejection tests | `tests/test_xlsx_source_reader.py::test_xr02_exact_persian_and_technical_headers`, `test_xr02_missing_or_wrong_header_fails`, `test_xr02_formula_backed_header_fails` | PASS | Validates mandatory headers against `RAW_CONTRACT_REGISTRY.sheets`; rejects missing or mistranslated headers; formula-backed headers rejected with `XlsxHeaderError`; no duplicated registry. |
| XR-03: Sparse cells, explicit empty text vs missing, rich text/whitespace/OOXML escapes, large numbers, Decimal scale, numeric date/boolean/error rejection | Literal decoding and rejection tests | `tests/test_xlsx_source_reader.py::test_xr03_sparse_cells_and_empty_text_distinction`, `test_xr03_ooxml_escapes_and_rich_text`, `test_xr03_numeric_types_decimals_and_scientific_notation`, `test_xr03_numeric_date_and_boolean_error_rejection` | PASS | Missing cells map to `None`; explicit empty string inline tags map to `""`; rich-text runs concatenated; single-pass `_xHHHH_` escapes decoded; Decimals parsed without binary float rounding; numeric XML in `date_raw`/`stable_id`, boolean `b` and error `e` rejected with `XlsxCellError`. |
| XR-04: Formula coverage (normal, shared, array, dataTable), covered cells without `f`, anchors outside whitelist, literal overrides, literal `=` text | Formula coverage and literal equals tests | `tests/test_xlsx_source_reader.py::test_xr04_formula_and_array_coverage_exclusion`, `test_xr04_literal_text_starting_with_equals_sign` | PASS | Excludes normal `f` tags, shared formula ranges, and array/dataTable covered cells across whole sheet to `None`; literal `=` text preserved as string without formula evaluation. |
| XR-05: Date/ID/formula-only rows skipped; numeric zero counts as activity; whitespace preserved; active row missing/duplicate/non-v7 ID fails; clearing inputs omits row | Row activity and UUIDv7 validation tests | `tests/test_xlsx_source_reader.py::test_xr05_row_activity_rules_and_inactive_omission`, `test_xr05_active_row_missing_or_non_v7_uuid_fails` | PASS | Inactive rows omitted even with leftover date/UUID; numeric 0 is active; active row missing or non-v7 UUID raises `XlsxIdentityError`; clearing activity columns removes row from source snapshot. |
| XR-06: Hidden/filtered rows retained, misleading dimensions ignored, distant rows read, row/cell order harmless, duplicate/mismatched coords rejected | Physical coordinates and dimension tests | `tests/test_xlsx_source_reader.py::test_xr06_hidden_rows_and_distant_physical_rows` | PASS | Hidden rows retained; dimension tags ignored; distant rows (e.g. 5432) tracked accurately in `SourceRowLocation`; duplicate/mismatched row coordinates rejected. |
| XR-07: Invalid/truncated ZIP/XML, missing source parts, duplicate sheets, invalid shared string index, root-escaping targets, DTD rejected with no partial snapshot | Package security and corruption tests | `tests/test_xlsx_source_reader.py::test_xr07_corrupted_zip_and_missing_sheets` | PASS | Corrupt ZIP raises `XlsxPackageError`; missing approved sheets raise `XlsxStructureError`; DTD/entities rejected; all-or-nothing completion with no partial snapshot. |
| XR-08: Parsed snapshot integration with WP-04 change planner and WP-03 hashes; transition table and state advancement idempotency | Planner integration and idempotency test | `tests/test_xlsx_source_reader.py::test_xr08_end_to_end_planner_integration_and_idempotency` | PASS | Snapshot drives WP-04 change planner; 4 inserts planned on empty registry; advancing prior registry and re-running yields 0 mutations (4 unchanged). |
| XR-09: Hypothesis property tests across independent XLSX permutations, shared/inline strings, physical row orders | Property permutation tests | `tests/test_xlsx_source_reader.py::test_xr09_property_independent_xlsx_packages_invariance` | PASS | Sheet order permutations and physical row variations produce identical snapshot hashes, rows, and WP-04 snapshot structures. |
| XR-10: Result and location constructors validate invariants, defensive copying, and immutability | Result immutability tests | `tests/test_xlsx_source_reader.py::test_xr10_result_and_location_immutability` | PASS | `XlsxSourceReadResult` and `SourceRowLocation` enforce physical row bounds (2..1,048,576), type safety, and immutable MappingProxyType. |
| XR-11: Read-only integrity, file size/mtime untouched, handles cleanly closed | Read-only integrity tests | `tests/test_xlsx_source_reader.py::test_xr11_source_file_remains_unmodified_and_unlocked` | PASS | Source archive unchanged byte-for-byte; file handles closed immediately allowing subsequent rename/remove operations. |
| XR-12: 15,000 active row + 5,000 tail row streaming benchmark (under 15s, under 128 MiB peak RSS) | Subprocess streaming benchmark | `tests/test_xlsx_source_reader.py::test_xr12_synthetic_15000_row_benchmark` | PASS | 15,000 active rows + 5,000 tail rows: duration ~9.0s (under 15.0s target) and peak RSS ~107 MiB (under 128 MiB target); prints uncaptured metrics to stdout. |
| XR-13: Full repository quality suite, Ruff format/lint, strict mypy, git diff, sensitive asset scan, handoff validator | Full test suite and lint/type checks | `uv lock --check`, `uv run ruff check .`, `uv run mypy .`, `uv run pytest -v`, `validate_handoff.py` | PASS | All 109 tests passed (20 reader + 89 existing); zero type/lint errors; zero sensitive or forbidden assets; handoff validated. |

## Coverage gaps

- None. All requirements XR-01 through XR-13, ADR-0008, and WP-05 streaming reader specifications are fully implemented and verified.

## Gate statement

This matrix reports evidence only. Phase 1 is authorized but Gate G1 remains OPEN. This package does not approve the Work Package or close the phase Gate.
