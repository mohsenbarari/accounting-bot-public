# Acceptance Matrix

> **Notice:** This acceptance matrix reflects the R5-G Windows Mypy Typing Correction for WP-05 (addressing CI run 33503837653 outcome). Work Package WP-05 is submitted for Codex review; Gate G1 remains **OPEN / IN PROGRESS** and status is **REQUEST_CHANGES** until independent Codex approval and CI pass.

| Roadmap acceptance criterion | Test or inspection | Evidence location | Result | Notes |
|---|---|---|---|---|
| XR-01 (R1, R2): Both namespace families, relationship-based part resolution, reordered declarations/nonstandard filenames, ignored extra sheets, all four source sheets returned, strict XML hierarchy | Namespace, relationship and hierarchy unit tests | `tests/test_xlsx_source_reader.py::test_r1_invalid_row_and_cell_hierarchy_reproduction_rejection`, `test_r2_missing_content_types_and_macro_enabled_rejection`, `test_r4_ambiguous_officedocument_relationships_rejected` | Implemented and tested; pending Codex review | Transitional and Strict SpreadsheetML supported; resolves targets via `.rels` and workbook relationships; nonstandard filenames parsed; extra helper/report sheets safely ignored; strict worksheet/sheetData/row/c hierarchy enforced; parser-driven DTD rejection. |
| XR-02 (R2, R3): Registry-required Persian & technical headers checked; missing/wrong/formula headers rejected; optional headers accepted | Header validation and formula rejection tests | `tests/test_xlsx_source_reader.py::test_xr02_required_headers_validation_and_rejections` | Implemented and tested; pending Codex review | Validates mandatory headers against `RAW_CONTRACT_REGISTRY.sheets`; rejects missing or mistranslated headers; formula-backed headers rejected with `XlsxHeaderError`. |
| XR-03 (R2, R5, R5-01, R5-03, A-01, A-02, A-03, E-01, E-03): Sparse cells, explicit empty text vs missing, rich text/whitespace/OOXML escapes, Raw preservation of text in numeric columns, Decimal scale in financial XML, numeric XML in RAW_TEXT returning str lexeme, strict namespace validation, strict ASCII coordinate parsing | Literal decoding, namespace, and coordinate regression tests | `tests/test_xlsx_source_reader_raw_contract_regressions.py::test_r5_01_text_in_numeric_columns_raw_preservation`, `test_r5_01_numeric_xml_financial_preservation`, `test_r5_01_numeric_xml_in_raw_text_fields_across_all_four_sheets`, `test_r5_01_namespace_strict_validation`, `test_r5_01_coordinate_ascii_and_fullmatch_validation`, `test_r5_01_negative_and_blank_activity_cases`, `test_r5_01_independent_oracle_all_four_sheets` | PASS (Linux) / Remediated in R5-A / Pending Codex Windows CI | Missing cells map to `None`; explicit empty string inline tags map to `""`; rich-text runs concatenated without phonetic rPh tags; single-pass `_xHHHH_` escapes decoded; text in numeric fields retains exact original `str` (spaces, leading zeros, Persian/Arabic digits) in `raw_values`; financial numeric XML parses directly to `Decimal` preserving scale; numeric XML in RAW_TEXT columns returns str lexeme; strict qualified namespace enforced on t and r tags; strict ASCII coordinate fullmatch enforced on cell attributes; Decimals parsed without binary float rounding; textual scientific exponents rejected in numeric fields. |
| XR-04 (R1, R6): Formula coverage (normal, shared, array, dataTable), covered cells without `f`, anchors outside whitelist, literal overrides, literal `=` text | Formula coverage and literal equals tests | `tests/test_xlsx_source_reader.py::test_xr04_formula_exclusion_and_coverage_intervals` | Implemented and tested; pending Codex review | Excludes normal `f` tags, shared formula ranges, and array/dataTable covered cells across whole sheet to `None` using fast interval indexing; rejects reversed ranges (e.g. K2:H2); literal overrides inside shared ranges preserved. |
| XR-05 (R3, R5, R5-01, R5-02, A-04, E-04, RB-01..RB-05, R5-C): Date/ID/formula-only rows skipped; numeric zero counts as activity; whitespace preserved; active row missing/duplicate/non-v7 ID fails; clearing inputs omits row; selective SST decoding excludes inactive secondary SST | Row activity and SST selection regression tests | `tests/test_xlsx_source_reader_sst_activity_regressions.py`, `tests/test_xlsx_source_reader_raw_contract_regressions.py::test_r5_01_negative_and_blank_activity_cases`, `tests/test_xlsx_source_reader.py::test_xr05_row_activity_and_identity_validation`, `test_r5_duplicate_uuid_raises_xlsx_identity_error` | Implemented and tested; pending Codex review | Inactive rows omitted even with leftover date/UUID; inactive rows with corrupted SST entries (e.g. index 0 `_xD800_`) or unneeded leaf tags in `date_raw` / `record_id` do not trigger SST decoding or errors; inlineStr container `&lt;is&gt;` extracted correctly across plain, rich runs, comments/tails, and Persian/ASCII zeros (RB-01); escaped whitespace (`_x0020_`, `_x0009_`, `_x000D_`, `_x00A0_`) unified between discovery and decode as inactive (RB-02); secondary SST evaluation is strictly lazy and consumer coordinates attached on needed SST decode failures (RB-03); formula-covered cells return `None` without evaluating cached values; active rows missing/duplicate/non-v7 ID raise `XlsxIdentityError`; active rows referencing corrupt SST raise `XlsxCellError(REASON_CELL_UNPAIRED_SURROGATE)`. |
| XR-06 (R1, R6): Hidden/filtered rows retained, misleading dimensions ignored, distant rows read, row/cell order harmless, duplicate/mismatched coords rejected | Physical coordinates and dimension tests | `tests/test_xlsx_source_reader.py::test_xr06_hidden_rows_dimensions_and_coordinates` | Implemented and tested; pending Codex review | Rows with hidden="1" and autoFilter retained; dimension tags ignored; distant rows tracked accurately in `SourceRowLocation`; duplicate/mismatched row coordinates rejected. |
| XR-07 (R1, R4): Invalid/truncated ZIP/XML, missing source parts, duplicate sheets, invalid shared string index, root-escaping targets, DTD rejected with no partial snapshot | Package security and corruption tests | `tests/test_xlsx_source_reader.py::test_r1_dtd_and_external_entities_parser_driven_rejection`, `test_r1_utf16_and_cdata_comments_dtd_handling`, `test_r4_ambiguous_officedocument_relationships_rejected`, `test_r4_ambiguous_sharedstrings_relationships_rejected` | Implemented and tested; pending Codex review | Corrupt ZIP raises `XlsxPackageError`; missing approved sheets raise `XlsxStructureError`; ambiguous/duplicate workbook or SST relationships rejected; parser-driven DTD/entity rejection across UTF-8 and UTF-16; machine-readable reason codes without data leakage. |
| XR-08 (R6, A-04, E-02, R5-05, F2): Parsed snapshot integration with WP-04 change planner and WP-03 hashes; transition table and state advancement idempotency | Planner integration and full lifecycle transitions test | `tests/test_xlsx_source_reader.py::test_r6_planner_full_lifecycle_transitions_and_idempotency`, `tests/test_xlsx_source_reader_raw_contract_regressions.py::test_r5_01_independent_oracle_all_four_sheets` | Implemented and tested; pending Codex review | Snapshot directly feeds WP-04 change planner; verified complete lifecycle transitions across initial inserts, subsequent in-place edits, voids via row deletion, reactivations via row re-addition, cross-sheet UUID collisions (rejected), and repeat run idempotency (0 mutations). Semantically correct state advancement preserves prior revisions on unchanged items, advances revisions on inserts/edits/voids, and retains voided identities. Full type-and-scale oracle matches WP-04 snapshot and WP-03 hashes. |
| XR-09 (R6, RB-04, R5-05, F2, E3): Genuine Hypothesis property-based testing, XML inspection, and randomized permutations of sheets, rows, and string representations | Hypothesis property test and permutation tests | `tests/test_xlsx_source_reader.py::test_r6_hypothesis_comprehensive_invariance_property`, `tests/test_xlsx_source_reader_sst_activity_regressions.py::test_rb_04_equivalent_representations_and_row_cell_permutation` | Implemented and tested; pending Codex review | Genuine Hypothesis property test (`@given` with permutations of 4 sheets, reversed rows, reversed cells, string storage modes `inline`, `direct_str`, `sst`, and row offsets). Row order, cell order, and row coordinates are explicitly inspected in worksheet XML; SST mode presence is explicitly inspected in `sharedStrings.xml`; sheet-order and inline/direct-string modes alter generated package bytes and are verified through successful parsing plus equality with the independent snapshot and change plan oracle on a nontrivial prior registry. Targeted `test_rb_04_equivalent_representations_and_row_cell_permutation` supplies explicit SST index remapping evidence; existing formula/cache oracle tests supply complementary exclusion evidence. |
| XR-10 (R6, R5-04, F1): Result and location constructors validate invariants, defensive copying, canonical uuid.bytes ordering, and immutability | Result immutability and comprehensive matrix tests | `tests/test_xlsx_source_reader.py::test_r6_direct_construction_comprehensive_matrix` | Implemented and tested; pending Codex review | `SourceRowLocation` and `XlsxSourceReadResult` constructors thoroughly hardened: validate sheet names against registry, enforce physical row integer range ($2 \le r \le 1{,}048{,}576$), check version string, verify snapshot instance, defensively rebuild `locations_by_uuid` in ascending `uuid.bytes` canonical order before wrapping in read-only `MappingProxyType`, verify exact 1-to-1 UUID match with snapshot, check sheet alignment per UUID, and reject duplicate physical rows within the same sheet. Comprehensive matrix test exercises all valid and invalid branches. |
| XR-11 (R6, RB-04, R5-05): Read-only integrity, file size/mtime untouched, handles cleanly closed, 4th-sheet late failure all-or-nothing | Read-only integrity and late-failure tests | `tests/test_xlsx_source_reader.py::test_r6_read_only_integrity_and_handle_cleanup`, `test_r6_all_or_nothing_fourth_sheet_late_failure`, `tests/test_xlsx_source_reader_sst_activity_regressions.py::test_rb_04_equivalent_representations_and_row_cell_permutation`, `test_rb_04_read_only_integrity_and_cleanup_on_failure` | Implemented and tested; pending Codex review | Source archive unchanged byte-for-byte; file handles closed immediately on success and failure allowing subsequent rename/unlink operations; late corruption on 4th sheet aborts entire operation yielding zero partial snapshot and leaves file unblocked. |
| XR-12 (R6, R5-06, F3, R5-D1, R5-D2, R5-E1, R5-E2, R5-F1..R5-F4, R5-G1): 15,000 active row + 5,000 formula/style/inactive tail row streaming benchmark (< 15.0s, < 128 MiB call peak RSS) with literal golden digests and hardened call-window RSS sampler | Subprocess streaming benchmark | `tests/test_xlsx_source_reader.py::test_xr12_synthetic_15000_row_benchmark`, `test_r6_windows_memory_probe_structure_and_types`, `test_r6_current_process_rss_probe_and_sampler`, `test_r6_current_process_rss_sampler_worker_error_propagation`, `test_r6_current_process_rss_sampler_termination_guard`, `test_r6_current_process_rss_sampler_lifecycle_and_validation`, `test_r6_linux_proc_status_vmrss_parser_rejections` | PASS (Implementer CPython 3.13.15 Linux: 3 runs at 11.13s / 59.20M peak, 11.82s / 59.18M peak, 11.18s / 59.26M peak; baseline current 27.58M–27.72M; full suite: 11.47s / 59.15M peak; Linux CI run 33503837653 SUCCESS in 1.6929s / 59.06M; Windows CI publication pending) | Contract-complete benchmark fixture with 15,000 active rows plus 5,000 tail rows and 100 unused SST entries. Pre-reader XML assertions verify formula, style, and unused SST elements. Returns 4 deterministic 64-hex golden digests. Hardened call-window RSS sampler samples absolute current process RSS strictly during reader call with concrete `int`/`float` conversion for Windows `WorkingSetSize`, interruptible `_stop_event.wait` loop, worker termination verification guard, background probe error propagation, strict `/proc/self/status` VmRSS grammar, one-shot lifecycle state machine, and interval validation. Six focused unit tests validate sampler lifecycle, probe layout, background error propagation, termination guard, invalid interval rejection, and strict VmRSS parsing. |
| XR-13 (R1-R6, R5-01..R5-06, RB-01..RB-05, C-01..C-05, F1..F4, E1..E3, R5-D, R5-E, R5-F, R5-G): Full repository quality suite, Ruff format/lint, strict mypy (Linux native & win32 target), git diff, portable tracked asset and sensitive scans, handoff validator | Full test suite and lint/type checks | `uv lock --check`, `uv run ruff check .`, `uv run mypy .`, `uv run mypy --platform win32 .`, `uv run pytest -v`, `validate_handoff.py` | PASS (Linux & Windows Mypy Clean) / Remediated in R5-G | 89 base tests + 45 reader suite tests + 7 raw contract regression tests + 59 SST activity regression tests = 200 tests (200 passed in 22.06s, zero failures); zero type/lint errors across both native Linux Mypy and target Windows `mypy --platform win32`; zero forbidden tracked binary/database assets via portable `git ls-files` scan; zero sensitive credentials/tokens via portable failure-on-match `git grep` scan; handoff validated. |

## Coverage gaps

This handoff delivery finalizes package R5-G addressing third CI run `33503837653` on PR #19:
- **CI Run 33503837653 Outcome:** Linux job `99843105587` passed completely in 23s (Mypy clean, benchmark 1.6929s / 27.47M baseline / 59.06M peak / 15,000 rows, full suite 200 passed in 6.61s, job conclusion SUCCESS). Windows job `99843105723` failed in 36s strictly during Mypy on `counters.WorkingSetSize` with `[no-any-return]` before pytest.
- **R5-G1 (Windows WorkingSetSize Explicit Concrete Typing Conversion):** Applied explicit concrete numeric conversion (`working_set_bytes = int(counters.WorkingSetSize)` and `working_set_mib = float(working_set_bytes) / (1024.0 * 1024.0)`) in both the benchmark-embedded probe and `_get_current_process_rss_mib`. Validated that both native Linux Mypy and mandatory cross-platform target `mypy --platform win32 .` pass with 0 errors across all 21 source files.
- **Contract & Benchmark Preservation:** Preserved all existing 200 tests, 15,000 rows, 5,000 tail rows, 15.0s and 128.0 MiB limits, golden digests, and WP-02/WP-03/WP-04 contracts.

Execution on Windows CI is PENDING independent Codex publication of PR #19.

## Gate statement

This matrix reports evidence only. Phase 1 is authorized but Gate G1 remains OPEN / IN PROGRESS. Work Package WP-05 remains in status REQUEST_CHANGES pending Codex independent review and CI publication. This package does not approve the Work Package or close the phase Gate.
