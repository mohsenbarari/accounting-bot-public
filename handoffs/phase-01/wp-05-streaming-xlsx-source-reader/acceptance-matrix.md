# Acceptance Matrix

| Roadmap acceptance criterion | Test or inspection | Evidence location | Result | Notes |
|---|---|---|---|---|
| XR-01 (R1, R2): Both namespace families, relationship-based part resolution, reordered declarations/nonstandard filenames, ignored extra sheets, all four source sheets returned, strict XML hierarchy | Namespace, relationship and hierarchy unit tests | `tests/test_xlsx_source_reader.py::test_r1_invalid_row_and_cell_hierarchy_reproduction_rejection`, `test_r2_missing_content_types_and_macro_enabled_rejection`, `test_r2_duplicate_rel_ids_rejected` | PASS | Transitional and Strict SpreadsheetML supported; resolves targets via `.rels` and workbook relationships; nonstandard filenames parsed; extra helper/report sheets safely ignored; strict worksheet/sheetData/row/c hierarchy enforced. |
| XR-02 (R3): Registry-required Persian & technical headers checked; missing/wrong/formula headers rejected; optional headers accepted | Header validation and formula rejection tests | `tests/test_xlsx_source_reader.py::test_r7_error_reason_consistency_and_zero_data_leakage` | PASS | Validates mandatory headers against `RAW_CONTRACT_REGISTRY.sheets`; rejects missing or mistranslated headers; formula-backed headers rejected with `XlsxHeaderError`. |
| XR-03 (R4, R5): Sparse cells, explicit empty text vs missing, rich text/whitespace/OOXML escapes, large numbers, Decimal scale, numeric date/boolean/error rejection | Literal decoding and rejection tests | `tests/test_xlsx_source_reader.py::test_r4_direct_string_empty_v_is_empty_text_not_none`, `test_r4_phonetic_rph_elements_excluded_from_rich_text`, `test_r4_per_fragment_escape_decoding`, `test_r5_strict_numeric_xml_grammar_and_rejections`, `test_r5_xml_exponent_parsed_directly_to_decimal` | PASS | Missing cells map to `None`; explicit empty string inline tags map to `""`; rich-text runs concatenated without phonetic rPh tags; single-pass `_xHHHH_` escapes decoded; Decimals parsed without binary float rounding; strict finite ASCII numeric grammar enforced; numeric XML in `date_raw`/`stable_id`, boolean `b` and error `e` rejected with `XlsxCellError`. |
| XR-04 (R6): Formula coverage (normal, shared, array, dataTable), covered cells without `f`, anchors outside whitelist, literal overrides, literal `=` text | Formula coverage and literal equals tests | `tests/test_xlsx_source_reader.py::test_r6_formula_anchor_anywhere_inside_range_accepted`, `test_r6_reversed_formula_coverage_range_rejected`, `test_r6_extlst_formula_does_not_affect_raw_cell` | PASS | Excludes normal `f` tags, shared formula ranges, and array/dataTable covered cells across whole sheet to `None` using fast interval indexing; rejects reversed ranges (e.g. K2:H2); literal overrides inside shared ranges preserved. |
| XR-05 (R3): Date/ID/formula-only rows skipped; numeric zero counts as activity; whitespace preserved; active row missing/duplicate/non-v7 ID fails; clearing inputs omits row | Row activity and UUIDv7 validation tests | `tests/test_xlsx_source_reader.py::test_r3_sst_type_missing_v_tag_fails_read`, `test_r3_inactive_row_with_invalid_date_or_uuid_is_omitted` | PASS | Inactive rows omitted even with leftover date/UUID; numeric 0 is active; active row missing or non-v7 UUID raises `XlsxIdentityError`; derived columns skipped pre-decode. |
| XR-06 (R9): Hidden/filtered rows retained, misleading dimensions ignored, distant rows read, row/cell order harmless, duplicate/mismatched coords rejected | Physical coordinates and dimension tests | `tests/test_xlsx_source_reader.py::test_r9_hidden_rows_and_autofilter_retained` | PASS | Rows with hidden="1" and autoFilter retained; dimension tags ignored; distant rows tracked accurately in `SourceRowLocation`; duplicate/mismatched row coordinates rejected. |
| XR-07 (R2, R7): Invalid/truncated ZIP/XML, missing source parts, duplicate sheets, invalid shared string index, root-escaping targets, DTD rejected with no partial snapshot | Package security and corruption tests | `tests/test_xlsx_source_reader.py::test_r2_dtd_and_entity_explicitly_rejected`, `test_r7_duplicate_uuid_raises_duplicate_identity_error` | PASS | Corrupt ZIP raises `XlsxPackageError`; missing approved sheets raise `XlsxStructureError`; duplicate ZIP members / rel IDs rejected; DTD/entities rejected; machine-readable reason codes without data leakage. |
| XR-08 (R9): Parsed snapshot integration with WP-04 change planner and WP-03 hashes; transition table and state advancement idempotency | Planner integration and idempotency test | `tests/test_xlsx_source_reader.py::test_r9_planner_transitions_and_state_advancement` | PASS | Snapshot drives WP-04 change planner; 4 inserts planned on empty registry; advancing prior registry and re-running yields 0 mutations (4 unchanged). |
| XR-09 (R9): Hypothesis property tests across independent XLSX permutations, shared/inline strings, physical row orders | Property permutation tests | `tests/test_xlsx_source_reader.py::test_r9_hypothesis_randomized_packages_invariance` | PASS | Randomized permutations of sheets, rows, and string representations produce identical snapshot hashes, rows, and WP-04 snapshot structures. |
| XR-10 (R7, R9): Result and location constructors validate invariants, defensive copying, and immutability | Result immutability tests | `tests/test_xlsx_source_reader.py::test_r9_result_immutability_and_defensive_copies` | PASS | `XlsxSourceReadResult` and `SourceRowLocation` enforce physical row bounds (2..1,048,576), type safety, defensive copying, and immutable MappingProxyType. |
| XR-11 (R9): Read-only integrity, file size/mtime untouched, handles cleanly closed | Read-only integrity tests | `tests/test_xlsx_source_reader.py::test_r9_read_only_integrity_and_handle_cleanup_on_success_and_failure` | PASS | Source archive unchanged byte-for-byte; file handles closed immediately on both success and failure allowing subsequent rename/remove operations. |
| XR-12 (R8): 15,000 active row + 5,000 tail row streaming benchmark (under 15s, under 128 MiB peak RSS) | Subprocess streaming benchmark | `tests/test_xlsx_source_reader.py::test_xr12_synthetic_15000_row_benchmark` | PASS (Linux) / Pending Codex Windows CI | 15,000 active rows + 5,000 tail rows: duration 12.2768s (under 15.0s target) and peak RSS 79.68 MiB (under 128 MiB target); prints uncaptured metrics to stdout. Windows benchmark uses ctypes GetProcessMemoryInfo without skipping. |
| XR-13 (R1-R9): Full repository quality suite, Ruff format/lint, strict mypy, git diff, sensitive asset scan, handoff validator | Full test suite and lint/type checks | `uv lock --check`, `uv run ruff check .`, `uv run mypy .`, `uv run pytest -v`, `validate_handoff.py` | PASS | All 115 tests passed (16 reader + 99 existing); zero type/lint errors; zero sensitive or forbidden assets; handoff validated. |

## Coverage gaps

All acceptance criteria XR-01 through XR-13, review axes R1 through R9, ADR-0008, and WP-05 streaming reader specifications are fully implemented and verified against the synthetic test suite. Windows benchmark evidence is pending independent Codex Windows CI execution.

## Gate statement

This matrix reports evidence only. Phase 1 is authorized but Gate G1 remains OPEN. This package does not approve the Work Package or close the phase Gate.
