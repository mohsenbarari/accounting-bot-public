# Acceptance Matrix

> **Notice:** This acceptance matrix reflects the delivery of Work Package WP-06 (Stable XLSX Snapshot Acquisition and Controlled Cleanup). Work Package WP-06 is submitted for independent Codex review; Gate G1 remains **OPEN / IN PROGRESS** and status is **REQUEST_CHANGES** until independent Codex approval and CI pass.

| Roadmap acceptance criterion | Test or inspection | Evidence location | Result | Notes |
|---|---|---|---|---|
| SA-01 (O-31, O-71, ADR-0009): Public version/API exports, immutable metadata and direct-construction invariants are exact | Unit tests for exports, dataclass invariants, and error taxonomy | `tests/test_xlsx_snapshot_acquisition.py::test_sa01_public_api_exports_and_version`, `test_sa01_stable_xlsx_snapshot_invariants`, `test_sa01_error_taxonomy_reasons_and_retryability` | PASS (Linux & Windows Mypy Clean) | Exports `XLSX_SNAPSHOT_ACQUISITION_VERSION = "xlsx-snapshot-acquisition.v1"`, `StableXlsxSnapshot`, `XlsxSnapshotAcquisitionReason`, error classes and `open_stable_xlsx_snapshot`. `StableXlsxSnapshot` enforces immutable slots, lowercase 64-hex SHA-256 digest, absolute `.xlsx` path, nonnegative byte count, and source `mtime_ns`. Error hierarchy strictly derives `retryable` from reason (`source_not_ready` is True, others False). |
| SA-02 (O-31, O-71, ADR-0009): Two ordered observations and explicit interval occur before source copy; invalid policy is rejected | Sleeper probe and argument validation unit tests | `tests/test_xlsx_snapshot_acquisition.py::test_sa02_two_observations_and_sleeper`, `test_sa02_invalid_arguments_and_policy_rejections` | PASS (Linux & Windows Mypy Clean) | Verifies two distinct stat observations separated by exact interval using private sleeper seam; rejects non-float, boolean, zero, negative, NaN, and Inf intervals with `XlsxSourcePolicyError`; rejects non-`.xlsx` extensions, directory paths, non-existent snapshot roots, and non-directory roots. |
| SA-03 (O-31, O-71, ADR-0009): Source bytes/hash/size/mtime/permissions remain unchanged on success and all injected failure paths | Source immutability regression tests | `tests/test_xlsx_snapshot_acquisition.py::test_sa03_source_immutability_on_success_and_failures` | PASS (Linux & Windows Mypy Clean) | Source file is opened strictly read-only (`"rb"`). Verified that file size, nanosecond timestamp (`st_mtime_ns`), file permissions (`st_mode`), and exact SHA-256 digest remain 100% untouched across normal return, consumer exceptions, and injected candidate corruption faults. |
| SA-04 (O-31, O-71, ADR-0009): Missing, locked/inaccessible, truncated and changing sources fail with correct retryable reason and yield nothing | Missing and disappearing source unit tests | `tests/test_xlsx_snapshot_acquisition.py::test_sa04_missing_and_disappearing_source_handling` | PASS (Linux & Windows Mypy Clean) | Missing, inaccessible, or changing source files raise `XlsxSourceNotReadyError` with `retryable=True` and yield zero snapshot. |
| SA-05 (O-31, O-71, ADR-0009): In-place mutation and atomic replacement at every copy/verification race point cannot produce a snapshot | Fault injection tests across all acquisition race stages | `tests/test_xlsx_snapshot_acquisition.py::test_sa05_race_mutation_fault_injections` | PASS (Linux & Windows Mypy Clean) | Injects in-place mutation and atomic file replacement at `during_observation`, `before_copy_open`, `during_copy_chunk`, `before_source_reverify`, and `during_source_reverify`. In all cases, acquisition immediately aborts with `XlsxSourceNotReadyError` and cleans up temporary candidate artifacts without leaking partial directories. |
| SA-06 (O-31, O-71, ADR-0009): Streaming copy is bounded, exact, SHA-256 verified and independent of workbook size in memory allocation | Bounded chunk streaming tests and custom buffer sizing | `tests/test_xlsx_snapshot_acquisition.py::test_sa06_streaming_copy_bounded_chunks` | PASS (Linux & Windows Mypy Clean) | Copies via fixed bounded chunks (64 KiB default, tested down to 256 bytes) without loading whole workbook into memory; computes SHA-256 incrementally and verifies byte count against observed source length. |
| SA-07 (O-31, O-71, ADR-0009): Invalid ZIP/container, write/flush/sync, disk/storage and atomic-promotion faults are typed and leave no candidate | Container verification and promotion failure tests | `tests/test_xlsx_snapshot_acquisition.py::test_sa07_invalid_container_and_storage_faults` | PASS (Linux & Windows Mypy Clean) | Non-ZIP data, ZIPs missing `[Content_Types].xml`, corrupted central directory members, and promotion failures raise typed errors (`XlsxSourceNotReadyError` or `XlsxSnapshotStorageError`) and clean up all partial files and lease directories. |
| SA-08 (O-31, O-71, ADR-0009): Consumer observes only promoted `.xlsx`; original-path mutation after yield does not change Reader output | Lease isolation and source mutation test | `tests/test_xlsx_snapshot_acquisition.py::test_sa08_promoted_path_and_source_mutation_isolation` | PASS (Linux & Windows Mypy Clean) | Consumer observes only a promoted `.xlsx` path inside private `acq-...` lease directory (never `.part`). Mutating or deleting the original source file while inside the lease has zero effect on the WP-05 reader. |
| SA-09 (O-31, O-71, ADR-0009): Lease mutation/deletion is detected; cleanup succeeds after normal return and consumer failure | Post-lease integrity and cleanup lifecycle tests | `tests/test_xlsx_snapshot_acquisition.py::test_sa09_lease_mutation_and_deletion_detection` | PASS (Linux & Windows Mypy Clean) | Mutating or deleting the leased file during the context is detected on exit and raises `XlsxSnapshotIntegrityError`. Cleanup removes candidate files and lease directory after normal exit and consumer exceptions. |
| SA-10 (O-31, O-71, ADR-0009): Cleanup failure remains visible and preserves already-active consumer/integrity exception | Cleanup failure and exception chaining tests | `tests/test_xlsx_snapshot_acquisition.py::test_sa10_cleanup_failure_and_exception_chaining` | PASS (Linux & Windows Mypy Clean) | If directory cleanup fails, raises `XlsxSnapshotCleanupError`. If consumer raised an exception, cleanup error is raised with `__cause__` set to the consumer exception, preserving both failures visibly. |
| SA-11 (O-31, O-71, ADR-0009): Concurrent same/different-content acquisitions use disjoint paths; cleanup of one cannot affect another | Multi-threaded concurrent acquisition tests | `tests/test_xlsx_snapshot_acquisition.py::test_sa11_concurrent_disjoint_acquisitions` | PASS (Linux & Windows Mypy Clean) | Parallel acquisitions create distinct private directories (`acq-{uuid}`) in the controlled root with no path collisions or cross-cleanup interference. Equal source bytes produce identical `file_sha256`. |
| SA-12 (O-31, O-71, ADR-0009): Full generated four-sheet workbook passes acquisition then WP-05 and equals direct WP-04 oracle | End-to-end acquisition, reader, and change planner integration test | `tests/test_xlsx_snapshot_acquisition.py::test_sa12_end_to_end_reader_and_planner_oracle` | PASS (Linux & Windows Mypy Clean) | Generated 4-sheet workbook passes through `open_stable_xlsx_snapshot`, is read by `read_xlsx_source_snapshot`, and produces valid change plan against WP-04/WP-03 oracle with 4 planned inserts. |
| SA-13 (O-31, O-71, ADR-0009): Source change produces no Reader call/result and therefore cannot produce incomplete Planner/void result | Safety guard against partial import / void creation | `tests/test_xlsx_snapshot_acquisition.py::test_sa13_source_change_prevents_partial_planner_voids` | PASS (Linux & Windows Mypy Clean) | Proves that a changing or unstable source halts acquisition immediately, preventing WP-05 invocation and guaranteeing zero partial snapshot or false deletion/void change plan generation. |
| SA-14 (O-31, O-71, ADR-0009): All 200 existing tests + 22 new tests pass; Hypothesis property tests and combined 15k-row benchmark pass | Full quality suite, Hypothesis tests, and combined benchmark | `tests/test_xlsx_snapshot_acquisition.py::test_sa14_hypothesis_chunk_and_interval_properties`, `test_sa14_combined_15000_row_benchmark` | PASS (Linux & Windows Mypy Clean: 222 passed in 36.84s; Benchmark: 11.35s – 11.95s, peak RSS 58.86 MiB – 59.24 MiB) | 222 tests total (89 base + 45 reader + 7 raw regressions + 59 SST regressions + 22 acquisition tests); Hypothesis tests arbitrary chunk sizes and intervals; combined 15,000 active row benchmark executes acquisition + WP-05 reader pipeline in 11.35s – 11.95s (< 15.0s limit) with peak RSS 58.86 MiB – 59.24 MiB (< 128.0 MiB limit); native and win32 Mypy 0 errors. |

## Coverage gaps

- Real OneDrive file-system events, two-second debounce coordinator, Excel COM writeback, UUID assignment, and crash-orphan scavenger are out of scope for WP-06 and belong to later coordinator/importer work packages.
- Execution on Windows and Linux CI is PENDING independent Codex publication of PR and CI execution.

## Gate statement

This matrix reports evidence only. Phase 1 is authorized but Gate G1 remains OPEN / IN PROGRESS. Work Package WP-06 is submitted for Codex review. This package does not approve the Work Package or close the phase Gate.
