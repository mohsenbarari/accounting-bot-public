# نقشه راه سامانه گزارش حسابداری تلگرام

> وضعیت سند: فاز 1 فعال؛ G0 بسته، WP-01 تا WP-10 پذیرفته و ادغام شده‌اند؛ WP-11 صادر شده و G1 باز است
>
> نسخه: 0.50
>
> آخرین به‌روزرسانی: 2026-09-03
>
> قاعده حاکم: شروع و ادامه پیاده‌سازی با Work Package شاهددار، بازبینی و Gateهای تحت مدیریت Codex انجام می‌شود؛ Antigravity حق خودتأییدی ندارد و مالک محصول آزمون پذیرش نهایی و اصلاحات محصول را اعلام می‌کند. G0 بسته شده است.
>
> قاعده فایل مرجع: هیچ تغییر نوشتاری، حتی افزودن شناسه فنی یا آزمایش روی فایل اصلی اکسل، بدون اجازه صریح و جداگانه کاربر انجام نمی‌شود.

## 1. راهنمای وضعیت‌ها

| نشان | معنی |
|---|---|
| ✅ قطعی | مالک محصول آن را تعیین کرده یا Codex مدیر پروژه براساس اختیار واگذارشده و شاهد کافی تصویب کرده است. |
| 🟦 پیشنهاد فعلی | پیشنهاد طراحی است و تا زمان مخالفت کاربر مبنای ادامه تحلیل قرار می‌گیرد. |
| 🟨 باز | هنوز به تصمیم یا تعریف دقیق نیاز دارد. |
| 🧪 نیازمند آزمون | قاعده پذیرفته شده ولی باید با داده واقعی یا آزمایش فنی اثبات شود. |
| ⛔ خارج از محدوده فعلی | در نسخه فعلی محصول انجام نمی‌شود. |

قاعده اجرایی وضعیت‌ها طبق O-50: `✅` مجوز اجرای همان متن مصوب است؛ `🧪` فقط مجوز ساخت شاهد فنی لازم را می‌دهد و بدون شاهد Gate بسته نمی‌شود؛ `🟦` نیازمند ADR/تحلیل و تصمیم Codex مدیر پروژه است؛ `🟨` تا ثبت تصمیم مدیر پروژه یا فراهم‌شدن وابستگی متوقف و `⛔` ممنوع است. دستور تازه مالک محصول بر تصمیم مدیریتی مقدم است و اقدام روی دارایی‌های ویژه O-47 همچنان مجوز صریح و جداگانه مالک می‌خواهد.

## 2. چشم‌انداز محصول

### 2.1 هدف

- ✅ ساخت سامانه‌ای برای ارائه گزارش حساب مشتریان فقط از طریق یک بات تلگرامی.
- ✅ پروژه قدیمی Sender Bot مرجع ایده، شکل گزارش و قواعد محاسباتی است؛ محصول جدید قرار نیست کپی دقیق آن باشد.
- ✅ موتور دریافت داده، تشخیص تغییرات، محاسبات و تولید گزارش باید از تلگرام جدا باشد؛ تلگرام فقط رابط کاربری و کانال تحویل است.
- ✅ منبع فعلی اطلاعات، فایل اکسل سالانه است.
- ✅ تاریخچه همه سال‌های شمسی باید در دیتابیس به‌صورت یکپارچه و پیوسته باقی بماند.
- ✅ مدل ارائه گزارش ترکیبی است: گزارش رسمی روزانه/اصلاحیه از طرف سامانه و گزارش درخواستی از طرف کاربران.

### 2.2 محدوده نسخه اول

- ✅ دریافت چهار شیت اصلی اکسل و تبدیل آنها به داده حسابداری قابل پیگیری.
- ✅ محاسبه مانده پول و کالا برای هر شخص.
- ✅ تولید گزارش رسمی روزانه و اصلاحیه به‌صورت متن تلگرام.
- ✅ تولید گزارش‌های درخواستی به‌صورت PDF.
- ✅ اتصال امن صاحب حساب و حداکثر سه حسابدار به یک حساب مالی.
- ✅ نقش‌های صاحب حساب، حسابدار، مدیر میانی و مدیر ارشد.
- ✅ تصمیم مشترک تأیید یا رد گزارش.
- ✅ تشخیص و ارسال مجدد موارد ناموفق.
- ✅ تحمل قطعی اینترنت و همگام‌سازی پس از اتصال مجدد.
- ✅ نگهداری نسخه گزارش‌ها، نسخه رکوردها و سوابق امنیتی.

### 2.3 خارج از محدوده فعلی

- ⛔ اپلیکیشن مستقل موبایل یا وب برای مشتریان.
- ⛔ جست‌وجوی آزاد مشتریان توسط صاحب حساب یا حسابدار.
- ⛔ ارسال گزارش رسمی روزانه یا اصلاحیه در قالب PDF.
- ⛔ تأیید دستی مدیر پیش از هر ارسال گروهی یا نمایش پیش‌نمایش اجباری.
- ⛔ امکان مدیریت حسابداران توسط خود حسابدار.
- ⛔ امکان تعلیق یا مدیریت کاربران توسط مدیر میانی.

## 3. دارایی‌ها و وضعیت فعلی

### 3.1 فایل اکسل و SQLite

- ✅ چهار شیت موردنظر شناسایی و یک نسخه اولیه از آنها در SQLite کپی شده است.

| شیت اکسل | جدول SQLite | تعداد ردیف واردشده |
|---|---|---:|
| خرید-فروش | buy_sell_transactions | 2,946 |
| دریافت-پرداخت | receipts_payments | 8,750 |
| ورود-خروج | inventory_movements | 3,102 |
| لیست کسبه | business_parties | 429 |

- ✅ فایل دیتابیس نمونه فعلی: data/accounting.sqlite3
- ✅ برنامه ورود اولیه، داده خام، شماره ردیف اکسل، نگاشت ستون‌ها و بخشی از اطلاعات ممیزی واردات را نگهداری می‌کند.
- 🧪 صحت کپی اولیه با شمارش و هش رفت‌وبرگشت بررسی شده است؛ صحت حسابداری نهایی هنوز باید برای همه اشخاص با اکسل تطبیق داده شود.
- ✅ شماره موبایل اشخاص در این مرحله تکمیل نمی‌شود و ورود شماره‌های واقعی به آخرین مرحله پیش از راه‌اندازی منتقل شده است.
- ✅ نام‌های تراکنشی خارج از «لیست کسبه» با حفظ نام خام، در جدول Alias/Resolution به یکی از چهار گروه شخص موجود، شخص جدید، حساب/طرف داخلی غیرکاربر یا غلط تایپی/نام جایگزین نگاشت می‌شوند. تطبیق فازی فقط پیشنهاد می‌دهد و اتصال را خودکار قطعی نمی‌کند.
- 🧪 103 نام متمایز شناسایی‌شده باید در فاز 1 یک‌به‌یک تعیین تکلیف شوند؛ ردیف مالی حل‌نشده حذف نمی‌شود، اما تا رفع ابهام امکان فعال‌سازی تلگرام برای آن هویت وجود ندارد.

### 3.2 پروژه قدیمی

- ✅ پروژه قدیمی دسکتاپ در مسیر Sender Bot برای برداشت ساختار گزارش و قواعد محاسباتی تحلیل شده است.
- ✅ ساختار اصلی گزارش جدید از بخش‌های «تراکنش پولی»، «خرید و فروش»، «ورود و خروج کالا» و «مانده نهایی» تشکیل می‌شود.
- ✅ قواعد قدیمی فقط مرجع هستند؛ هرجا کاربر قاعده جدیدی تعیین کرده، قاعده جدید مقدم است.
- ✅ رفتار قدیمی در حذف H، HA و HS از متن گزارش کنار گذاشته می‌شود؛ این کدها طبق تعریف جدید گزارش خواهند شد.
- ✅ فهرست کالاهای سخت‌کدشده و استثناهای اسمی پروژه قدیمی مستقیماً منتقل نمی‌شوند؛ هر کالا و Alias باید از مسیر Item Master مصوب بازبینی و نگاشت شود.

### 3.3 Skill اجرایی Google Antigravity

- ✅ نسخه منبع Skill اختصاصی `accounting-bot-implementer` طبق قالب رسمی Agent Skills در `.agents/skills/accounting-bot-implementer/` ساخته و به‌عنوان نصب Project Scope نهایی روی Workspace سرور مستقر شد.
- ✅ Skill، Roadmap را منبع یگانه می‌خواند، G0 و وضعیت‌ها را اعمال می‌کند، Antigravity را صرفاً مجری نگه می‌دارد، دارایی‌های محافظت‌شده را منع و Handoff/ADR شاهددار را اجباری می‌کند.
- ✅ قالب‌های `handoff.md`، `acceptance-matrix.md`، `test-results.txt` و ADR به‌همراه Validator بدون وابستگی خارجی ساخته و Validator روی Templateها و Fixture تکمیل‌شده با Exit Code صفر آزموده شد.
- ✅ پیش از ایجاد Workspace، Skill موقتاً در مسیر Global قابل‌کشف Antigravity نصب شده بود؛ پس از Clone مخزن، آن نسخه با Backup حفظ و مسیر Global به نسخه Project Scope پیوند مدیریت‌شده شد.
- ✅ `.agents/skills/accounting-bot-implementer/` در مخزن عمومی عملیاتی منبع یگانه است و نصب Global مستقیماً همان منبع را ارائه می‌کند؛ نسخه مستقل قابل‌ویرایش باقی نمانده است.
- ✅ طبق خروجی نشست واقعی ارائه‌شده توسط کاربر در 2026-08-30، Antigravity پس از `/skills` نسخه `accounting-bot-implementer` را کشف و در Dry Run با خواندن Roadmap، تصویب‌نشدن G0 را تشخیص داد، شروع کدنویسی را رد کرد، کارهای مجاز قبل از G0 را به تحلیل/شاهد محدود و Roadmap، Excel، داده واقعی، `main`، Production و سوابق مالی را محافظت‌شده اعلام کرد.

## 4. مدل مفهومی داده

### 4.1 موجودیت‌های اصلی

| موجودیت | وظیفه | وضعیت |
|---|---|---|
| شخص حسابداری | طرف حساب موجود در داده مالی | ✅ قطعی |
| شخص هویتی | فرد دارای شماره موبایل و احتمالاً حساب تلگرام؛ مستقل از شخص حسابداری و ممکن است حسابدارِ خارج از لیست کسبه باشد | ✅ قطعی |
| حساب مالی | مجموعه تراکنش‌ها و مانده‌های یک شخص حسابداری | ✅ قطعی |
| رویداد مالی | واقعیت مالی پایدارِ دریافت، پرداخت، خرید، فروش یا جابه‌جایی کالا که Revision رکوردهای منبع آن را تغییر می‌دهد، نه هویت آن را | ✅ قطعی |
| پایه‌های دفتر (`ledger_entries`) | اثر نسخه‌دار بدهکار/بستانکار هر رویداد بر یک یا چند شخص و مبنای Query مانده | ✅ قطعی |
| نسخه رکورد | نگهداری مقدار قبل و بعد هر ویرایش | ✅ قطعی |
| گزارش منطقی | گزارش روز، اصلاحیه یا PDF مستقل از پیام‌های تلگرام | ✅ قطعی |
| نسخه گزارش | متن و داده ثابت گزارش در زمان ارسال | ✅ قطعی |
| تحویل گزارش | وضعیت ارسال همان گزارش برای هر گیرنده | ✅ قطعی |
| تصمیم گزارش | نتیجه مشترک تأیید/رد و تصمیم‌های فردی | ✅ قطعی |
| صف خروجی | نگهداری عملیات ارسال یا همگام‌سازی تا موفقیت | ✅ قطعی |
| لاگ ممیزی | ثبت دسترسی‌ها، تغییرات، ارسال‌ها و خطاها | ✅ قطعی |

- ✅ «شخص هویتی» و «شخص حسابداری» موجودیت‌های جدا هستند: حسابدار ممکن است در «لیست کسبه» نباشد، شخص حسابداری ممکن است هنوز Telegram نداشته باشد و اتصال دسترسی از طریق عضویت/نقش انجام می‌شود، نه ادغام این دو هویت.
- ✅ رویداد مالی هویت معنایی پایدار دارد و ایجاد/ویرایش/حذف رکوردهای Raw، Revision یا ابطال همان رویداد را طبق قواعد نگاشت می‌سازد؛ شماره ردیف Excel هویت رویداد نیست.
- ✅ `ledger_entries` داده مشتق‌شده، نسخه‌دار و قابل بازتولید از Raw معتبر با `calculation_version` هستند؛ Formula یا خروجی محاسباتی Excel منبع آنها نیست و مانده‌های پول/کالا با Query روی نسخه فعال این پایه‌ها محاسبه می‌شوند.

### 4.2 شناسه‌ها

- ✅ هر شخص باید party_id دائمی داشته باشد و تغییر نام، هویت او را عوض نکند.
- ✅ هر رکورد تراکنشی باید record_id دائمی داشته باشد و شماره ردیف اکسل فقط مرجع مکانی باشد.
- ✅ دو پایه متناظر RS باید relation_id مشترک داشته باشند.
- ✅ هر گزارش و هر اصلاحیه شناسه یکتا و قابل پیگیری دارد.
- ✅ طراحی افزودن شناسه فنی به اکسل تأیید شد، ولی این تأیید مجوز تغییر فایل نیست؛ فایل اصلی و هر کپی آزمایشی تا اجازه صریح و جداگانه کاربر بدون تغییر می‌مانند.
- ✅ شناسه‌ها با UUIDv7 استاندارد و مولد تصادفی امن سیستم‌عامل ساخته می‌شوند؛ در Excel متن ثابت، در SQLite مقدار 16بایتی BLOB و در PostgreSQL نوع native `uuid` هستند. تاریخ داخل UUID زمان ایجاد شناسه است و تاریخ مالی محسوب نمی‌شود.
- ✅ بررسی واقعی فایل نشان داد راه کم‌خطر، استفاده از ستون‌های خالی موجود است، نه Insert کردن ستون و جابه‌جایی فرمول‌ها: `Z` در «خرید-فروش»، `P` در «دریافت-پرداخت»، `P` در «ورود-خروج» و `D` در «لیست کسبه».
- ✅ سه ستون اول با عنوان فنی `record_id` و ستون «لیست کسبه» با عنوان `party_id` مخفی می‌شوند. محدوده فیلتر «دریافت-پرداخت» از `A:L` تا `A:P` و محدوده «لیست کسبه» تا `A:D` گسترش می‌یابد تا شناسه هنگام Sort همراه ردیف جابه‌جا شود؛ محدوده‌های فعلی «خرید-فروش» و «ورود-خروج» از قبل ستون پیشنهادی را پوشش می‌دهند.
- ✅ هیچ فرمول مالی، ورودی حسابداری، متن گزارش یا ترتیب ستون‌های موجود تغییر نمی‌کند. Hash در Excel محاسبه یا ذخیره نمی‌شود و UUID یک مقدار ثابت و بدون وابستگی فرمولی است.
- ✅ سه Hash نسخه‌دار با SHA-256 خارج از Excel محاسبه می‌شوند: `source_hash` برای تغییر ورودی، `ledger_hash` برای تغییر معنای مالی/گزارش و `sheet_snapshot_hash` بر جفت‌های مرتب‌شده `record_id + source_hash` برای تشخیص Insert/Delete بدون حساسیت به Sort.
- ✅ Payload پیش از Hash با Schema Version، ترتیب ثابت کلیدها، UTF-8، تاریخ استاندارد، Integer/Decimal بدون Float و قواعد صریح Null/Text به قالب Canonical تبدیل می‌شود. شماره ردیف، موقعیت سلول و فرمول‌های مشتق در هویت یا Hash مالی وارد نمی‌شوند.
- ✅ UUID تکراری ناشی از Copy Row در صورت تشخیص بدون ابهام برای کپی جدید بازتولید می‌شود؛ اگر تعیین رکورد اصلی قطعی نباشد، کل Import اتمیک متوقف و مغایرت اعلام می‌شود.

### 4.3 نسخه‌بندی و حذف

- ✅ تغییرات بعدی نباید مقدار و متن تاریخی قبلی را از بین ببرد.
- ✅ ویرایش یک رکورد، نسخه جدید ایجاد می‌کند و نسخه قبلی را غیرفعال ولی قابل ممیزی نگه می‌دارد.
- ✅ حذف یک ردیف اکسل به‌صورت حذف نرم/ابطال ثبت می‌شود و رکورد از دیتابیس پاک فیزیکی نمی‌شود؛ حذف و Save معتبر اکسل بدون هیچ تأیید یا آستانه اضافی فوراً معتبر است.
- ✅ مرتب‌سازی یا جابه‌جایی ردیف، تغییر مالی محسوب نمی‌شود.
- ✅ در صورت ویرایش رکورد تاریخی با شناسه ثابت، فقط Revision همان رکورد افزایش می‌یابد. اگر تاریخ یا شخص عوض شود، هم کلید قدیمی و هم کلید جدید «شخص + تاریخ» متأثر محسوب می‌شوند.
- ✅ تغییر تاریخ از A به B، گزارش روز A را با حذف اثر قبلی و گزارش روز B را با افزودن اثر جدید اصلاح می‌کند؛ سپس آخرین مانده صحیح ارسال می‌شود. تغییر سایر فیلدهای رکورد گذشته فقط اصلاحیه همان تاریخ و آخرین مانده را می‌سازد.
- ✅ حذف عادی پس از یک Snapshot کامل و معتبر به `void` تبدیل می‌شود؛ نسخه قبلی، شناسه، زمان حذف و Import ایجادکننده آن باقی می‌مانند و اصلاحیه تاریخ اصلی به‌علاوه آخرین مانده ساخته می‌شود.
- ✅ ردیف جدید با شناسه خالی، پس از اعتبارسنجی UUID جدید می‌گیرد. اگر تاریخ آن مربوط به روزی باشد که گزارش رسمی آن قبلاً ارسال شده، اصلاحیه همان روز و آخرین مانده ساخته می‌شود؛ اگر تاریخ آن روز جاری باشد، در دفتر روز جاری ثبت می‌شود و تا ارسال رسمی روز بعد فقط در گزارش درخواستی قابل مشاهده است.
- ✅ محاسبه مانده از قدیمی‌ترین تاریخ متأثر تا امروز دوباره اجرا می‌شود، اما برای روزهای میانی پیام جداگانه فرستاده نمی‌شود؛ فقط اصلاحیه تاریخ‌های واقعاً تغییرکرده و آخرین مانده ارسال می‌شوند.
- ✅ کاربر اکسل می‌تواند هر تعداد رکورد فایل عملیاتی سال جاری را ویرایش یا حذف کند؛ حجم تغییر، نام شیت یا حذف از «لیست کسبه» باعث درخواست تأیید، قرنطینه یا توقف مبتنی بر اختیار نمی‌شود.
- ✅ داده سال‌های آرشیوی پس از ورود نهایی تغییر نمی‌کند؛ عامل همگام‌سازی فایل‌های آرشیوی را پایش یا دوباره Import نمی‌کند. اصلاح رکورد تاریخی فقط درباره داده موجود در فایل عملیاتی سال جاری صدق می‌کند.

## 5. ورود داده و همگام‌سازی اکسل

### 5.1 تشخیص ذخیره فایل

- ✅ هدف این است که ذخیره با Ctrl+S یا ذخیره و بستن فایل باعث ورود تغییرات شود.
- ✅ عامل محلی ویندوز فقط مسیر دقیق فایل عملیاتی تنظیم‌شده را پایش می‌کند؛ فایل‌های موقت، `~$`، `.tmp` و Conflict Copyهای OneDrive نادیده گرفته می‌شوند و روش به VBA یا xlsm وابسته نیست.
- ✅ پس از آخرین رویداد فایل، عامل 2 ثانیه Debounce می‌کند و آزادشدن فایل، سالم‌بودن ZIP/XLSX و ثابت‌ماندن Size/mtime در دو بررسی پیاپی را می‌سنجد؛ صرف پایان تایمر برای Import کافی نیست.
- ✅ واردات باید اتمیک باشد: یا همه تغییرات معتبر ثبت شوند یا هیچ‌کدام.
- ✅ خواندن ناقص فایل نباید باعث حذف یا تغییر اشتباه اطلاعات شود.
- ✅ تکرار همان فایل و همان نسخه نباید تراکنش تکراری بسازد؛ واردات باید idempotent باشد.
- ✅ شناسه ردیف جدید پیش از ذخیره نهایی در حافظه زنده Excel درج می‌شود تا Save بعدی آن را بازنویسی نکند. اتصال رویداد `WorkbookBeforeSave` از عامل محلی/افزونه مورداعتماد استفاده می‌شود؛ فایل همچنان `.xlsx` می‌ماند و VBA داخل فایل قرار نمی‌گیرد.
- ✅ نوشتن شناسه توسط عامل فقط در ستون فنی مجاز است، Write داخلی از Save کاربر تفکیک می‌شود و نباید حلقه Import ایجاد کند. این قابلیت تا اجازه صریح و جداگانه کاربر فعال نمی‌شود.
- ✅ Agent برای ویرایش یا حذف، پنجره تأیید، سقف تعداد، توقف حجمی یا قرنطینه مبتنی بر تعداد/نوع تغییر ایجاد نمی‌کند. پس از یک Save کامل و معتبر، تمام ایجادها، ویرایش‌ها و Soft Deleteها در یک Commit اتمیک وارد SQLite محلی و Outbox می‌شوند.
- ✅ توقف Import فقط برای نامعتبر بودن فنی Snapshot مجاز است؛ مانند فایل ناقص یا درحال‌نوشتن، ساختار شکسته، ردیف فعالِ دارای داده با UUID خالی/تکراریِ غیرقابل‌حل یا داده‌ای که قواعد ضروری اعتبارسنجی را نقض می‌کند. نبود کامل یک UUID به علت حذف کل ردیف، حذف معتبر است و خطای شناسه محسوب نمی‌شود.
- 🧪 اتصال رویداد Excel، حفظ UUID در Saveهای بعدی، Copy/Insert/Sort و عدم ایجاد Recalculation یا حلقه Save باید روی کپی کنترل‌شده اثبات شود.
- ✅ چند Save پیاپی تا وضعیت نهایی در یک Import ادغام می‌شوند؛ Save تازه هنگام Import دقیقاً یک Follow-up Import می‌سازد و Hash یکسان صفر رویداد مالی/Sync تولید می‌کند.
- 🧪 قاعده فوق باید با Save، Replace/Rename و Conflictهای واقعی OneDrive آزموده و فاصله دو بررسی پایداری بدون تغییر قاعده کسب‌وکار تنظیم شود.

### 5.2 مرحله‌بندی واردات

1. ✅ پس از Debounce و Stability Probe موفق، یک Snapshot موقت و ثابت از کل فایل در فضای کنترل‌شده عامل ساخته می‌شود و Import فقط همان Snapshot را می‌خواند، نه فایل اصلی در حال استفاده. کامل‌بودن کپی و Hash فایل برای Audit کنترل می‌شود؛ ورود عملیاتی همچنان فقط از ستون‌های Whitelist است. Snapshot پس از Commit یا Failure کنترل‌شده پاک و هیچ تغییر نوشتاری در Excel ایجاد نمی‌شود.
2. ✅ استخراج فقط ستون‌های ورودی Whitelistشده از چهار شیت ورودی به ناحیه staging؛ فرمول‌ها، توابع، شیت‌های کمکی و خروجی‌های گزارش در مرز Sync نیستند.
3. ✅ اعتبارسنجی دو سطح دارد: خطای فنی سراسری مانند فایل/ساختار ناقص، نبود شیت یا ستون الزامی، UUID مبهم، تاریخ یا مبلغ ضروری نامعتبر و RS جدیدِ ساختاری ناسازگار، کل Snapshot را بدون Commit رد می‌کند. ابهام قابل‌حل مانند نام یا کالای ناشناخته و RS تاریخی چندکاندیدایی Raw را حفظ و Import سایر داده‌های معتبر را ادامه می‌دهد، اما انتشار Ledger/مانده/گزارش همان دامنه متأثر را تا رفع ابهام متوقف می‌کند. Formula/Cached Value معیوب با Raw معتبر خطای Import نیست.
4. ✅ Staging با آخرین Revision فعال براساس `record_id`/`party_id`، `source_hash` و `sheet_snapshot_hash` مقایسه می‌شود: شناسه تازه Insert، Hash ورودی متفاوت Edit و فقدان شناسه قبلی فقط در Snapshot کامل و معتبر `void` است. Sort، شماره ردیف و تغییر صرفاً فرمولی بی‌اثر و Save با Hash یکسان دارای صفر رویداد است؛ فایل آرشیوی وارد این مقایسه عملیاتی نمی‌شود.
5. ✅ تمام تغییرات یک Import با `import_id` واحد در یک تراکنش SQLite ثبت می‌شوند: آینه محلی، Revisionها، `void`ها، `change_events`/Outbox و کلیدهای دامنه متأثر یا همگی Commit می‌شوند یا همگی Rollback. حذف فیزیکی وجود ندارد، Idempotency از ثبت دوباره جلوگیری می‌کند و تعداد/نوع ویرایش یا حذف نیازمند تأیید نیست.
6. ✅ عامل محلی دامنه اثر را ثبت می‌کند و محاسبه مرجع Ledger/مانده پس از دریافت و Commit کامل رویدادها روی سرور انجام می‌شود. Insert مقدار جدید، `void` مقدار قبلی و Edit اجتماع شخص/تاریخ/کالای قبل و بعد را متأثر می‌کند؛ RS هر دو طرف را دربرمی‌گیرد. مانده از قدیمی‌ترین تاریخ متأثر بازسازی می‌شود، ولی فقط تاریخ‌های واقعاً تغییرکرده و قبلاً منتشرشده اصلاحیه می‌سازند و پیش از Commit کامل Backlog هیچ اصلاحیه‌ای تولید یا ارسال نمی‌شود.
7. ✅ جدول `change_events` خودِ صف پایدار محلی Sync است و داده در صف موازی تکرار نمی‌شود. رویدادها با `import_id` و Sequence به Checkpoint پیوسته تعلق دارند؛ سرور فقط پس از Commit کامل تا High-watermark اعلام‌شده، Jobهای مرکزی را با کلید Idempotency مبتنی بر «شخص + تاریخ مالی + نوع گزارش + نسخه Checkpoint» می‌سازد. تاریخ منتشرنشده اصلاحیه نمی‌سازد، Retry/Crash Job تکراری ایجاد نمی‌کند و تغییر پس از Checkpoint، Revision/Job مستقل بعدی است. ترتیب پایدار اجرا Sync کامل ← اصلاحیه‌های مرتب زمانی ← آخرین Catch-up ← PDF/عملیات عادی است و صف‌ها در SQLite/PostgreSQL می‌مانند، نه فقط RAM.
8. ✅ هر Import یک گزارش ساختاریافته با `import_id`، زمان شروع/پایان ایران، سال مالی و Hash فایل، شمارش هر چهار شیت، تعداد Insert/Edit/`void`/بدون‌تغییر، هشدار و دامنه متوقف، وضعیت Outbox/آخرین ACK، مدت پردازش و نتیجه نهایی ثبت می‌کند. Save موفق پیام جداگانه نمی‌فرستد؛ خلاصه آخر و تاریخچه از مسیر موجود «سلامت همگام‌سازی» مدیر ارشد دیده می‌شود. Failure، توقف دامنه، کمبود دیسک یا ماندگاری غیرعادی صف فوراً هشدار می‌دهد و فرمان ارسال رسمی تا نمایش و Commit کامل آخرین Import/Sync آغاز نمی‌شود؛ صفحه امنیتی تازه‌ای ایجاد نمی‌شود.

### 5.3 فایل‌های سالانه و مانده افتتاحیه

- ✅ در آغاز هر سال شمسی، فایل سال قبل آرشیو و فایل جدیدی با همان نام ساخته می‌شود.
- ✅ نام فایل برای تشخیص سال قابل اتکا نیست؛ سال مالی باید از محتوا یا مشخصه داخلی فایل تعیین شود.
- ✅ طبق O-75/ADR-0013، شواهد تاریخ خام پیش از تصمیم سال/آرشیو به‌صورت گزارش خالص سال‌های مشاهده‌شده، شمارش و ردیف‌های بدون تاریخ استخراج می‌شوند. نبود تاریخ یا مشاهده چند سال به‌تنهایی مجوز رد فایل، تغییر سال، حذف یا Import نیست؛ تعیین زمینه عملیاتی منبع و اتصال آن به رجیستری قبلی تصمیم جداگانه می‌خواهد.
- ✅ طبق O-76/[ADR-0014](docs/adr/ADR-0014-source-binding.md)، قرارداد منطقی اتصال سالانه شامل `source_id` ثابت UUIDv7 و سال اعلام‌شده، رجیستری با حداکثر یک منبع فعال و انتخاب وضعیت قبلی همان منبع است؛ آرشیو یا منبع ثبت‌نشده هیچ رجیستری عملیاتی انتخاب نمی‌کند. سال اعلام‌شده با سال تاریخ هر ردیف یکی فرض نمی‌شود. این تصمیم فقط معنای مشخصه داخلی آینده را تعیین می‌کند؛ خواندن مشخصه از همان Snapshot ثابت، ثبت اولیه، پیوستگی هویت/Revision و انتقال سال اتمیک باید پیش از Import عملیاتی جداگانه اثبات شوند.
- ✅ دیتابیس باید بین سال‌ها پیوسته بماند.
- ✅ پس از پایان ورود نهایی یک سال، فایل و داده آن سال آرشیوی و تغییرناپذیر محسوب می‌شوند؛ عامل محلی آنها را مانیتور یا مجدداً Import نمی‌کند. Hash نسخه نهایی فقط برای کنترل تمامیت و Backup نگهداری می‌شود و تغییر احتمالی فایل آرشیو نباید دفتر مرکزی را تغییر دهد.
- ✅ چون دیتابیس فعلی اطلاعات سال‌های پیش از 1405 را ندارد، مانده افتتاحیه پول و کالا در تاریخ 1405/01/01 یک‌بار وارد می‌شود.
- ✅ مانده افتتاحیه 1405 بر محاسبه مانده اثر دارد ولی تراکنش واقعی روز محسوب نمی‌شود و به‌تنهایی گزارش روزانه ایجاد نمی‌کند.
- ✅ از سال 1406 به بعد، ردیف‌های انتقال مانده ابتدای سال وارد دفتر مالی نمی‌شوند تا دوباره‌شماری رخ ندهد.
- ✅ انتقال‌های سال 1406 و بعد طبق O-05 فقط به‌عنوان نقطه کنترل با مانده محاسبه‌شده دیتابیس مقایسه می‌شوند.
- ✅ اختلاف بین مانده دیتابیس و انتقال اکسل طبق O-05 به مدیر ارشد اعلام و از اصلاح خودکار مانده جلوگیری می‌شود.
- ✅ اصلاح مانده افتتاحیه 1405، گزارش ویژه «اصلاح مانده افتتاحیه» همراه با آخرین مانده ایجاد می‌کند.
- ✅ طبق تضمین کاربر، تمام ردیف‌های انتقال مانده هر سال در `YYYY/01/01` ثبت می‌شوند؛ تاریخ 01/01 شرط لازم است ولی به‌تنهایی شرط کافی نیست.
- ✅ امضای ترکیبی پول عبارت است از: تاریخ 01/01، کد `H`، جهت بدهکار/بستانکار، مبلغ معتبر و شرح نرمال‌شده «مانده انتهای سال [سال قبل]». در داده 1405 هر 152 ردیف پولی افتتاحیه، ردیف‌های منبع 2 تا 153، همین امضا را دارند.
- ✅ امضای ترکیبی کالا عبارت است از: تاریخ 01/01، جهت ورود/خروج، کالا و مقدار معتبر و همان شرح سال قبل. در داده 1405 هر 80 ردیف کالایی افتتاحیه، ردیف‌های منبع 2 تا 81، همین امضا را دارند؛ در «خرید-فروش» هیچ ردیف 01/01 وجود ندارد.
- ✅ برای 1406 و بعد، طبقه‌بندی نهایی فقط وقتی پذیرفته می‌شود که مانده انتقالی هر «شخص + پول» و «شخص + کالا» با مانده بسته‌شده سال قبل در دیتابیس برابر باشد. ردیف نامنطبق وارد دفتر نمی‌شود، در Reconciliation متوقف و به مدیر ارشد اعلام می‌شود.
- ✅ نشانگر `entry_origin=opening_transfer` در دیتابیس از این قواعد مشتق می‌شود و افزودن ستون جداگانه Opening Balance به اکسل لازم نیست. برای 1405، به‌دلیل نبود دیتای 1404، امضای ترکیبی و بازبینی یک‌باره مبنای Baseline است.
- 🧪 این قاعده تصمیم‌شده است، اما باید روی نخستین فایل واقعی 1406 آزمون پذیرش شود.

### 5.4 منبع مقادیر محاسباتی

- ✅ موتور محاسبه باید از فرمول‌های اکسل مستقل باشد.
- ✅ فقط داده خام و ستون‌های ورودی چهار شیت مصوب Sync می‌شوند. فرمول‌ها، توابع، Cached Valueها، شیت‌های کمکی و خروجی‌های Query/گزارش اکسل وارد دفتر مالی نمی‌شوند و تغییر آنها بدون تغییر ورودی خام، رویداد Sync، Revision یا اصلاحیه نمی‌سازد.
- ✅ معماری داده سه لایه است: Raw Immutable برای ورودی خام و Revisionها؛ Canonical Ledger/Calculation Engine برای قواعد مالی و کالایی؛ و Query/View/Projection برای مانده، گزارش روزانه، اصلاحیه و PDF.
- ✅ مقادیر مشتق مانند مبلغ کل، وزن 750 و مانده در موتور دفتر و Queryهای دیتابیس دوباره تولید می‌شوند. View یا Materialized Projection برای سرعت مجاز است، اما با `calculation_version` نسخه‌دار، از Raw قابل بازتولید و فاقد جایگاه منبع حقیقت مستقل است.
- ✅ خطا یا قدیمی‌بودن Formula/Cached Value با Raw معتبر، Import را متوقف نمی‌کند. Raw ناقص/نامعتبر، UUID مبهم یا RS جدیدِ دارای ناسازگاری ساختاری کل Snapshot را پیش از Commit رد می‌کند؛ نام/کالای ناشناخته و RS تاریخی چندکاندیدایی طبق مرحله 3 فقط انتشار دامنه متأثر را متوقف می‌کنند و Raw و سایر Importها حفظ می‌شوند.
- ✅ خروجی معتبر اکسل فقط در مهاجرت و آزمون پذیرش برای Reconciliation استفاده می‌شود، نه به‌عنوان ورودی عملیاتی.
- 🧪 نتیجه محاسبه مستقل باید با نتیجه معتبر اکسل تطبیق کامل داشته باشد.

## 6. قواعد تراکنش‌های پولی

### 6.1 کد C

- ✅ C از Credit می‌آید.
- ✅ شخص وجه را به حساب خود مجموعه واریز کرده است؛ نام/شرح حساب مقصد در ستون توضیحات قرار دارد.
- ✅ متن پایه گزارش: «دریافت از شما: مبلغ» و در صورت وجود «حساب/شرح: توضیحات».
- ✅ واحد قطعی مبلغ منبع و نمایش گزارش «تومان» است و هیچ تبدیل ضمنی ریال/تومان انجام نمی‌شود.

### 6.2 کد D

- ✅ D از Debit می‌آید.
- ✅ مجموعه از حساب خود به حساب معرفی‌شده توسط شخص واریز کرده است؛ نام حساب مقصد در توضیحات قرار دارد.
- ✅ متن پایه گزارش: «پرداخت به درخواست شما: مبلغ» و «حساب مقصد: توضیحات».
- ✅ اگر توضیحات C یا D خالی باشد، مبلغ از مانده یا گزارش حذف نمی‌شود؛ متن عمومی «دریافت» یا «پرداخت» بدون نام حساب نمایش داده و نقص توضیحات برای مدیر ارشد/Audit ثبت می‌شود.

### 6.3 کد RS

- ✅ RS از Relation می‌آید.
- ✅ شخص بستانکار برای شخص بدهکار و به حساب/شرح ثبت‌شده در ستون توضیحات واریز کرده است.
- ✅ داده فعلی نشان می‌دهد RS معمولاً دو ردیف متناظر دارد: یک بستانکار و یک بدهکار با تاریخ، مبلغ و توضیحات یکسان.
- ✅ متن طرف بستانکار: «واریز شما بابت [نام بدهکار]: مبلغ» و در صورت وجود «حساب/شرح مقصد: توضیحات».
- ✅ متن طرف بدهکار: «واریز [نام بستانکار] بابت شما: مبلغ» و در صورت وجود «حساب/شرح مقصد: توضیحات».
- ✅ زوج RS در دیتابیس با relation_id متصل می‌شود و به مجاورت ردیف‌ها وابسته نیست.
- ✅ اصلاح یک طرف RS باید هر دو طرف و گزارش هر دو شخص را تحت تأثیر قرار دهد.
- ✅ زوج ناقص، ناسازگار یا مبهم RS در Canonical Ledger حل‌نشده می‌ماند و به مدیر ارشد اعلام می‌شود؛ Raw آن حفظ و Import سایر ردیف‌های معتبر ادامه می‌یابد، اما مانده و گزارش RS اشخاص متأثر تا رفع ابهام منتشر نمی‌شود.
- ✅ RS تاریخی با اشخاص دو طرف، تاریخ، مبلغ، صاحب حساب/توضیحات و جهت مالی تطبیق می‌یابد. تطبیق یکتای یک‌به‌یک `relation_id` قطعی می‌سازد؛ چند کاندیدای کاملاً یکسان هرگز تصادفی یا صرفاً براساس مجاورت جفت نمی‌شوند و به گروه ابهام برای بازبینی می‌روند.
- ✅ RSهای جدید با UUID پایدار و `relation_id` دیتابیسی نگهداری می‌شوند و پس از تشکیل رابطه به تطبیق اسمی وابسته نیستند.

### 6.4 کدهای H، HA و HS

- ✅ این کدها طلب و بدهی مجموعه بابت موارد متفرقه هستند.
- ✅ اگر ردیف بدهکار باشد، در گزارش به‌عنوان «طلب ما: مبلغ» نمایش داده می‌شود.
- ✅ اگر ردیف بستانکار باشد، در گزارش به‌عنوان «طلب شما: مبلغ» نمایش داده می‌شود.
- ✅ اگر توضیحات پر باشد، همان شرح عیناً در گزارش می‌آید.
- ✅ اگر توضیحات خالی باشد، فقط مبلغ و جهت طلب/بدهی می‌آید و عبارت «بدون شرح» نمایش داده نمی‌شود.
- ✅ H، HA و HS در متن گزارش و محاسبه مانده لحاظ می‌شوند.
- ✅ H، HA و HS هیچ تفاوتی در محاسبه یا متن پایه گزارش ندارند؛ تفاوت نام فقط برای فیلترکردن راحت‌تر است. کد خام هر ردیف بدون تبدیل نگهداری می‌شود.

### 6.5 اثر مانده پولی

- 🧪 قاعده استخراج‌شده از پروژه قدیمی باید با همه داده‌های واقعی تأیید شود:
  - مانده خالص پول = جمع خرید + جمع بستانکار پول − جمع فروش − جمع بدهکار پول.
  - نتیجه مثبت = طلب شخص/«طلب شما».
  - نتیجه منفی = طلب مجموعه/«طلب ما».
- ✅ مانده نهایی دیتابیس باید برای هر شخص با مانده معتبر اکسل برابر باشد.
- ✅ اعداد پولی فایل مرجع تومان هستند، بدون ضرب یا تقسیم و به‌صورت عدد صحیح منبع ذخیره می‌شوند و Metadata واحد نیز صریحاً `TOMAN` است؛ عنوان نمایش گزارش «تومان» خواهد بود.
- ✅ پول فقط با عدد صحیح تومان و بدون Float ذخیره و محاسبه می‌شود؛ مقدار خام منبع هیچ‌گاه با مقدار گرد‌شده جایگزین نمی‌شود.
- 🧪 اگر محاسبه‌ای مشتق پولی ایجاد کند، نقطه دقیق تبدیل به عدد صحیح و رفتار آن باید همراه قواعد O-25 با فرمول و نمونه واقعی اکسل تطبیق داده شود.

## 7. خرید، فروش و مانده کالا

- ✅ بخش خریدوفروش باید نوع معامله، کالا، مقدار، قیمت، تخفیف، مبلغ کل و توضیحات لازم را منعکس کند.
- ✅ بخش ورودوخروج باید نوع حرکت، کالا، مقدار، عیار، وزن 750، شماره فاکتور و توضیحات لازم را منعکس کند.
- 🧪 قاعده اولیه مانده کالا از پروژه قدیمی:
  - مانده کالا = فروش + ورود − خرید − خروج.
  - نتیجه مثبت = طلب کالایی شخص/«طلب شما».
  - نتیجه منفی = طلب کالایی مجموعه/«طلب ما».
- ✅ وزن، عیار و مقدار کالایی خام با Decimal و دقت کامل منبع نگهداری می‌شوند؛ Float و گردکردن تکراری مانده مجاز نیست.
- 🧪 برای آبشده، وزن 750 با Decimal و قاعده Excel-compatible `ROUND_HALF_UP` تا سه رقم اعشار محاسبه می‌شود؛ محل دقیق اعمال Round در سطح تراکنش یا تجمیع باید از فرمول واقعی و نمونه‌های مرزی مثبت و منفی اثبات شود.
- 🧪 برای کالاهای تعدادی پروژه قدیمی، مقدار صحیح استفاده شده است.
- ✅ مانده کالای تاریخی که مستقیم در اکسل موجود نیست باید با اجرای دفتر از مانده افتتاحیه تا تاریخ موردنظر محاسبه شود.
- ✅ Item Master قابل مدیریت با `item_id` پایدار، نام معیار، نام‌های جایگزین، واحد پایه، تعداد اعشار، نوع وزنی/تعدادی، نیاز به عیار، روش وزن 750 و وضعیت فعال ساخته می‌شود؛ افزودن کالا نیازمند تغییر کد نخواهد بود.
- ✅ کالای ناشناخته همراه نام خام وارد Staging/دیتابیس می‌شود و حذف نمی‌شود، اما مانده و گزارش کالایی وابسته به آن تا تعیین واحد و قاعده صحیح منتشر نمی‌شود. توقف به همان کالا و حساب‌های متأثر محدود است و ورود و پردازش پول یا حساب‌های نامرتبط ادامه می‌یابد.
- 🧪 فهرست واقعی کالاها، واحد، تعداد اعشار و قواعد تبدیل هرکدام باید پیش از G2 از داده مرجع استخراج و تأیید شود.
- ✅ در متن روزانه/اصلاحیه، تخفیف صفر، توضیحات خالی و شماره فاکتور خالی حذف می‌شوند و عبارت جایگزین «بدون توضیح» درج نمی‌شود. قیمت، مبلغ، کالا و مقدار الزامی همیشه نمایش داده و مبلغ/قیمت تومان با جداکننده هزارگان قالب‌بندی می‌شوند؛ صفر واقعی فیلد الزامی با مقدار خالی یکی نیست. در PDF ستون‌ها حفظ و مقدار خالی به‌صورت سلول خالی نمایش داده می‌شود.

## 8. انواع گزارش

### 8.1 گزارش رسمی روزانه

- ✅ فرمت تحویل: متن تلگرام، نه PDF و نه فایل دیگر.
- ✅ شامل چهار بخش اصلی است:
  1. تراکنش پولی
  2. خرید و فروش
  3. ورود و خروج کالا
  4. مانده نهایی پول و کالا
- ✅ هر گزارش شناسه یکتا دارد.
- ✅ گزارش برای صاحب حساب و همه حسابداران فعال او ارسال می‌شود.
- ✅ فقط اشخاصی مشمول گزارش‌اند که در روز هدف حداقل یک تراکنش مؤثر داشته‌اند.
- ✅ ارسال دسته‌ای پس از اجرای فرمان مدیر ارشد بدون پیش‌نمایش و تأیید میانی آغاز می‌شود.
- ✅ روز هدف همیشه تاریخ شمسی روز قبل است و عضویت رکورد در گزارش فقط از تاریخ ثبت‌شده در اکسل تعیین می‌شود؛ زمان Save یا Import ملاک مالی نیست.
- ✅ ورود اطلاعات روز قبل می‌تواند صبح روز جاری تکمیل شود؛ مدیر ارشد پس از تکمیل ورود و Sync موفق، فرمان ارسال را اجرا می‌کند.
- ✅ گزارش رسمی روز جاری ارسال نمی‌شود، چون روز هنوز باز و در حال دریافت تراکنش است. ارسال زمان‌بندی‌شده خودکار نیز در نسخه اول وجود ندارد.
- ✅ هر اجرای ارسال از Snapshot فنی سازگار در لحظه فرمان استفاده می‌کند. رکورد روز قبل که پس از ارسال وارد شود، تغییر تاریخی محسوب و از مسیر اصلاحیه همان تاریخ به‌علاوه آخرین مانده پردازش می‌شود.

### 8.2 اصلاحیه

- ✅ تغییر رکورد تاریخی باید گزارش کامل و صحیح همان روز را دوباره برای شخص و حسابدارانش ارسال کند.
- ✅ اصلاحیه عبارت روشن «اصلاحیه تاریخ [تاریخ روز اصلاح‌شده]» دارد.
- ✅ اصلاحیه آخرین مانده فعلی شخص را نیز نمایش می‌دهد.
- ✅ اگر در همان نوبت ارسال، شخص هم اصلاحیه معوق و هم گزارش رسمی تاریخ هدفِ روز قبل داشته باشد، ابتدا اصلاحیه و سپس گزارش روز قبل ارسال می‌شود؛ گزارش رسمی روز جاری ساخته نمی‌شود.
- ✅ اصلاحیه متن تلگرام است و PDF نیست.
- ✅ گزارش قبلی بازنویسی نمی‌شود؛ متن و داده آن به‌عنوان نسخه تاریخی باقی می‌ماند.
- ✅ اصلاحیه شناسه مستقل دارد و به report_id قبلی ارجاع می‌دهد.
- ✅ هر اصلاحیه منتشرشده دوره تصمیم سه‌ساعته مستقل دارد.
- ✅ در قطعی یا چند Save پیاپی، ابتدا کل Backlog تا یک نقطه سازگار Sync و Commit می‌شود؛ سپس همه تغییرات ارسال‌نشده با کلید «شخص + تاریخ مالی» از وضعیت نهایی به یک اصلاحیه تجمیع می‌شوند و نسخه‌های میانی در ممیزی باقی می‌مانند.
- ✅ فقط تاریخی که پیش‌تر گزارش رسمی آن منتشر شده اصلاحیه می‌سازد؛ تاریخ گزارش‌نشده از مسیر گزارش روزانه/Catch-up پردازش می‌شود. تاریخ‌های اصلاح‌شده مختلف، پیام‌های جدا و مرتب‌شده زمانی دارند.
- ✅ تغییر تازه پس از انتشار یک اصلاحیه، Revision و اصلاحیه مستقل جدید با `report_id` و ارجاع به نسخه قبلی می‌سازد؛ اصلاحیه منتشرشده بازنویسی نمی‌شود.
- ✅ بالای اصلاحیه تعداد ردیف‌های افزوده، ویرایش و حذف‌شده می‌آید؛ برای ردیف ویرایش‌شده فقط فیلدهای مالی تغییرکرده به صورت «قبلی ← جدید» و ردیف افزوده/حذف‌شده با عنوان روشن نمایش داده می‌شود. UUID، شماره سطر و Metadata فنی برای مصرف‌کننده پنهان است. سپس گزارش کامل صحیح همان تاریخ و آخرین مانده می‌آید؛ Diff خام کامل فقط برای مدیر ارشد/Audit باقی می‌ماند.

### 8.3 گزارش درخواستی

- ✅ صاحب حساب و حسابداران می‌توانند گزارش حساب خود را برای امروز، یک تاریخ یا بازه زمانی درخواست کنند.
- ✅ مدیر میانی و مدیر ارشد می‌توانند گزارش همه مشتریان را در تاریخ یا بازه دلخواه دریافت کنند.
- ✅ تمام گزارش‌های درخواستی فقط PDF هستند.
- ✅ PDF باید مانده ابتدای دوره، رویدادهای دوره و مانده انتهای دوره را بر اساس نیاز گزارش نشان دهد.
- ✅ مرجع ساختاری اولیه PDF، شیت «ریزحساب» در فایل اکسل مرجع است: راست‌به‌چپ، A4 عمودی، بازه تاریخ و نام شخص در بالا، جدول ده‌ستونی و ردیف مانده انتهای هر روز.
- ✅ PDF از نظر ساختار، ترتیب ستون‌ها و منطق نمایش به شیت «ریزحساب» وفادار است، اما بازتولید پیکسلی رنگ/فونت/فاصله‌ها الزام نیست. فقط `report_id`، زمان تولید به وقت ایران و شماره صفحه به سربرگ/پاورقی افزوده می‌شوند؛ شماره موبایل، Telegram User ID و واترمارک نمایش داده نمی‌شوند و PDF نسخه اول رمز ندارد.
- ✅ PDF درخواستی دکمه تأیید/رد، مهلت سه‌ساعته یا نتیجه مشترک ندارد؛ چرخه تصمیم فقط برای گزارش رسمی روزانه و اصلاحیه است.
- ✅ کاربر از درخواست بازه طولانی منع نمی‌شود، اما هر فایل PDF حداکثر یک سال شمسی را پوشش می‌دهد؛ بازه چندساله به فایل‌های مرتب زمانی با `parent_report_id` مشترک تقسیم می‌شود. اگر یک سال از سقف ردیف آزمون‌شده بزرگ‌تر باشد، همان سال نیز قطعه‌بندی می‌شود.
- ✅ هر درخواست‌کننده هم‌زمان یک کار PDF فعال و حداکثر سه درخواست Pending دارد؛ درخواست کاملاً یکسان Deduplicate می‌شود و صف در دیتابیس پایدار است، نه RAM.
- 🧪 سقف ردیف هر فایل باید با فونت فارسی، حافظه، زمان تولید و اندازه خروجی در آزمون بار G3/G7 تعیین شود.

### 8.4 ساختار متن پایه

- ✅ شکل کلی از پروژه قدیمی گرفته می‌شود ولی متن‌ها برای خوانایی، تفکیک جهت طلب و توضیح رابطه‌ها اصلاح می‌شوند.
- ✅ ترتیب پایه:

    عنوان گزارش و تاریخ

    نام صاحب حساب

    بخش تراکنش پولی

    بخش خرید و فروش

    بخش ورود و خروج کالا

    مانده نهایی پول و هر کالا

    شناسه گزارش

    دکمه‌های تأیید و رد

- ✅ در نسخه اول، ساختار متن تا حد امکان مشابه خروجی پروژه قدیمی است. خرید/فروش و ورود/خروج برای هر تراکنش یک سطر مستقل دارند؛ دریافت/پرداخت برای هر «صاحب حساب» درج‌شده در توضیحات، در هر تاریخ و جهت مالی، یک سطر تجمیعی دارد.
- 🧪 فرمول‌های پروژه قدیمی تأیید می‌کنند تجمیع پول با کلید «تاریخ + بدهکار/بستانکار + صاحب حساب» و جمع مبلغ انجام می‌شد؛ پیاده‌سازی جدید باید همین خروجی را همراه با قواعد جدید C/D/RS/H/HA/HS روی نمونه‌های واقعی تطبیق دهد.
- ✅ ترتیب ثابت عبارت است از نوع گزارش و تاریخ، نام شخص، تراکنش پولی، خریدوفروش، ورودوخروج، مانده‌ها، شناسه گزارش و مهلت تصمیم. بخش بدون تراکنش حذف می‌شود؛ هر ردیف با `•`، جداکننده ساده و ایموجی محدود فقط در عنوان بخش‌ها نمایش داده می‌شود و شماره سطر اکسل/شماره‌گذاری 100به‌بعد پروژه قدیمی استفاده نمی‌شود.
- ✅ اگر گزارش از محدودیت یک پیام بیشتر شود، قطعه‌ها با «بخش X از N» و report_id مشترک ارسال و دکمه‌ها فقط زیر آخرین قطعه قرار می‌گیرند.
- 🧪 واژه‌پردازی و خوانایی همین ساختار باید در G3 با چند گزارش واقعی فارسی تأیید شود.

## 9. تأیید، رد و اعلام مغایرت

- ✅ زیر گزارش رسمی برای صاحب حساب و همه حسابداران فعال، دکمه‌های مشترک «تأیید» و «رد/اعلام مغایرت» نمایش داده می‌شود.
- ✅ تمام گیرندگان یک وضعیت منطقی مشترک برای گزارش دارند.
- ✅ اعتبار دکمه‌ها سه ساعت است.
- ✅ اگر تا پایان سه ساعت تصمیمی ثبت نشود، نتیجه خودکار «تأیید» است.
- ✅ دلیل رد اختیاری است.
- ✅ پس از نهایی‌شدن، دکمه‌ها از پیام همه گیرندگان حذف و نتیجه برای همه آنها ارسال می‌شود.
- ✅ کنترل دسترسی فقط با مخفی‌کردن دکمه انجام نمی‌شود؛ هر Callback در سمت سرور دوباره مجوزسنجی می‌شود.
- ✅ اگر تأیید و رد با اختلاف حداکثر 100 میلی‌ثانیه دریافت شوند، تأیید ملاک است.
- ✅ پیاده‌سازی رقابت: تأیید معتبرِ اول نتیجه را فوراً تأیید می‌کند. اگر رد اول برسد، نتیجه تا پایان پنجره 100 میلی‌ثانیه‌ای موقت می‌ماند؛ تأیید معتبر با زمان دریافت سرور کوچک‌تر یا مساوی `reject_received_at + 100ms` نتیجه را تأیید می‌کند و در غیر این صورت رد نهایی می‌شود. قفل/تراکنش دیتابیس باید از دو نهایی‌سازی جلوگیری کند.
- ✅ زمان دریافت درخواست در سرور، مبنای سنجش هم‌زمانی است.
- ✅ طبق قواعد علت رد و منع Override، همه تصمیم‌های فردی موافق و مخالف برای ممیزی حفظ می‌شوند، حتی اگر نتیجه مشترک تأیید شود.
- ✅ کلیک «رد» فوراً رأی را ثبت می‌کند و سپس گزینه‌های «ارسال علت» و «بدون توضیح» نمایش داده می‌شوند؛ برای ارسال یک علت توسط هر ردکننده 10 دقیقه فرصت وجود دارد و نبود علت رأی را باطل نمی‌کند.
- ✅ علت‌های ردکنندگان جداگانه با هویت آنها برای مدیر ارشد و Audit نگهداری می‌شوند و خودکار برای همه اعضای حساب بازنشر نمی‌شوند. اگر تأیید در پنجره 100 میلی‌ثانیه‌ای برنده شود، علت مخالف حفظ ولی نتیجه مشترک تأیید می‌ماند.
- ✅ مهلت سه‌ساعته از `published_at` مشترک آغاز می‌شود: لحظه‌ای که اولین گیرنده تمام قطعات گزارش و دکمه‌ها را با موفقیت گرفته باشد. اگر هیچ گیرنده‌ای گزارش کامل نگرفته باشد، ساعت شروع نمی‌شود.
- ✅ گیرنده‌ای که با Retry پیش از انقضا گزارش را می‌گیرد فقط زمان باقی‌مانده را دارد؛ پس از انقضا یا نهایی‌شدن زودتر، گزارش بدون دکمه و همراه نتیجه نهایی برای او ارسال می‌شود.
- ✅ مدیر ارشد Override مستقیم رأی یا نتیجه تاریخی ندارد. او می‌تواند با دلیل اجباری، گزارش را در یک رویداد مدیریتی مستقل باطل کند یا پس از اصلاح داده، اصلاحیه تازه صادر کند؛ هویت مدیر، زمان، دلیل و report_id ثبت و به همه اعضای حساب اعلام می‌شود و رأی‌ها/نتیجه تاریخی دست‌نخورده می‌مانند.

## 10. وضعیت ارسال و ارسال مجدد

- ✅ نیازی به وضعیت‌های کاربری «آماده‌سازی» و «تأیید مدیر قبل از ارسال» نیست.
- ✅ وضعیت تحویل برای هر گیرنده جداگانه ثبت می‌شود: ارسال‌شده، ناموفق و ارسال مجدد.
- ✅ وضعیت تصمیم مالی از وضعیت تحویل تلگرام جداست.
- ✅ موفقیت کل یعنی تمام قطعه‌های گزارش برای تمام گیرندگان هدف با موفقیت ارسال شده باشند.
- ✅ `published` لحظه اولین تحویل کامل به یک گیرنده و مبنای Cursor تولید حساب است؛ `fully_delivered` فقط پس از تحویل کامل همه گیرندگان ثبت می‌شود. Cursor تحویل هر گیرنده مستقل است.
- ✅ در پایان ارسال موفق، مدیر ارشد خلاصه موفقیت دریافت می‌کند.
- ✅ در ارسال ناقص، نام شخص، نقش گیرنده و علت/نوع خطا برای موارد ناموفق نمایش داده می‌شود.
- ✅ مدیر ارشد امکان ارسال مجدد همه موارد ناموفق را دارد.
- ✅ ارسال مجدد idempotent است و پیام یا قطعه‌ای که قبلاً موفق تحویل شده دوباره ارسال نمی‌شود.
- ✅ هر تحویل حداکثر پنج تلاش خودکار دارد: تلاش اولیه و Retryها تقریباً در 30 ثانیه، 2 دقیقه، 10 دقیقه و 30 دقیقه پس از تلاش اولیه. در Rate Limit، زمان `retry_after` اعلام‌شده تلگرام مقدم است.
- ✅ خطاهای موقت مانند Timeout و اختلال شبکه Retry می‌شوند؛ خطاهای قطعی مانند Blockشدن بات، Chat Not Found یا غیرفعال‌شدن حساب بدون تکرار بی‌پایان مستقیماً «نیازمند اقدام مدیر» می‌شوند.
- ✅ پس از پایان سقف تلاش‌ها، مدیر ارشد خلاصه ناموفق‌ها را می‌گیرد و «ارسال مجدد همه ناموفق‌ها» یک دور جدید را فقط برای تحویل‌های ناقص آغاز می‌کند.
- ✅ Retry موفق پیش از پایان مهلت، همان deadline مشترک را نمایش می‌دهد؛ Retry پس از نهایی‌شدن، گزارش را بدون دکمه و با نتیجه قطعی تحویل می‌دهد.
- ✅ شکست یک گیرنده باعث ساخت گزارش تازه یا تکرار برای گیرنده موفق نمی‌شود؛ Retry همان report_id فقط Cursor گیرنده ناموفق را پس از تحویل کامل جلو می‌برد.

## 11. قطعی اینترنت و Catch-up

- ✅ در زمان قطع اینترنت ایران، تعامل کاربران ایرانی با تلگرام و ورود داده جدید به سرور ممکن نیست.
- ✅ ویرایش و حذف معتبر اکسل در زمان قطعی نیز بدون تأیید بات یا مدیر در SQLite و Outbox محلی ثبت می‌شود و پس از اتصال به‌ترتیب Sync خواهد شد.
- ✅ هیچ تغییر معتبر، گزارش یا اصلاحیه نباید به علت قطعی از بین برود.
- ✅ صف محلی پایدار، بسته‌های همگام‌سازی را تا اتصال مجدد نگه می‌دارد.
- ✅ پس از اتصال، اصلاحیه‌های معوق نیز ارسال می‌شوند.
- ✅ برای گزارش‌های روزانه از دست‌رفته، برای هر شخص فقط گزارش آخرین تاریخی ارسال می‌شود که از زمان آخرین گزارش در آن تراکنش داشته است؛ همه روزهای میانی جداگانه ارسال نمی‌شوند.
- ✅ اگر اصلاحیه و گزارش روز برای یک شخص وجود داشته باشد، اصلاحیه مقدم است.
- ✅ پس از Commit کامل Backlog، اصلاحیه‌های چند روز مختلف حذف نمی‌شوند، برای هر «شخص + تاریخ مالی» تجمیع و به ترتیب تاریخ ارسال می‌شوند؛ سپس فقط آخرین گزارش روزانه مشمول Catch-up فرستاده می‌شود.
- ✅ در Backlog، ابتدا Sync و Commit کامل داده، سپس اصلاحیه رسمی، بعد گزارش روزانه Catch-up و در پایان PDF درخواستی/عملیات عادی اجرا می‌شوند. VIP یا اولویت دستی مشتری وجود ندارد؛ داخل هر سطح، تاریخ مالی قدیمی‌تر و سپس party_id پایدار ملاک است.
- ✅ صف‌های Sync، تولید و ارسال جدا و پایدارند؛ Rate Limit و `retry_after` تلگرام مقدم است و مدیر ارشد پیشرفت تخلیه را می‌بیند. تعداد Worker و اندازه Batch ارسال در آزمون بار تنظیم می‌شوند.
- ✅ Cursor تولید گزارش حساب با `published`، یعنی اولین تحویل کامل، جلو می‌رود؛ `fully_delivered` فقط پس از تحویل کامل همه گیرندگان است. هر گیرنده Cursor تحویل جدا دارد. گیرنده ناموفق همان report_id را Retry می‌گیرد، گیرنده موفق تکرار نمی‌شود و اگر هیچ‌کس تحویل کامل نداشته باشد Cursor تولید جلو نمی‌رود.

## 12. هویت، شماره موبایل و اتصال تلگرام

### 12.1 قاعده هویت

- ✅ هر شخص مجاز با شماره موبایل ثبت‌شده در دیتابیس شناخته می‌شود.
- ✅ فقط Telegram User ID متصل به همان شخص می‌تواند اطلاعات حساب مربوط را ببیند.
- ✅ برای صاحب حساب و حسابدار، نام مشتری ورودی قابل انتخاب نیست؛ حساب هدف از عضویت احراز‌شده استخراج می‌شود.
- ✅ شماره موبایل هنگام فعال‌سازی به قالب استاندارد ایران تبدیل می‌شود.
- ✅ فعال‌سازی با دکمه رسمی اشتراک Contact انجام و تعلق Contact به فرستنده کنترل می‌شود.
- ✅ پس از اتصال، Telegram User ID کلید درخواست‌های روزمره است و هر هویت فقط یک Telegram User ID فعال دارد.
- ✅ ورود شماره‌های واقعی کاربران در آخرین مرحله پیش از راه‌اندازی انجام می‌شود.
- ✅ طبق تصمیم ورود شماره واقعی در فاز 8، توسعه و آزمون پیش از آن فقط با هویت‌ها و شماره‌های آزمایشی انجام می‌شود.
- ✅ هر کاربر مصرف‌کننده در هر ثبت‌نام فعال دقیقاً یک نقش دارد: صاحب حساب یا حسابدار. نخستین نقش فعال‌شده ملاک است و نقش دوم یا عضویت هم‌زمان به او افزوده نمی‌شود.
- ✅ تغییر نقش درجا مجاز نیست. برای ورود با نقش دیگر، دسترسی/عضویت قبلی باید طبق اختیارهای موجود حذف شود و شخص از مسیر ثبت‌نام یا دعوت نقش جدید دوباره وارد شود؛ هویت و تاریخچه ممیزی قبلی پاک نمی‌شود.
- ✅ جایگزینی Telegram User ID فقط با Contact خودِ حساب جدید، تطبیق شماره ثبت‌شده و تأیید مدیر ارشد انجام می‌شود. فعال‌سازی اتصال جدید و لغو اتصال قبلی اتمیک است؛ Callbackها و دسترسی‌های اتصال قبلی فوراً نامعتبر و تمام مراحل ممیزی می‌شوند. به اتصال قبلی در صورت امکان و به صاحب حساب/مدیر ارشد اعلان امنیتی داده می‌شود.
- ✅ تغییر خود شماره موبایل فقط پس از احراز خارج از بات توسط مدیر ارشد و ثبت دلیل انجام می‌شود؛ تاریخچه اتصال‌ها پاک نمی‌شود.

### 12.2 افزودن حسابدار

- ✅ صاحب حساب می‌تواند حداکثر سه حسابدار اضافه کند.
- ✅ حسابدار همه اطلاعات و قابلیت‌های مالی صاحب حساب را می‌بیند.
- ✅ حسابدار نمی‌تواند حسابدار اضافه یا حذف کند؛ این ممنوعیت هم در رابط و هم در سرور اعمال می‌شود.
- ✅ یک شخص نمی‌تواند حسابدار چند صاحب حساب باشد.
- ✅ صاحب حساب شماره موبایل حسابدار را وارد و پس از تأیید، لینک بات را برای او ارسال می‌کند.
- ✅ لینک دعوت یک‌بارمصرف، غیرقابل حدس و دارای اعتبار دقیق 24 ساعت است و شماره موبایل را آشکار نمی‌کند.
- ✅ دعوت‌های در انتظار نیز جزو سقف سه حسابدار محسوب می‌شوند.
- ✅ حسابدار هنگام پذیرش لینک باید Contact متعلق به خودش را ارسال کند و شماره با دعوت تطبیق داده شود.
- ✅ حذف حسابدار، دسترسی و ارسال‌های آینده را قطع می‌کند؛ پاک‌شدن نسخه‌های قبلی از دستگاه او قابل تضمین نیست.
- ✅ صاحب حساب می‌تواند دعوت را پیش از مصرف لغو یا دوباره صادر کند؛ صدور مجدد برای همان حساب و شماره، دعوت قبلی را فوراً باطل می‌کند. مصرف، انقضا، لغو و صدور مجدد ممیزی می‌شوند.
- ✅ صاحب حساب نمی‌تواند هم‌زمان حسابدار شخص دیگری باشد و حسابدار نیز نمی‌تواند هم‌زمان صاحب حساب باشد؛ قاعده نقش نخست برای همه کاربران مصرف‌کننده اعمال می‌شود.
- ✅ حسابدار با جریان دومرحله‌ای «خروج از این حساب» فقط عضویت خودش را فوراً لغو می‌کند؛ دسترسی آینده و Callbackهای فعال او قطع، صاحب حساب و مدیر ارشد مطلع و اتصال دوباره منوط به دعوت جدید می‌شود. رأی‌ها، تحویل‌های قبلی و ممیزی باقی می‌مانند و حذف پیام‌های قدیمی دستگاه او تضمین نمی‌شود.

### 12.3 محدودیت تلگرام

- ✅ بات پیش از آنکه فرد یک‌بار Start را بزند نمی‌تواند برای او پیام آغازین ارسال کند.
- ✅ کاربر ثبت‌شده‌ای که هنوز Start/احراز نکرده است وضعیت مستقل `pending_activation` دارد؛ ناموفق ارسال یا مشمول Retry تلگرام محسوب نمی‌شود و مدیر ارشد او را جدا از ناموفق‌ها می‌بیند.
- ✅ پس از فعال‌سازی، فقط آخرین گزارش رسمی مشمولِ آن حساب به‌علاوه گزارش‌های آینده ارسال می‌شود؛ همه گزارش‌های قدیمی دوره انتظار به‌صورت Backlog ارسال نمی‌شوند.
- ✅ طبق O-30، لینک دعوت فورواردشده توسط شخص غیرمجاز، بدون Contact متعلق به خود فرد و شماره مطابق قابل استفاده نیست.

## 13. نقش‌ها و مجوزها

| قابلیت | صاحب حساب | حسابدار | مدیر میانی | مدیر ارشد | وضعیت |
|---|---:|---:|---:|---:|---|
| مشاهده اطلاعات مالی حساب مربوط | بله | بله | همه حساب‌ها | همه حساب‌ها | ✅ |
| دریافت PDF درخواستی | حساب خود | حساب متصل | همه حساب‌ها | همه حساب‌ها | ✅ |
| دریافت متن روزانه و اصلاحیه | بله | بله | خیر | خلاصه مدیریتی | ✅ |
| تأیید یا رد گزارش | بله | بله | خیر | خیر | ✅ |
| مشاهده نتیجه تصمیم | حساب مربوط | حساب متصل | خیر | همه | ✅ |
| ابطال اداری گزارش با دلیل/صدور اصلاحیه | خیر | خیر | خیر | بله | ✅ |
| افزودن/حذف حسابدار | بله | خیر | خیر | بله | ✅ |
| افزودن/حذف/تعلیق کاربر | خیر | خیر | خیر | بله | ✅ |
| ارسال گروهی رسمی | خیر | خیر | خیر | بله | ✅ |
| ارسال مجدد ناموفق‌ها | خیر | خیر | خیر | بله | ✅ |
| مشاهده مالی همه مشتریان | خیر | خیر | بله | بله | ✅ |
| صفحه/دکمه مستقل رویدادهای امنیتی | خیر | خیر | خیر | خیر | ✅ خارج از کاربرد بات |

- ✅ مدیر میانی هیچ دسترسی برای تعلیق یا مدیریت کاربر ندارد.
- ✅ مدیر میانی به اطلاعات مالی همه مشتریان دسترسی دارد.
- ✅ سه قابلیت اختصاصی مدیر ارشد عبارت‌اند از ارسال رسمی، مشاهده/دریافت نتایج تأیید و رد، و مدیریت کاربران.
- ✅ ابطال اداری مدیر ارشد یک رویداد مستقل و قابل ممیزی است و Override رأی یا نتیجه تاریخی محسوب نمی‌شود.
- ✅ مدیر ارشد و مدیر میانی، متناسب با دسترسی مصوبشان به اطلاعات همه مشتریان، جست‌وجوی مجاز و ثبت‌شده دارند؛ منع جست‌وجوی آزاد مربوط به صاحب حساب و حسابداران است.
- ✅ نخستین مدیر ارشد با اسکریپت Bootstrap خارج از بات و شماره موبایل ازپیش‌ثبت‌شده ساخته می‌شود. مدیران بعدی را مدیر ارشد با تعیین نقش و شماره موبایل اضافه می‌کند.
- ✅ فعال‌سازی مدیر فقط با اشتراک Contact متعلق به خود او و تطبیق دقیق شماره ازپیش‌ثبت‌شده انجام می‌شود؛ پس از تطبیق، Telegram User ID به همان هویت متصل می‌شود. PIN، OTP و TOTP لازم نیست.
- ✅ راه‌اندازی با یک مدیر ارشد اصلی Bootstrapشده و یک مدیر ارشد پشتیبان با هویت جدا انجام می‌شود؛ پشتیبان را مدیر اصلی پیش از فعال‌سازی کاربران واقعی می‌سازد. حساب مشترک ممنوع، حذف/تعلیق آخرین مدیر ارشد فعال غیرمجاز و مدیر میانی اولیه اختیاری است و بعداً به تعداد لازم افزوده می‌شود.

## 14. تجربه مکالمه پیشنهادی بات

### 14.1 منوی صاحب حساب

- ✅ مانده فعلی
- ✅ تراکنش‌های امروز
- ✅ گزارش یک روز
- ✅ گزارش بازه زمانی
- ✅ سوابق گزارش‌ها
- ✅ حسابداران من
- ✅ راهنما

### 14.2 منوی حسابدار

- ✅ همان قابلیت‌های مالی صاحب حساب.
- ✅ بدون بخش «حسابداران من» و بدون عملیات افزودن یا حذف.

### 14.3 منوی مدیر میانی

- ✅ ابتدا «انتخاب مشتری» با جست‌وجوی مجاز و ثبت‌شده.
- ✅ سپس مانده فعلی، تراکنش‌های امروز، گزارش یک روز و گزارش بازه زمانی همان مشتری.
- ✅ ساخت PDF برای مشتری منتخب.
- ✅ بدون ارسال رسمی، نتایج تصمیم و مدیریت کاربران.

### 14.4 منوی مدیر ارشد

- ✅ همه امکانات مدیر میانی.
- ✅ «ارسال گزارش روز قبل».
- ✅ «ارسال‌های ناموفق» و ارسال مجدد.
- ✅ «نتایج تأیید/رد» و علت مغایرت.
- ✅ «مدیریت کاربران»، نقش‌ها، دعوت‌ها و تعلیق دسترسی.
- ✅ مشاهده سوابق عملیاتی مصوب و سلامت همگام‌سازی، بدون افزودن صفحه یا دکمه مستقل رویدادهای امنیتی.
- ✅ «سلامت همگام‌سازی» خلاصه آخرین Import، وضعیت Outbox/ACK و تاریخچه Importهای ساختاریافته را نشان می‌دهد؛ Save موفق اعلان جداگانه ندارد و فقط Failure، توقف دامنه، کمبود دیسک یا صف غیرعادی هشدار فوری می‌سازد.
- ✅ پیش از اجرای «ارسال گزارش روز قبل»، آخرین وضعیت Import/Sync نمایش داده می‌شود و تا Commit کامل داده‌ها ارسال رسمی شروع نمی‌شود.

- ✅ منوی سطح اول با Reply Keyboard ثابت و عملیات داخل مسیرها با Inline Button انجام می‌شوند.
- ✅ روش اصلی انتخاب تاریخ، تقویم شمسی سفارشی داخل بات با Inline Keyboard، پیمایش ماه قبل/بعد و انتخاب مستقیم روز است؛ این قابلیت به Date Picker بومی تلگرام وابسته نیست.
- ✅ تایپ تاریخ با قالب `YYYY/MM/DD` نیز پشتیبانی و ارقام فارسی، عربی و لاتین و جداکننده `/` یا `-` نرمال می‌شوند. برای بازه ابتدا شروع و سپس پایان انتخاب و خلاصه برای تأیید نمایش داده می‌شود.
- ✅ تاریخ نامعتبر، آینده یا پایان کوچک‌تر از شروع بی‌صدا اصلاح نمی‌شود؛ پیام خطای روشن، ورود مجدد و دکمه‌های «بازگشت» و «لغو» در همه مراحل وجود دارند. بازه بزرگ طبق O-35 تقسیم می‌شود.
- 🧪 Prototype مکالمه و Inline Calendar باید در G5/G6 با همه نقش‌ها، Callbackهای منقضی و متن فارسی آزموده شود.

## 15. معماری اجرا

### 15.1 گزینه‌های مطرح‌شده

| گزینه | شرح | وضعیت |
|---|---|---|
| اجرای کامل محلی | اکسل، دیتابیس و بات روی سیستم فعلی و اتصال تلگرام از طریق VPN | ⛔ گزینه اصلی انتخاب نشد |
| اجرای کامل سرور | انتقال برنامه و دیتابیس به سرور هتزنر فنلاند و همگام‌سازی داده اکسل | ⛔ گزینه اصلی انتخاب نشد |
| معماری ترکیبی | عامل اکسل و صف محلی در ایران؛ بات، API و دیتابیس مرکزی روی سرور | ✅ انتخاب کاربر |

### 15.2 معماری پیشنهادی ترکیبی

1. عامل همگام‌سازی محلی کنار فایل اکسل.
2. SQLite محلی برای آینه منبع، staging، تاریخچه Sync و Outbox.
3. ارتباط امن و تکرارپذیر با API سرور پس از دسترسی اینترنت.
4. دیتابیس مرکزی پیوسته برای دفتر مالی، هویت، گزارش و ممیزی.
5. موتور محاسبه مستقل از تلگرام.
6. Worker تولید گزارش و PDF.
7. Worker ارسال تلگرام و Retry.
8. بات تلگرام به‌عنوان رابط دسترسی.

- ✅ معماری ترکیبی به‌عنوان معماری مقصد تأیید شد.
- ✅ این معماری در قطعی اینترنت، تغییرات محلی را حفظ می‌کند و سرور تلگرام را از VPN سیستم حسابداری مستقل می‌سازد.
- ✅ در قطعی سراسری، کاربران داخل ایران همچنان ممکن است به تلگرام دسترسی نداشته باشند؛ معماری نمی‌تواند این محدودیت بیرونی را حذف کند.
- ✅ SQLite برای آینه و صف پایدار محلی و PostgreSQL برای دیتابیس مرکزی سرور انتخاب شد.
- ✅ پروتکل تفصیلی زیر، شامل Event Ledger، Batch، ACK، Idempotency، HTTPS عمومی و امضای Ed25519 اختصاصی دستگاه، توسط کاربر تأیید شد.

### 15.3 پروتکل تأییدشده همگام‌سازی محلی با سرور

1. عامل ویندوز پس از Save و پایدارشدن فایل، یک Snapshot موقت و ثابت می‌سازد؛ فایل در حال نوشته‌شدن مستقیم خوانده نمی‌شود.
2. چهار شیت در Staging خوانده و اعتبارسنجی می‌شوند. مقایسه براساس `record_id`/`party_id` است؛ Save بدون تغییر واقعی هیچ رویدادی به صف اضافه نمی‌کند.
3. در یک تراکنش SQLite، آینه محلی به‌روزرسانی و هر ایجاد، ویرایش یا حذف نرم فقط یک‌بار در جدول پایدار `change_events` ثبت می‌شود. همین جدول هم تاریخچه تغییر محلی و هم Outbox است و داده دوبار نگهداری نمی‌شود.
4. همگام‌سازی مالی یک‌طرفه از سیستم اکسل به سرور است؛ اطلاعات مالی ثبت‌شده در PostgreSQL از سمت بات به اکسل بازنویسی نمی‌شود. پاسخ سرور فقط ACK، خطا و تنظیمات مجاز است.
5. اتصال فقط به‌صورت خروجی از سیستم ایران به `HTTPS/443` برقرار می‌شود؛ هیچ پورت ورودی روی سیستم حسابداری باز نمی‌شود. TLS 1.3 و گواهی عمومی ACME هویت سرور را اثبات می‌کنند. عامل پس از ثبت اولیه یک‌بارمصرف، با کلید اختصاصی Ed25519 خود احراز می‌شود؛ کلید خصوصی در مخزن امن Windows باقی می‌ماند و mTLS، CA خصوصی، IP ثابت و Session ثابت یک‌ماهه استفاده نمی‌شوند.
6. هر رویداد شامل `device_id`، `event_id`، `import_id`، `sequence_no` افزایشی، `record_id/party_id`، `revision`، نوع عملیات `upsert/void`، سال و تاریخ مالی، زمان UTC ثبت، Payload، `payload_hash` و `previous_version_hash` است.
7. Worker قدیمی‌ترین رویداد تأییدنشده را در Batch حداکثر 500 رویداد یا 1 MiB، هرکدام زودتر پر شد، با Gzip ارسال می‌کند. هر Batch شناسه یکتا و بازه Sequence دارد. پیش از ارسال، Challenge یک‌بارمصرف سرور دریافت و پاکت Canonical شامل `device_id`، روش و مسیر، Challenge، `batch_id`، بازه Sequence و `body_hash` با Ed25519 امضا می‌شود.
8. سرور کلید عمومی ثبت‌شده، فعال‌بودن دستگاه، امضا، مصرف‌نشدن Challenge، Hash بدنه و ترتیب را کنترل و سپس در یک تراکنش PostgreSQL Commit می‌کند؛ درج تکراری با کلیدهای یکتای `(device_id, sequence_no)` و `event_id` بی‌اثر است. پاسخ شامل بالاترین Sequence پیوسته تأییدشده و خطاهای ردیفی است.
9. عامل فقط پس از ACK قطعی، وضعیت رویداد را `acknowledged` می‌کند. هیچ رویداد تأییدنشده‌ای پاک یا Skip نمی‌شود؛ خطای ساختاری Stream را متوقف، رکورد را قرنطینه و به مدیر ارشد هشدار می‌دهد.
10. Retry پس از تلاش فوری با Backoff پیشنهادی 1، 5 و 15 دقیقه و سپس هر یک ساعت با Jitter انجام می‌شود. با بازگشت اینترنت، Batchها به ترتیب Sequence تخلیه می‌شوند.
11. پس از تخلیه Backlog موجود، عامل یک اعلام Checkpoint امضاشده شامل `checkpoint_id` و High-watermark محلی می‌فرستد. Checkpoint فقط وقتی بسته است که سرور همه Sequenceها را تا آن High-watermark پیوسته Commit کرده باشد؛ ACK میانی یا Batch ناقص مجوز تولید گزارش نیست.
12. سرور پس از بستن Checkpoint، دامنه نهایی اشخاص/تاریخ‌ها را محاسبه و Jobهای پایدار را با کلید Idempotency «شخص + تاریخ مالی + نوع گزارش + نسخه Checkpoint» درج می‌کند. تاریخ منتشرنشده اصلاحیه نمی‌سازد؛ اصلاحیه‌ها مرتب زمانی و سپس فقط آخرین روز Catch-up هر شخص اجرا می‌شوند. Retry/Crash همان Job را ادامه می‌دهد و تغییر بعدی Checkpoint تازه و Revision مستقل می‌سازد.
13. ساعت سیستم برای ترتیب مرجع نیست؛ Sequence، Revision و Checkpoint ترتیب را تعیین می‌کنند. زمان داخلی UTC و نمایش و تاریخ مالی مطابق Asia/Tehran و تقویم شمسی است.

### 15.4 ظرفیت صف در قطعی سه‌ماهه

- اندازه فایل مرجع هنگام اندازه‌گیری 6,059,882 بایت (حدود 5.8 MiB) و SQLite نمونه 20,332,544 بایت (حدود 19.4 MiB) بود. بیشتر حجم SQLite فعلی مربوط به نگهداری خام سلول‌ها و Indexهای کنترل Import است، نه صف.
- داده فعلی شامل 14,798 ردیف مالی است که 232 ردیف افتتاحیه‌اند. برای 14,566 رویداد عملیاتی در 115 روز فعال، میانگین 126.7، صدک 95 برابر 249 و بیشینه 303 ردیف در روز مشاهده شد.
- شبیه‌سازی Payload کامل Sync روی همین داده، با UUID، Revision، Hash و Metadata، میانگین حدود 660 بایت برای هر رویداد خام نشان داد. برای محاسبه دیسک و احتساب Page و Index و فضای رشد، بودجه محافظه‌کارانه 4 KiB برای هر نسخه رویداد در نظر گرفته می‌شود.
- با نرخ تقویمی مشاهده‌شده، 90 روز حدود 8,400 رویداد و نزدیک 34 MiB فضای پایدار می‌خواهد. اگر هر 90 روز، روز فعال با میانگین کامل باشد حدود 11,400 رویداد و 46 MiB؛ اگر هر روز در سطح صدک 95 باشد حدود 22,400 رویداد و 90 MiB مصرف می‌شود.
- سناریوی فشار بسیار بالای 1,000 ایجاد/ویرایش/حذف واقعی در هر روز، طی 90 روز حدود 90,000 رویداد و نزدیک 360 MiB می‌خواهد. این اعداد شامل Backupهای تاریخی نیستند.
- Save تکراری یا Ctrl+S بدون تغییر، صفر رویداد می‌سازد. ذخیره Snapshot کامل یا ساخت 14,798 رویداد در هر Save ممنوع است؛ چنین طراحی اشتباهی می‌تواند صف را به ده‌ها گیگابایت برساند.
- Snapshot موقت فقط تا پایان Import می‌ماند. برای فایل فعلی حدود 5.8 MiB است؛ با Staging و WAL، سقف طراحی فضای موقت 128 MiB است و پس از Commit/Failure پاک‌سازی کنترل‌شده انجام می‌شود.
- صف روی دیسک SQLite است و با طول قطعی در RAM انباشته نمی‌شود. Batch 500تایی معمولاً کمتر از 5 MiB RAM کاری برای خود صف می‌خواهد. هدف آزمون عامل محلی کمتر از 100 MiB RAM در حالت بیکار و کمتر از 350 MiB هنگام Import/Sync است؛ CPU در حالت انتظار نزدیک به صفر و مصرف اصلی فقط چند ثانیه پس از Save خواهد بود.
- حداقل 2 GiB فضای آزاد رزروشده برای عامل محلی پیشنهاد می‌شود تا علاوه بر سناریوی فشار، WAL، Snapshot، رشد فایل و خطا جا داشته باشند. هشدار ظرفیت در آستانه‌های 1 GiB و 500 MiB ثبت می‌شود و Outbox تأییدنشده حتی در کمبود فضا حذف نمی‌شود.
- تحمل قطعی اینترنت به‌تنهایی خرابی دیسک سیستم ایران را پوشش نمی‌دهد؛ SQLite محلی و فایل اکسل باید در زمان قطعی نیز روی یک رسانه محلی دوم و جدا Backup شوند. طبق O-43 رمزگذاری در سطح خود برنامه الزام نیست، اما دسترسی فیزیکی و مجوزهای سیستم‌عامل باید محدود بمانند.

### 15.5 دامنه و گواهی TLS

- ✅ API همگام‌سازی باید روی یک دامنه یا زیردامنه اختصاصی مانند `sync.example.com` منتشر شود؛ رکورد DNS نوع A به IP سرور هتزنر اشاره می‌کند.
- ✅ الگوی مصوب `sync.<domain>` با رکورد A مستقیم به `${SYNC_SERVER_HOST}` از فایل `.env` محلی و غیرقابل‌Commit، اتصال Direct/DNS-only و انتشار عمومی فقط روی پورت 443 است. Reverse Proxy گواهی عمومی TLS را خاتمه می‌دهد، ولی پاکت امضاشده دستگاه بدون تغییر تا لایه API اعتبارسنجی می‌شود.
- ✅ گواهی عمومی TLS سرور از طریق ACME و با تمدید خودکار روی Reverse Proxy دریافت می‌شود؛ خرید گواهی تجاری الزامی نیست.
- ✅ گواهی عمومی دامنه، هویت سرور را برای عامل محلی اثبات می‌کند. کلید عمومی ثبت‌شده و امضای Ed25519 هر درخواست، هویت عامل مجاز را برای API اثبات می‌کنند؛ یکی جایگزین دیگری نیست.
- ✅ تمدید گواهی عمومی روی سرور فنلاند انجام می‌شود و قطعی اینترنت ایران مانع تمدید آن نخواهد شد.
- ✅ کاربر فعلاً دامنه‌ای انتخاب نکرده است؛ انتخاب/خرید نام دامنه و سرویس DNS یک ورودی اجرایی مؤجل تا پیش از فاز 4 است و معماری مصوب را باز نمی‌کند.
- ✅ طبق O-41، mTLS و CA خصوصی حذف شدند؛ بنابراین سرور یا رسانه آفلاین جدا برای نگهداری کلید CA لازم نیست. فقط کلید خصوصی هر دستگاه در همان میزبان و کلید عمومی آن در سرور نگهداری می‌شود.

### 15.6 معماری نرم‌افزار و Stack مصوب

> وضعیت این زیربخش: ✅ معماری و خانواده فناوری‌های O-52 تا O-56 تأیید شده‌اند. نسخه دقیق بسته‌ها هنگام ساخت Baseline فنی در `uv.lock` قفل می‌شود؛ Spikeها و آزمون‌های ذکرشده همچنان شرط عبور Gate مربوط هستند.

- ✅ تصمیم‌ها و برنامه شواهد معماری O-52 تا O-56 در پنج ADR پذیرفته‌شده ثبت شده‌اند: [ADR-0001](docs/adr/ADR-0001-monorepo-modular-monolith-python.md)، [ADR-0002](docs/adr/ADR-0002-windows-excel-agent.md)، [ADR-0003](docs/adr/ADR-0003-server-postgresql-durable-queue.md)، [ADR-0004](docs/adr/ADR-0004-telegram-persian-date-pdf.md) و [ADR-0005](docs/adr/ADR-0005-deployment-dependencies-quality.md). قرارداد Canonical/Hash منبع O-68 در [ADR-0006](docs/adr/ADR-0006-canonical-source-hashing.md)، قرارداد Snapshot کامل/Change Plan منبع O-69 در [ADR-0007](docs/adr/ADR-0007-full-snapshot-change-plan.md)، قرارداد Reader فقط‌خواندنی XLSX در O-70/[ADR-0008](docs/adr/ADR-0008-streaming-xlsx-source-reader.md)، قرارداد Acquisition/پاک‌سازی Snapshot پایدار در O-71/[ADR-0009](docs/adr/ADR-0009-stable-xlsx-snapshot-acquisition.md)، قرارداد هماهنگ‌کننده Save در O-72/[ADR-0010](docs/adr/ADR-0010-save-import-coordinator.md) و قرارداد اجرای Watcher در O-73/[ADR-0011](docs/adr/ADR-0011-source-watch-runtime.md) ثبت شده‌اند؛ اجرای همه آنها تابع Gate و Work Package شاهددار است.

- ✅ ساختار کلان: Monorepo با Modular Monolith برای بخش سرور و یک Agent مستقل ویندوز. هسته Domain و قواعد حسابداری Python خالص و مستقل از FastAPI، Telegram و دیتابیس می‌ماند؛ API/Webhook و Workerها Processهای جدا ولی مصرف‌کننده همان کد دامنه‌اند.
- ✅ زبان پایه: Python 3.13 برای Agent و سرور، با `pyproject.toml` و Lockfile قطعی. یک Spike روی کپی مجاز اکسل باید پیش از G1 سازگاری COM، بسته‌بندی و نسخه Python را اثبات کند.
- ✅ Agent ویندوز: `pywin32` برای رویداد Excel/COM و DPAPI، `watchdog` برای اعلان فایل، `zipfile` + `lxml.iterparse` برای خواندن Streaming ورودی‌های Whitelistشده XLSX، `sqlite3` برای Mirror/Outbox، `httpx` برای HTTPS، `cryptography` برای Ed25519، `Pydantic`/`pydantic-settings` برای قرارداد و تنظیمات و `PyInstaller` برای بسته قابل نصب. Agent در Session تعاملی کاربر با Task Scheduler/Startup اجرا می‌شود، نه به‌صورت Windows Service دارای Office Automation.
- ✅ سرور: `FastAPI` + `Pydantic v2`، `SQLAlchemy 2.0`، `Alembic`، `psycopg 3` و PostgreSQL 18. صف مرکزی در نسخه اول جدول‌های پایدار PostgreSQL با Lease، `due_at`، Attempt، Idempotency و `FOR UPDATE SKIP LOCKED` است؛ Redis/Celery/RabbitMQ فقط در صورت اثبات نیاز با ADR اضافه می‌شوند.
- ✅ تلگرام و گزارش: `aiogram 3` روی Webhook دارای Secret Token، `Jinja2` برای Template و `Playwright + Chromium` برای PDF فارسی RTL/A4 با فونت محلی Vazirmatn. `persiantools` همراه `zoneinfo` برای تقویم شمسی و `Asia/Tehran` استفاده و نسخه آن با آزمون‌های طلایی قفل می‌شود. WeasyPrint گزینه اصلی نیست، چون پشتیبانی متن دوجهته/RTL آن برای سند فارسی معیار پذیرش کافی ندارد.
- ✅ استقرار و کیفیت: Docker Compose روی یک سرور، Caddy برای Reverse Proxy و ACME، `uv` و `uv.lock` برای وابستگی‌ها، `pytest`/`pytest-asyncio`/`Hypothesis` و Testcontainers برای آزمون، `Ruff` و `mypy` برای کنترل ایستا و Structured JSON Logging برای عملیات. سرویس‌های Compose شامل Caddy، API/Webhook، Worker و PostgreSQL‌اند؛ Agent ویندوز Container نمی‌شود.
- ✅ ساختار اولیه مخزن پس از G0: `apps/local_agent`، `apps/server_api`، `apps/worker`، `packages/domain`، `packages/contracts`، `packages/persistence`، `packages/reporting`، `infra`، `tests`، `docs/adr` و `handoffs`.

## 16. زمان، تاریخ و ترتیب رویدادها

- ✅ تمام تاریخ‌ها و ساعت‌های نمایش‌داده‌شده براساس منطقه زمانی ایران هستند.
- ✅ تاریخ مالی به‌صورت شمسی و منطقه زمانی مرجع `Asia/Tehran` تفسیر می‌شود؛ برای گزارش رسمی، «روز قبل» نیز با همین مرجع تعیین می‌شود.
- ✅ طبق O-28/O-42، برای ترتیب دقیق، ممیزی و رقابت 100 میلی‌ثانیه‌ای، زمان UTC با دقت بالا نیز در داخل سیستم ذخیره می‌شود ولی نمایش کاربر ایران است.
- ✅ تاریخ خام اکسل بدون بازنویسی حفظ می‌شود؛ در دیتابیس یک تاریخ Canonical قابل Query به‌همراه اجزای شمسی سال/ماه/روز و کلید سال مالی نگهداری می‌شود. تبدیل دست‌نویس ممنوع، نسخه کتابخانه و `calculation_version` ثابت و منطقه زمانی `Asia/Tehran` مرجع است.
- ✅ کتابخانه تبدیل شمسی `persiantools` همراه `zoneinfo` انتخاب شد؛ نسخه دقیق در `uv.lock` قفل می‌شود.
- ✅ `persiantools 6.2.0` و `zoneinfo` در WP-03 با مرز نوروز، کبیسه/غیرکبیسه، پایان همه خانواده ماه‌ها، تبدیل رفت‌وبرگشت، رقم‌ها/جداکننده‌های مجاز و نامعتبر، Raw preservation و عبور نیمه‌شب `Asia/Tehran` آزمون شدند؛ تاریخ مالی همچنان مستقل از زمان Save/Import است.
- ✅ Save نزدیک نیمه‌شب مرز مالی را تغییر نمی‌دهد؛ تاریخ ثبت‌شده در شیت ورودی تنها ملاک روز حسابداری است و زمان Save/Import فقط برای Audit به وقت ایران ثبت می‌شود.

## 17. امنیت، حریم خصوصی و ممیزی

- ✅ کنترل مجوز روی تمام دستورات، دکمه‌ها، دریافت PDF و Callbackها در سرور انجام می‌شود.
- ✅ شناسه گزارش یا Party در Callback به‌تنهایی مجوز ایجاد نمی‌کند؛ عضویت و نقش دوباره کنترل می‌شود.
- ✅ مدیر ارشد می‌تواند دسترسی کاربران را تغییر یا لغو کند.
- ✅ موارد زیر در لاگ ممیزی ثبت می‌شوند: ورود و خروج داده، تغییر رکورد، اتصال/قطع تلگرام، دعوت حسابدار، تغییر نقش، درخواست گزارش، ارسال، خطا، Retry، تأیید، رد و دسترسی ردشده.
- ✅ هرجا شماره موبایل در مسیرهای مصوب هویت، دعوت، فهرست حسابداران یا مدیریت کاربر نمایش داده می‌شود، کامل، واضح و بدون Mask نمایش داده می‌شود. این تصمیم شماره را به گزارش روزانه، اصلاحیه، PDF، Handoff یا لاگ فنی‌ای که اساساً محل نمایش شماره نیست اضافه نمی‌کند.
- ✅ طبق O-41/O-43، توکن بات، کلیدهای همگام‌سازی و رمزهای دیتابیس خارج از کد و فایل اکسل و در مخزن مناسب Secret سیستم‌عامل/استقرار نگهداری می‌شوند.
- ✅ PDF موقت پس از تحویل موفق پاک می‌شود و لینک عمومی دائمی ندارد.
- ✅ احراز مدیران عامل دوم ندارد. بات فقط Contact خودِ فرستنده را می‌پذیرد، برابری `contact.user_id` با Telegram User ID فرستنده و تطبیق دقیق شماره نرمال‌شده با شماره ازپیش‌ثبت‌شده را کنترل می‌کند و همه تلاش‌ها را در ممیزی ثبت می‌کند.
- ✅ طبق O-43 رمزگذاری در سطح برنامه برای داده مالی، دیتابیس و Backup الزام نسخه اول نیست؛ این تصمیم عبارت «Backup رمزگذاری‌شده» در O-20 و بخش 18 را جایگزین می‌کند. رمزگذاری شفاف احتمالی دیسک/ارائه‌دهنده منع نمی‌شود، ولی جزو تعهد محصول نیست.
- ✅ تصمیم O-43 شامل اسرار احراز هویت نمی‌شود: HTTPS/TLS انتقال، Hash یک‌طرفه گذرواژه، محافظت کلید خصوصی/Token در مخزن امن سیستم‌عامل، منع ثبت Secret در کد/لاگ و کنترل دسترسی فایل همچنان الزامی‌اند.
- ✅ داده مالی، Snapshot نسخه گزارش و ممیزی اصلی در نسخه اول حذف خودکار ندارند؛ Backupها طبق چرخه 30 روزانه، 12 هفتگی و 12 ماهانه چرخش می‌کنند و PDF موقت پس از تحویل موفق حذف می‌شود.
- ✅ لاگ فنی/Debug پس از 90 روز و لاگ امنیتی پس از یک سال حذف می‌شوند؛ دفتر مالی، Snapshot گزارش و ممیزی اصلی در نسخه اول حذف خودکار ندارند. شماره موبایل و Telegram User ID پس از پایان دسترسی یک سال نگهداری و سپس، مگر وجود الزام قانونی یا اختلاف باز، ناشناس‌سازی می‌شوند.
- ✅ انتقال خودکار دسترسی براساس مالکیت شماره یا حساب تلگرام وجود ندارد. فقط Telegram User ID دقیقاً ثبت‌شده مجاز است؛ ID جدید حتی با همان شماره تا اجرای فرایند دستی و ممیزی‌شده O-22 هیچ دسترسی ندارد.

## 18. پشتیبان‌گیری و بازیابی

- ✅ حذف فیزیکی تراکنش و گزارش تاریخی نباید مسیر عادی باشد.
- ✅ دیتابیس و Outbox محلی هر ساعت به‌صورت خودکار روی محل فیزیکی دوم Backup می‌شوند؛ قرارگرفتن نسخه دوم روی همان دیسک قابل قبول نیست. رمزگذاری برنامه‌ای الزام نیست و دسترسی با مجوز سیستم‌عامل و کنترل فیزیکی محدود می‌شود.
- ✅ سرور مرکزی بازیابی نقطه‌ای با RPO حداکثر 15 دقیقه و نسخه‌ای در فضای ذخیره‌سازی جدا از خود سرور دارد؛ رمزگذاری در سطح برنامه طبق O-43 الزام نیست.
- ✅ نگهداری Backup برابر 30 نسخه روزانه، 12 نسخه هفتگی و 12 نسخه ماهانه است.
- ✅ Restore ماهانه آزموده می‌شود و هدف RTO حداکثر 4 ساعت است؛ وجود فایل Backup بدون آزمون بازیابی کافی نیست.
- ✅ طبق O-28/O-54، Outbox و کلیدهای Idempotency مانع گم‌شدن یا دوباره‌فرستادن عملیات پس از Crash می‌شوند.
- ✅ فایل موقت PDF درخواستی پس از تحویل موفق حذف می‌شود؛ داده و Snapshot منطقی گزارش در دیتابیس می‌ماند تا همان گزارش در صورت نیاز دوباره تولید شود.
- ✅ RPO مرکزی، RTO، تعداد نسخه‌ها، محل دوم و مدت نگهداری تعیین شدند.
- 🧪 فرایند Disaster Recovery هنگام ازبین‌رفتن سیستم محلی یا سرور باید پیش از G7 در Runbook نوشته و با Restore واقعی اثبات شود.

## 19. اعتبارسنجی و معیارهای پذیرش

### 19.1 داده و محاسبات

- 🧪 شمارش و محتوای چهار شیت با منبع برابر باشد.
- 🧪 مانده پول همه اشخاص با اکسل مرجع برابر و هیچ اختلاف توضیح‌داده‌نشده‌ای باقی نماند.
- 🧪 مانده هر کالا برای نمونه‌های منتخب و سپس کل داده اعتبارسنجی شود.
- 🧪 C، D، RS، H، HA و HS با توضیحات پر و خالی پوشش داده شوند.
- 🧪 RS صحیح، ناقص، تکراری و ویرایش‌شده آزمایش شود.
- 🧪 RS تاریخی یکتای یک‌به‌یک relation_id قطعی بگیرد؛ کلاستر چندکاندیدایی تصادفی جفت نشود، Raw و سایر Importها حفظ شوند و فقط انتشار مانده/گزارش اشخاص متأثر متوقف شود.
- 🧪 واردات تکراری نتیجه مالی را تغییر ندهد.
- 🧪 مرتب‌سازی ردیف‌ها اصلاحیه ایجاد نکند.
- 🧪 ویرایش و حذف فقط اشخاص و تاریخ‌های واقعاً متأثر را تغییر دهد.
- 🧪 حذف یا ویرایش گروهی، از جمله حذف از «لیست کسبه»، صرفاً به علت تعداد یا نوع شیت متوقف نشود و پس از Snapshot معتبر به تعداد صحیح Revision/Soft Delete ایجاد کند.
- 🧪 UUIDv7 تمام ردیف‌های فعال یکتا و پس از Edit/Sort ثابت بماند؛ Insert شناسه جدید و حذف، فقدان همان شناسه را ایجاد کند.
- 🧪 تغییر ورودی `source_hash` و تغییر معنای مالی `ledger_hash` را طبق Canonical Schema عوض کند؛ تغییر صرفاً مکانی `sheet_snapshot_hash` را عوض نکند.
- 🧪 افزودن شناسه هیچ Formula جدید یا وابستگی Recalculation به Excel اضافه نکند و Hashها فقط خارج از Excel محاسبه شوند.
- 🧪 تغییر یا جایگزینی احتمالی فایل آرشیوی توسط عامل Sync نادیده گرفته شود و هیچ Revision یا اثر مالی در دیتابیس مرکزی نسازد.
- 🧪 روی فایل مرجع فعلی، Canonicalization و Hash زیر 0.5 ثانیه و کل خواندن/Parse/Hash در عامل پس‌زمینه زیر 15 ثانیه و 128 MiB RAM بماند؛ نتیجه با Benchmark مرحله تحلیل مقایسه شود.
- 🧪 انتقال سال 1406 باعث دوباره‌شماری پول یا کالا نشود.
- 🧪 تمام اعداد پولی بدون تبدیل ده‌برابری با واحد `TOMAN` وارد شوند و جمع و مانده آنها با اعداد صحیح اکسل برابر باشد.
- 🧪 نام‌های خارج از «لیست کسبه» بدون حذف ردیف و بدون اتصال خودکار فازی در چهار گروه مصوب تعیین تکلیف شوند؛ هویت حل‌نشده نتواند تلگرام را فعال کند.
- 🧪 Item Master یکتایی `item_id`، Alias، واحد، اعشار و قواعد عیار/وزن 750 را کنترل کند و کالای ناشناخته فقط دامنه متأثر را از انتشار گزارش کالایی بازدارد.
- 🧪 هیچ محاسبه پولی از Float استفاده نکند؛ وزن و عیار خام با Decimal کامل حفظ شوند و ROUND_HALF_UP سه‌رقمی وزن 750 روی نمونه‌های `.xxx4`، `.xxx5` و `.xxx6` مثبت/منفی با نقطه گردکردن واقعی اکسل تطبیق یابد. جمع مانده نباید براثر Round چندمرحله‌ای تغییر کند.
- 🧪 فقط ورودی‌های Whitelistشده چهار شیت وارد Raw شوند؛ تغییر Formula/Function/Cached Value به‌تنهایی هیچ Event، Revision یا اصلاحیه نسازد و Formula خطادار با Raw معتبر Import را متوقف نکند. Ledger و Queryهای مستقل باید از Raw کاملاً قابل بازتولید و با نمونه معتبر اکسل برابر باشند.
- 🧪 Reader طبق O-70 روی فایل XLSX تولیدشده از داده کاملاً ساختگی، Header/Relationship/UUID، تفکیک متن/عدد/Null، فعالیت ردیف، Formula و محدوده خروجی Array/Data Table را بدون Float یا ورود Cache به Raw اثبات کند؛ فایل ناقص هیچ Snapshot جزئی نسازد، ردیف پنهان/فیلترشده حذف نشود و شواهد Streaming زمان/RAM جایگزین آزمون فایل واقعی یا G1 سرتاسری نشوند.
- 🧪 هر نگاشت Raw → Financial Event → Ledger Entry با `calculation_version` معین قطعی و تکرارپذیر باشد؛ Import یا Rebuild دوباره برای همان نسخه، Event یا پایه تکراری نسازد و Edit/Void فقط Revision/نسخه فعال صحیح را تغییر دهد.
- 🧪 مانده پول و کالا فقط از `ledger_entries` فعال قابل بازسازی باشد و حذف Projection و Rebuild کامل از Raw همان مانده، relation_id و گزارش را تولید کند.
- 🧪 Debounce دوثانیه‌ای، دو بررسی پایداری، نادیده‌گرفتن فایل موقت/Conflict، ادغام Saveهای متوالی، یک Follow-up هنگام Save وسط Import و صفر Event برای Hash یکسان در محیط واقعی OneDrive اثبات شوند.
- 🧪 Import فقط Snapshot ثابت را بخواند، Snapshot پس از موفقیت و شکست پاک شود و تغییر فایل اصلی هنگام Parse نتواند داده نیمه‌کاره یا حذف کاذب بسازد.
- 🧪 Acquisition نسخه‌دار O-71 روی Linux/Windows با دو مشاهده، تشخیص Replace/Mutation، Hash و اندازه یکسان در بازخوانی منبع/کپی، ارتقای اتمیک، پاک‌سازی بدون حذف بازگشتی و اتصال کامل به Reader اثبات شود؛ شاهد ساختگی جای آزمون Save/OneDrive و فایل واقعی را نمی‌گیرد.
- 🧪 هماهنگ‌کننده O-72 با ساعت یکنواخت ساختگی، مرز دقیق دو ثانیه، تطبیق مسیر و دو سوی Move، تجمیع Save، یک تلاش فعال و یک Follow-up، Retry منبع آماده‌نشده و حفظ خطا/مالکیت تا خروج Lease اثبات شود؛ خروجی Reader مجوز Commit یا جایگزین آزمون Observer/OneDrive و بازیابی پس از Crash نیست.
- ✅ Runtime نسخه‌دار O-73 در WP-08 با Observer بومی Windows/Linux روی پوشه‌های موقت کاملاً ساختگی، پایش غیر‌بازگشتی والد مسیر دقیق، اعلان اولیه پس از شروع، تحویل ترتیبی نتیجه پس از Cleanup، حفظ اعلان حین I/O و توقف/شکست همراه با جمع‌کردن Threadها را اثبات کرد؛ آزمون‌های رقابت و خطای Start/Callback/Join در بازبینی مستقل و CI دو سکویی PR #28 پذیرفته شدند و اصلاح پایداری آزمون WR-08 با ACK و انتظار تحویل دوم در PR #30 و CI دو سکویی Run `33670197344` تأیید شد. این شاهد تضمین عدم اتلاف رویداد سیستم‌عامل، بازیابی پایدار یا پذیرش Excel/OneDrive واقعی نیست و G1 باز می‌ماند.
- ✅ پیش‌بررسی O-74/WP-09 روی Snapshot کامل چهار شیت ساختگی، همه فیلدهای الزامی خالی را با ترتیب و شمارش قطعی گزارش کرد؛ تفکیک Null/Blank/Zero، حفظ مقدار/شناسه/Hash و ردیف خام، اختیاری‌ماندن فیلدهای مصوب و عدم رد نام/کالای ناشناخته در 53 آزمون اختصاصی و CI دو سکویی PR #32 اثبات و توسط Codex پذیرفته شدند. قبولی این بررسی به‌تنهایی مجوز Commit یا اثبات اعتبار مالی نیست؛ معیار محیط واقعی و G1 باز می‌مانند.
- ✅ شواهد سال O-75/WP-10 از `date_raw` تمام ردیف‌های سه شیت تراکنشی با parser پذیرفته‌شده استخراج شدند؛ حفظ ردیف بدون تاریخ، چند سال، فایل خالی و شمارش جداگانه «لیست کسبه»، ترتیب قطعی و دست‌نخوردگی Raw/UUID/Hash در 56 آزمون اختصاصی، بازبینی غیرنویسنده و CI دو سکویی PR #35 اثبات شد. نام فایل، ساعت سیستم، UUID، شرح و Formula/Cache شاهد سال نیستند؛ این گزارش مجوز Commit یا تصمیم آرشیو/حذف نمی‌سازد و معیار محیط واقعی و G1 باز می‌مانند.
- 🧪 ماتریس اعتبارسنجی ثابت کند خطای فنی سراسری هیچ Commitی نمی‌سازد، ولی نام/کالای ناشناخته و RS تاریخی چندکاندیدایی Raw و سایر Importها را حفظ و فقط انتشار دامنه متأثر را متوقف می‌کنند؛ Formula/Cached Value خراب با Raw معتبر بی‌اثر باشد.
- 🧪 مقایسه UUID/Hash برای Insert، Edit، `void`، Sort، تغییر صرفاً فرمولی و Save بدون تغییر دقیقاً رویدادهای مورد انتظار را بسازد و فایل آرشیوی هیچ رویداد عملیاتی ایجاد نکند.
- 🧪 تزریق خطا در هر مرحله تراکنش SQLite، آینه، Revision، `void`، `change_events`/Outbox و کلیدهای اثر را همگی Rollback کند؛ Retry همان Import هیچ رویداد تکراری نسازد.
- 🧪 تغییر شخص/تاریخ/کالا اجتماع دامنه قدیم و جدید، `void` دامنه قبلی و RS هر دو طرف را متأثر کند؛ محاسبه مرجع فقط پس از Commit کامل سرور انجام و هیچ اصلاحیه زودهنگام یا پیام روز میانی ساخته نشود.
- 🧪 تاریخ خام اکسل بدون تغییر حفظ و تاریخ Canonical، اجزای شمسی و سال مالی به‌صورت یک‌به‌یک تولید شوند؛ آزمون نوروز، کبیسه، پایان ماه، رفت‌وبرگشت، تاریخ نامعتبر و مرز زمانی `Asia/Tehran` با نسخه ثابت کتابخانه موفق باشد.
- 🧪 Agent با Python 3.13 و PyInstaller روی Windows هدف بسته‌بندی، پس از Login/Restart از Task Scheduler اجرا و بدون نیاز به Python نصب‌شده، Excel COM، DPAPI، SQLite و Sync آزمایشی را به‌درستی استفاده کند.
- 🧪 گزارش هر Import همه فیلدهای مصوب و شمارش Insert/Edit/`void`/بدون‌تغییر را دقیق ثبت کند؛ Save موفق اعلان جدا نسازد، هشدارهای Failure/توقف دامنه/کمبود دیسک/صف غیرعادی فوری باشند و تاریخچه از مسیر موجود سلامت همگام‌سازی مدیر ارشد قابل بازیابی باشد.

- 🧪 اتصال منطقی O-76/WP-11 باید با متادیتای صرفاً ساختگی انتخاب دقیق رجیستری منبع فعال، عدم انتخاب برای آرشیو/شناسه ناشناخته، رد ناسازگاری سالِ شناسه ثبت‌شده، حفظ فایل خالی و عدم ادغام رجیستری‌های سالانه را اثبات کند. آزمون مستقل Planner باید نشان دهد حذف در B هیچ هویت A را هدف نمی‌گیرد؛ این شاهد جای خواندن مشخصه از فایل، پیوستگی سراسری شناسه/Revision، ثبت نهایی یا انتقال سال اتمیک نیست.

### 19.2 گزارش

- 🧪 متن هر بخش با داده و قواعد مالی منطبق و برای فارسی راست‌به‌چپ خوانا باشد.
- 🧪 گزارش روزانه و اصلاحیه فقط متن باشند.
- 🧪 خرید/فروش و ورود/خروج هر تراکنش را در یک سطر جدا نشان دهند؛ دریافت/پرداخت با کلید «شخص گزارش + تاریخ + جهت مالی + صاحب حساب نرمال‌شده» تجمیع و مبلغ جمع‌شده با ردیف‌های منبع تطبیق داده شود. رابطه‌های متفاوت RS نباید به‌اشتباه یکی شوند.
- 🧪 PDF درخواستی فونت فارسی، صفحات کامل، مانده ابتدا و انتها و شناسه یکتا داشته باشد.
- 🧪 PDF از نظر ترتیب و معنای ستون‌ها، سطرهای مانده روزانه، راست‌به‌چپ و تنظیم A4 با شیت «ریزحساب» تطبیق داده شود.
- 🧪 PDF فقط `report_id`، زمان تولید ایران و شماره صفحه را به ساختار مرجع اضافه کند؛ فاقد شماره موبایل، Telegram User ID، واترمارک و رمز باشد و فایل موقت آن پس از تحویل موفق حذف و از Snapshot ثبت‌شده قابل بازتولید باشد.
- 🧪 متن، تخفیف صفر و فیلد اختیاری خالی را حذف ولی صفر واقعی و فیلدهای الزامی را حفظ کند؛ قالب تومان و سلول خالی PDF با قواعد مصوب برابر باشد.
- 🧪 C و D با توضیح خالی مبلغ صحیح را در مانده و متن عمومی دریافت/پرداخت حفظ کنند، نام حساب ساختگی نمایش ندهند و دقیقاً یک هشدار قابل‌پیگیری برای مدیر ارشد/Audit بسازند.
- 🧪 نسخه گزارش قبلی پس از اصلاح داده ثابت بماند.
- 🧪 اصلاحیه شمارش افزوده/ویرایش/حذف، Diff مالی قبلی ← جدید، گزارش کامل همان تاریخ و آخرین مانده را بدون افشای UUID/شماره ردیف نمایش دهد و Diff خام در Audit بماند.
- 🧪 گزارش طولانی بدون گم‌شدن بخش‌ها به چند پیام تقسیم شود.
- 🧪 گزارش ترتیب مصوب بخش‌ها، حذف بخش خالی، Bullet، ایموجی محدود، جداکننده ساده و «بخش X از N» را رعایت و دکمه را فقط زیر قطعه آخر نشان دهد.
- 🧪 PDF چندساله به سال‌های مرتب با parent_report_id مشترک تقسیم، سال بسیار بزرگ با سقف ردیف آزمون‌شده قطعه‌بندی و صف یک Active/سه Pending و Dedup در دیتابیس پایدار اعمال شود.
- 🧪 سیاست پنج تلاش در زمان‌های مصوب، تقدم `retry_after`، توقف زودهنگام خطای قطعی و ارسال مجدد فقط قطعات/گیرندگان ناموفق بدون تکرار تحویل موفق آزمایش شود.
- 🧪 تخلیه کامل Backlog پیش از تولید، تجمیع چند Revision در یک «شخص + تاریخ»، ترتیب اصلاحیه‌های چندروزه، تفکیک تاریخ گزارش‌نشده از اصلاحیه و ساخت اصلاحیه تازه پس از انتشار قبلی آزمایش شود.

### 19.3 تصمیم مشترک

- 🧪 تأیید تنها، رد تنها، بدون پاسخ تا سه ساعت و چند پاسخ هم‌نوع آزمایش شود.
- 🧪 ردِ اول و تأیید با اختلاف کمتر، دقیقاً برابر و بیشتر از 100 میلی‌ثانیه آزمایش شود؛ دو Callback هم‌زمان و Retry تکراری تحت قفل/تراکنش فقط یک نتیجه نهایی بسازند و مرز `<= 100ms` همیشه به تأیید برسد.
- 🧪 دکمه‌های قدیمی پس از نهایی‌شدن از نظر Backend غیرقابل استفاده باشند.
- 🧪 نتیجه برای تمام اعضای حساب اعلام و علت اختیاری درست ثبت شود.
- 🧪 مهلت 10دقیقه‌ای علت، یک علت برای هر ردکننده، چند علت از افراد مختلف و حفظ علت مخالف در نتیجه مشترک تأیید آزمایش شوند.
- 🧪 در ارسال ناقص، حالت‌های هیچ تحویل کامل، اولین تحویل کامل، Retry پیش از deadline و Retry پس از نتیجه نهایی با یک `published_at` مشترک آزمایش شوند.
- 🧪 published، fully_delivered، Cursor تولید حساب و Cursor تحویل هر گیرنده در حالت موفق مالک/ناموفق حسابدار و حالت هیچ تحویل کامل، بدون بازتولید یا تکرار گیرنده موفق آزمایش شوند.
- 🧪 PDF درخواستی هیچ تصمیم مشترک یا دکمه تأیید/رد نسازد.
- 🧪 مدیر ارشد نتواند با API یا Callback دست‌کاری‌شده رأی/نتیجه تاریخی را Override کند؛ ابطال اداری باید رویداد جدا با دلیل، هویت، زمان، report_id و اعلان اعضا بسازد.

### 19.4 دسترسی و امنیت

- 🧪 صاحب حساب و حسابدار نتوانند شناسه حساب دیگری را جعل کنند.
- 🧪 کاربر ثبت‌شده ولی فعال‌نشده `pending_activation` باشد، در فهرست ناموفق‌ها یا Retry تلگرام وارد نشود و پس از Start/احراز فقط آخرین گزارش رسمی مشمول و گزارش‌های آینده را بگیرد، نه تمام Backlog دوره انتظار.
- 🧪 شخص حسابداری بدون Telegram، حسابدار خارج از «لیست کسبه» و صاحب حساب متصل، هویت‌های جدا داشته باشند؛ ادغام نام/شماره یا تغییر نقش نتواند party_id مالی یا سابقه دفتر را جابه‌جا کند.
- 🧪 حسابدار حتی با Callback دست‌کاری‌شده نتواند حسابدار اضافه یا حذف کند.
- 🧪 مدیر میانی نتواند کاربر را مدیریت، تعلیق یا گزارش رسمی ارسال کند.
- 🧪 دعوت فورواردشده بدون شماره مطابق فعال نشود.
- 🧪 یک حسابدار نتواند عضو دو حساب شود.
- 🧪 کاربر مصرف‌کننده نتواند نقش دوم بگیرد یا هم‌زمان صاحب حساب و حسابدار باشد؛ پس از حذف معتبر، ثبت‌نام مجدد با نقش تازه ممکن و همه مجوزهای نقش قبلی لغو شده باشند.
- 🧪 مدیر فقط در صورت ثبت قبلی توسط اسکریپت/مدیر ارشد و اشتراک Contact متعلق به خودش فعال شود؛ Contact تایپی، فورواردی یا متعلق به فرد دیگر رد شود و هیچ OTP درخواست نشود.
- 🧪 یک مدیر اصلی و یک مدیر ارشد پشتیبان با هویت جدا فعال شوند؛ حساب مشترک و حذف/تعلیق آخرین مدیر ارشد فعال از Backend ممکن نباشد.
- 🧪 جایگزینی Telegram User ID بدون Contact منطبق یا تأیید مدیر ارشد رد شود؛ تعویض معتبر، اتصال قدیمی و همه Callbackهای آن را اتمیک باطل و اعلان امنیتی/Audit ایجاد کند.
- 🧪 خروج دومرحله‌ای حسابدار فقط عضویت خودش را لغو کند، دسترسی آینده را فوراً ببندد، رأی و ممیزی قبلی را نگه دارد و برای بازگشت دعوت تازه بخواهد.
- 🧪 دعوت حسابدار دقیقاً پس از 24 ساعت منقضی، یک‌بارمصرف و فاقد شماره در Token باشد؛ صدور مجدد قبلی را باطل کند، دعوت Pending در سقف سه نفر شمرده شود و همه چرخه‌ها Audit شوند.
- 🧪 در همه مسیرهای مصوب نمایش شماره، شماره موبایل کامل و بدون Mask دیده شود؛ شماره به گزارش روزانه، اصلاحیه، PDF، Handoff و لاگ فنی فاقد نیاز اضافه نشود و مجوز Backend مسیر نمایش کنترل شود.
- 🧪 هیچ نقش بات صفحه یا دکمه مستقل رویدادهای امنیتی نداشته باشد؛ دکمه‌ها و کاربردهای قبلی بات بدون توسعه O-63 باقی بمانند و سوابق عملیاتی/سلامت همگام‌سازی موجود مدیر ارشد به قابلیت امنیتی تازه تبدیل نشود.
- 🧪 Telegram User ID متفاوت، حتی با Contact دارای همان شماره، پیش از تعویض دستی O-22 هیچ داده یا Callback فعالی دریافت نکند.
- 🧪 ثبت اولیه با Credential یک‌بارمصرف فقط یک بار کلید عمومی دستگاه را فعال کند؛ Credential مصرف‌شده/لغوشده، کلید ثبت‌نشده، امضای نامعتبر، دستگاه لغوشده، Challenge تکراری و بدنه تغییریافته همگی پیش از Commit رد شوند.
- 🧪 Retry همان Batch با Challenge و امضای تازه، به کمک `batch_id`، Sequence و Idempotency هیچ رویداد تکراری نسازد؛ لغو و چرخش کلید دستگاه نیز بدون تغییر دامنه یا کلید سایر دستگاه‌ها اثبات شود.
- 🧪 زنجیره گواهی عمومی دامنه، تمدید خودکار ACME و هشدار پیش از انقضا روی محیط آزمایشی اثبات شوند.
- 🧪 `sync.<domain>` انتخاب‌شده با A مستقیم به `${SYNC_SERVER_HOST}` از فایل `.env` محلی و غیرقابل‌Commit، DNS-only، فقط پورت 443، ACME خودکار و اعتبارسنجی انتهابه‌انتهای پاکت Ed25519 در API روی محیط فاز 4 اثبات شود.
- 🧪 هیچ گذرواژه، Token یا کلید خصوصی به‌صورت متن آشکار در کد، Excel یا لاگ قرار نگیرد؛ Backup بدون رمزگذاری برنامه‌ای فقط برای حساب‌های سیستم‌عامل مجاز و محل فیزیکی کنترل‌شده قابل خواندن باشد.
- 🧪 حذف 90روزه لاگ فنی، حذف یک‌ساله لاگ امنیتی و ناشناس‌سازی شناسه‌های هویتی یک سال پس از پایان دسترسی، با استثنای اختلاف/الزام قانونی، بدون حذف دفتر مالی و ممیزی اصلی آزموده شود.
- 🧪 Reply Keyboard نقش‌ها، Inline Button مسیرها، تقویم شمسی ماه‌به‌ماه، ورودی تایپی با ارقام مختلف، انتخاب بازه، Back/Cancel و Callback منقضی بدون دورزدن مجوز آزمایش شوند.

### 19.5 قطعی و بازیابی

- 🧪 Save هنگام قطع اینترنت در صف محلی باقی بماند.
- 🧪 هنگام قطع کامل اینترنت، ویرایش و حذف تکی یا گروهی بدون تأیید، پنجره یا محدودیت حجمی وارد Outbox محلی شوند و پس از اتصال بدون گم‌شدن یا تکرار Sync شوند.
- 🧪 پس از اتصال، همه اصلاحیه‌ها و فقط آخرین روز مشمول Catch-up طبق قاعده ارسال شوند.
- 🧪 Backlog با اولویت Sync → اصلاحیه → Catch-up → PDF/عادی، بدون VIP، با ترتیب تاریخ/party_id، صف‌های پایدار جدا، `retry_after` و نمایش پیشرفت مدیر تخلیه شود.
- 🧪 هیچ اصلاحیه‌ای پیش از دریافت و Commit کامل Backlog ساخته نشود و تغییرات میانی یک تاریخ پیام‌های تکراری ایجاد نکنند.
- 🧪 Checkpoint امضاشده تا Commit پیوسته همه Sequenceها به High-watermark بسته نشود؛ Batch ناقص یا ACK میانی Job نسازد و Retry/Crash با کلید «شخص + تاریخ + نوع گزارش + نسخه Checkpoint» اصلاحیه یا Catch-up تکراری ایجاد نکند.
- 🧪 تاریخ منتشرنشده اصلاحیه نسازد، تغییر پس از Checkpoint Revision/Job مستقل تولید کند و ترتیب Sync کامل ← اصلاحیه زمانی ← آخرین Catch-up ← PDF/عادی در قطع و وصل حفظ شود.
- 🧪 فرمان ارسال رسمی آخرین Import/Sync ناتمام را روشن نمایش دهد و تا Commit کامل شروع نشود؛ Save موفق باعث سیل اعلان نشود و شکست یا صف غیرعادی پنهان نماند.
- 🧪 قطع برنامه در میانه Import یا Send باعث داده نیمه‌کاره یا پیام تکراری نشود.
- 🧪 دو Worker آزمایشی PostgreSQL با Lease و `FOR UPDATE SKIP LOCKED` یک Job را هم‌زمان Claim نکنند؛ Crash پس از Claim، انقضای Lease و Retry همان Idempotency Key نیز Job یا پیام تکراری نسازد.
- 🧪 Backlog مصنوعی 90روزه با نرخ مشاهده‌شده و سناریوی فشار 1,000 تغییر در روز، بدون گم‌شدن، تکرار یا عبور از سقف منابع مصوب تخلیه شود.
- 🧪 Batch تکراری، خارج از ترتیب، دارای Hash نادرست، ACK گم‌شده و قطع اتصال وسط ارسال آزمایش شود.
- 🧪 Save بدون تغییر واقعی، حتی در قطعی، اندازه Outbox را افزایش ندهد.
- 🧪 Backup روی محیط آزمایشی با موفقیت Restore شود.
- 🧪 Backup ساعتی محلی روی محل فیزیکی دوم، PITR مرکزی با RPO 15 دقیقه، چرخه 30/12/12 و Restore ماهانه با RTO حداکثر 4 ساعت در سناریوی قطع و خرابی واقعی اثبات شوند.

### 19.6 حاکمیت اجرا، بازبینی و انتشار کد

- 🧪 هر Work Package اجراشده توسط Antigravity دارای Handoff کامل، Acceptance Matrix، فرمان‌ها و Exit Code، نتایج آزمون، شواهد، ریسک و Rollback باشد؛ ادعای متنی بدون شاهد پذیرش نمی‌شود.
- 🧪 Antigravity نتواند وضعیت Roadmap، Gate یا کار خود را تأیید کند و پس از تحویل برای بازبینی متوقف شود؛ Codex مدیر پروژه Handoff را مستقل بازبینی و درباره پذیرش Work Package، Gate و Merge تصمیم می‌گیرد و مالک محصول آزمون پذیرش نهایی و اصلاحات را اعلام می‌کند.
- 🧪 هر تغییر روی Branch/Worktree نام‌گذاری‌شده انجام و هیچ Commit مستقیم یا Force Push روی `main` ممکن نباشد؛ ادغام فقط پس از Handoff معتبر، آزمون موفق و بازبینی Codex مدیر پروژه انجام شود.
- 🧪 تغییر مؤثر بر قواعد مالی، امنیت، هزینه، سرویس بیرونی، داده واقعی، رفتار قابل‌مشاهده یا Acceptance Criteria بدون ADR و تصمیم Codex مدیر پروژه اجرا نشود؛ اگر اقدام نیازمند دسترسی ویژه O-47، هزینه تازه یا تغییر جهت صریح مالک باشد، مدیر پروژه آن بخش را تا دریافت اختیار لازم متوقف می‌کند.
- 🧪 هیچ Agentی بدون اجازه صریح فایل Excel مرجع، شماره/داده واقعی، Telegram تولید، سرور/DNS تولید، Secret، Migration مخرب یا وضعیت‌های Roadmap را تغییر ندهد.
- ✅ مخزن عمومی مستقل با یک Root Commit تازه ساخته و کل تاریخ آن پیش از انتشار Scan شد؛ Excel، SQLite، داده/شماره واقعی، Credential، IP/شناسه مخزن خصوصی و Artifact داخلی در آن وجود ندارند و مقادیر استقرار فقط با Placeholder محیطی نمایش داده می‌شوند.
- ✅ Antigravity در نشست واقعی نسخه Project Scope مخزن عملیاتی را از طریق مسیر Global در `/skills` کشف کرد و Dry Run ارائه‌شده توسط کاربر نشان داد پیش از G0 کدنویسی را رد و Roadmap/Excel/داده واقعی/`main`/Production را محافظت می‌کند.
- ✅ نخستین Work Package شاهددار پس از G0 با Handoff کامل، Acceptance Matrix، Test Results، توقف مجری و دو چرخه بازبینی/اصلاح مستقل انجام شد؛ WP-01 در PR #7 با Merge Commit `4f9ad3eae9ccfe376a917ec7a9653bcc7e7fc128` پذیرفته شد.
- ✅ `uv` نسخه `0.12.7` و `uv.lock` نصب Frozen همه Workspaceها/Groupها را روی Linux و Windows تکرار کردند و CI نهایی Run `33324657599` در Lock check، Ruff، mypy و 21 آزمون روی هر دو سیستم موفق شد.
- 🧪 Docker Compose آزمایشی شامل Caddy، API/Webhook، Worker و PostgreSQL با Healthcheck، Restart و Migration کنترل‌شده بالا بیاید و بازیابی پس از Restart میزبان را بدون از دست‌رفتن Job اثبات کند.

## 20. فازهای اجرایی و دروازه‌ها

### قاعده مشترک همه فازها

- ✅ کاربر مالک محصول، تعیین‌کننده چشم‌انداز و آزمون‌کننده پذیرش نهایی است؛ Codex مدیر پروژه، نگهدارنده Roadmap، تعریف‌کننده Work Package، بازبین مستقل، تصویب‌کننده Gate و Merge است؛ Antigravity فقط مجری است.
- ✅ شروع فاز و بسته‌شدن Gate با تصمیم شاهددار Codex مدیر پروژه انجام می‌شود. اصلاحات و تصمیم‌های داخل چشم‌انداز مصوب بدون توقف برای تأیید روزمره مالک پیش می‌روند؛ مالک در پایان آزمون می‌کند و اصلاحات او بر تصمیم‌های قبلی مقدم است.
- ✅ هر فاز به Work Packageهای کوچک روی Branch/Worktree جدا تقسیم و با قالب Handoff مصوب تحویل می‌شود. هیچ Work Package با گزارش خود مجری پذیرفته‌شده محسوب نمی‌شود.
- ✅ `main`، Roadmap، فایل Excel، داده واقعی و محیط Production دارایی محافظت‌شده‌اند و قواعد O-46 تا O-51 بر تمام فازها مقدم‌اند.

### فاز 0 — تکمیل و تصویب Roadmap

وضعیت: تکمیل‌شده؛ G0 در 2026-08-30 بسته شد

- ثبت همه نیازمندی‌های قطعی و باز.
- پاسخ‌دادن به تصمیم‌های بحرانی بخش 21.
- تأیید ساختار گزارش نمونه و مسیرهای بات.
- ✅ پنج ADR معماری/Stack مصوب O-52 تا O-56 همراه گزینه‌ها، Trade-off، شرط بازنگری و برنامه شواهد Spike/Gate ثبت شدند.
- ✅ ممیزی همه وضعیت‌های `🟦` انجام و هر هشت مرحله Import با تأیید صریح کاربر قطعی شده‌اند؛ هیچ پیشنهاد `🟦` اثرگذار بر محصول برای G0 باقی نمانده است.
- ✅ Baseline محلی پاک روی شاخه `main` با `.gitignore`/`.gitattributes`، Roadmap، پنج ADR، README و Skill اجرایی ساخته شد؛ SQLite واقعی، Excel، PDF، داده/Secret، پوشه کار و Prototypeهای پیش از G0 خارج از Commit ماندند و اسکن الگوی Secret/شماره موبایل و `git diff --check` موفق بود.
- ✅ مخزن Private پیشین برای Baseline اولیه ساخته شد؛ پس از تصمیم جدید کاربر، آن مخزن فقط آرشیو تاریخچه پیشین است و منبع عملیاتی یا مقصد تغییرات تازه نیست.
- ✅ مخزن پاک و عمومی `mohsenbarari/accounting-bot-public` با تاریخچه مستقل منتشر و سپس به‌عنوان منبع عملیاتی و مرجع یگانه Roadmap/Skill انتخاب شد؛ `main` آن Require Pull Request و Enforce for administrators دارد و Force Push/Deletion ممنوع است.
- ✅ روی تنها Volume متصل سرور یک دایرکتوری مستقل بدون Format یا تغییر داده موجود ایجاد و با Bind Mount پایدار در `/srv/accounting-bot` ارائه شد؛ `fstab` با Backup نصب و با صفر خطای Parse/Filesystem راستی‌آزمایی شد.
- ✅ مخزن عمومی به‌صورت کنترل‌شده و بدون نیاز به Credential در `/srv/accounting-bot/workspace` Clone شد؛ `main`، `origin/main` و Commit عمومی ادغام‌شده `24da1133ceafb27e58e0cb9f2ff422071e043d0b` برابر و Worktree پاک بودند.
- ✅ محدودیت Branch Protection مخزن Private با انتقال مرجع عملیاتی به مخزن Public رفع شد؛ حفاظت فنی `main` عمومی فعال و O-67 بسته است و خرید GitHub Pro لازم نیست.
- ✅ نصب Global مستقل با Backup حفظ و به Symlink مدیریت‌شده نسخه Project Scope مخزن عملیاتی تبدیل شد؛ Validator قالب‌ها و تطبیق SHA-256 فایل اصلی Skill موفق بود و کشف واقعی `/skills` و Dry Run پیش از G0 نیز طبق شاهد ارائه‌شده توسط کاربر موفق شد.
- ✅ مالک محصول در 2026-08-30 اختیار مدیریت روزمره، Gateها و Mergeها را به Codex مدیر پروژه واگذار کرد؛ Codex پس از بررسی تکمیل شواهد فاز صفر، G0 را بست و شروع فاز 1 را مجاز اعلام کرد.

معیار خروج G0: Roadmap، Baseline، Skill و Dry Run شواهد کافی داشته و Codex مدیر پروژه اجازه شروع کدنویسی را ثبت کند. ✅ محقق شد.

### فاز 1 — تثبیت منبع و مدل داده

- ✅ WP-01 اسکلت Monorepo مصوب، Python 3.13، `uv`/Lockfile، مرز خودکار Domain، کنترل‌های Ruff/mypy/pytest و CI دو سکویی را ایجاد کرد؛ Codex آن را پس از Handoff، اصلاحات و Run نهایی PR #7 پذیرفت. این شاهد فقط زیرساخت G1 است و خود G1 را نمی‌بندد.
- ✅ WP-02 قرارداد نسخه‌دار و عمیقاً تغییرناپذیر ورودی خام چهار شیت، نگاشت دقیق `Z/P/P/D`، تفکیک Literal/Formula/Cached/Derived/Unlisted، نوع‌های بدون Float و ناوردایی‌های ساختاری را با Fixture صرفاً ساختگی تثبیت کرد؛ Codex آن را پس از بازبینی مستقل، یک دور اصلاح، 36 آزمون و CI دو سکویی PR #10 پذیرفت. این شاهد مرز منبع G1 است و خود G1 را نمی‌بندد.
- ✅ WP-03 قرارداد Canonical نسخه‌دار تاریخ/عدد، `source_hash` و `sheet_snapshot_hash` را طبق O-68/ADR-0006 پیاده کرد؛ Codex پس از بازبینی مستقل، یک دور اصلاح Grammar/TypeTag/Property Test/اسکن، 64 آزمون و CI دو سکویی PR #13 آن را پذیرفت. این شاهد تغییر قطعی منبع و شیت است و خود G1 را نمی‌بندد.
- ✅ WP-04 قرارداد `source-change-plan.v1` طبق O-69/ADR-0007 را با Snapshot کامل چهار شیت، رجیستری Active/Voided، برنامه قطعی Insert/Edit/`void`/بدون‌تغییر، Revision بعدی و منع انتقال UUID میان شیت‌ها پیاده کرد؛ Codex پس از بازبینی مستقل و سه دور اصلاح سازنده‌ها/کپی دفاعی/ترتیب Raw/آزمون جایگشت و شواهد کارایی، 89 آزمون و CI دو سکویی PR #16 آن را پذیرفت. این شاهد فقط مدل و Planner خالص است؛ Excel/SQLite/Import Commit واقعی را پوشش نمی‌دهد و G1 باز می‌ماند.
- ✅ WP-05 مرز Reader فقط‌خواندنی `xlsx-source-reader.v1` طبق O-70/ADR-0008 را با Streaming چهار شیت، Header/شناسه از رجیستری، حذف Formula/Cache و محدوده خروجی، Decode دقیق متن/Decimal/Null، فعالیت ردیف، Location مستقل از Hash و خروجی Snapshot معتبر WP-04 پیاده کرد؛ Codex پس از بازبینی مستقل چندمرحله‌ای، 200 آزمون و CI دو سکویی Run `33505486610` آن را در PR #19 پذیرفت. این شاهد فقط روی XLSXهای کاملاً ساختگی است؛ فایل واقعی، Stable Copy، Save/COM، نوشتن UUID، SQLite/Import Commit و G1 را پوشش نمی‌دهد.
- ✅ WP-06 مرز `xlsx-snapshot-acquisition.v1` طبق O-71/ADR-0009 را با دو مشاهده پایدار، ایجاد انحصاری Candidate، کپی/Hash جریانی و بازخوانی مستقل منبع/کپی، ارتقای بدون بازنویسی، Lease معتبر برای WP-05 و پاک‌سازی با کنترل مالکیت پیاده کرد؛ Codex پس از بازبینی مستقل چندمرحله‌ای و CI دو سکویی Run `33616797405` با 272 آزمون موفق Linux و 273 آزمون موفق Windows آن را در PR #22 پذیرفت و ادغام کرد. چهار سناریوی symlink در CI ویندوز اجرا شدند. شواهد فقط روی فایل‌های ساختگی‌اند؛ Watcher/Debounce واقعی، OneDrive/COM، UUID، فایل واقعی، SQLite/Outbox و معیار خروج G1 همچنان خارج از این بسته‌اند.
- ✅ WP-07 مرز `save-import-coordinator.v1` طبق O-72/ADR-0010 را با مسیر دقیق، Debounce یکنواخت دوثانیه‌ای، تجمیع اعلان‌ها، یک Token فعال و یک Follow-up، Retry منبع آماده‌نشده و Driver کامل WP-06/WP-05 پیاده کرد. Codex پس از بازبینی مستقل، رفع موارد خطا/مالکیت و اثبات oracle رقابت، آن را با CI دو سکویی Run `33632031865` و 312 قبولی Linux / 313 قبولی Windows در PR #25 پذیرفت و با تأیید صریح مالک ادغام کرد؛ چهار آزمون symlink ویندوز اجرا شدند. شواهد فقط ساختگی‌اند؛ Observer واقعی، OneDrive/COM، UUID، فایل واقعی، Commit/SQLite/Outbox و بازیابی Crash خارج از بسته‌اند و G1 باز می‌ماند.
- ✅ [WP-08](docs/work-packages/phase-01/WP-08-source-watch-runtime.md) مرز `source-watch-runtime.v1` طبق O-73/ADR-0011 را با Observer بومی غیر‌بازگشتی، اتصال اعلان به WP-07، حلقه ترتیبی WP-06/WP-05، اعلان اولیه و مدیریت شروع/توقف/شکست پیاده کرد. Codex پس از هشت دور بازبینی، رفع رقابت‌های آزمون و تطابق Rollback، آن را با CI دو سکویی Run `33666088469` در PR #28 پذیرفت و با تأیید صریح مالک ادغام کرد. شکست بعدی آزمون WR-08 در Windows Run `33666981958` با حذف خواب ثابت و انتظار شاهددار چرخه دوم در PR #30 رفع شد؛ بازبینی مستقل هر چهار فاز و CI نهایی اصلاحیه Run `33670197344` با 348 قبولی Linux / 349 قبولی Windows و اجرای چهار سناریوی symlink ویندوز موفق بودند. معیارهای WR-01 تا WR-18 فقط شواهد ساختگی‌اند؛ فایل واقعی، OneDrive/COM/UUID، نصب، Commit/SQLite/Outbox و بازیابی پایدار خارج از بسته‌اند و G1 باز می‌ماند. [ثبت پذیرش Codex](handoffs/phase-01/wp-08-source-watch-runtime/codex-review.md)
- ✅ [WP-09](docs/work-packages/phase-01/WP-09-source-requiredness.md) مرز `source-requiredness.v1` طبق O-74/ADR-0012 را با جدول مستقل 16 فیلد الزامی، تفکیک Null/Blank/Zero، گزارش قطعی و حفظ کامل Raw اثبات کرد. پس از اصلاحات و ادامه محدود آزمون‌ها توسط Codex با مجوز مالک، بازبین Codex جداگانه بدون مشارکت در نگارش اصلاحات، یافته قابل اقدام نداشت و 14 آزمون متمرکز را مستقلاً اجرا کرد. Codex مدیر پروژه شواهد SR-01 تا SR-14، حفظ 350 آزمون پایه و CI دو سکویی Run `33721864845` را پذیرفت؛ PR #32 با تأیید مالک و Commit `fb077a7` ادغام شد. CI پس از ادغام Run `33722627358` با 401 قبولی Linux / 402 قبولی Windows و اجرای چهار سناریوی symlink ویندوز موفق شد. شواهد فقط ساختگی‌اند؛ قبولی Requiredness جای قواعد مالی/RS، سال/آرشیو، Resolution یا تراکنش SQLite/Outbox نیست و G1 باز است. [ثبت پذیرش Codex](handoffs/phase-01/wp-09-source-requiredness/codex-review.md)
- ✅ [WP-10](docs/work-packages/phase-01/WP-10-source-fiscal-evidence.md) قرارداد خالص `source-fiscal-evidence.v1` را طبق O-75/ADR-0013 با متادیتای تمام ردیف‌های تراکنشی، None برای تاریخ خالی، شمارش جداگانه «لیست کسبه» و حفظ Snapshot/Raw/UUID/Hash پیاده کرد؛ Codex مدیر پروژه پس از بازبینی عامل غیرنویسنده، 56 آزمون اختصاصی، حفظ 403 آزمون پایه و CI دو سکویی Run `33728526844` آن را در PR #35 با Commit `0573775` پذیرفت و ادغام کرد. [ثبت پذیرش و CI پس از ادغام](handoffs/phase-01/wp-10-source-fiscal-evidence/codex-review.md) مرز شاهد ساختگی را نگه می‌دارد: گزارش سال عملیاتی انتخاب نمی‌کند و به‌تنهایی آرشیو، حذف یا Import را مجاز یا ممنوع نمی‌کند؛ اتصال منبع به سال و تفکیک رجیستری، انتقال مانده و تراکنش پایدار مؤجل‌اند و G1 باز است.
- ✅ قرارداد فنی O-76/[ADR-0014](docs/adr/ADR-0014-source-binding.md) تصویب و [WP-11](docs/work-packages/phase-01/WP-11-source-binding.md) برای `source-binding.v1` صادر شد: کلید منطقی منبع سالانه، رجیستری تغییرناپذیر فعال/آرشیو و انتخاب دقیق وضعیت قبلی منبع فعال، بدون حدس از نام/تاریخ یا تغییر APIهای پذیرفته‌شده. 🧪 شواهد SB-01 تا SB-14، بازبینی غیرنویسنده و CI بومی لازم‌اند؛ اجرای WP-11 هنوز پذیرفته نشده، اتصال به بایت فایل و رجیستری پایدار/هویت سراسری مؤجل‌اند و G1 باز است.
- تهیه نسخه پشتیبان کنترل‌شده از اکسل.
- اجرای کنترل‌شده UUIDv7، Source/Ledger/Sheet SHA-256 و سال مالی پس از دریافت اجازه جداگانه تغییر کپی اکسل.
- تعریف Whitelist ورودی خام چهار شیت و جداسازی Raw Immutable از Formula/Function/Query/Cached Value اکسل.
- حفظ تاریخ خام و ساخت تاریخ Canonical، اجزای شمسی و سال مالی با کتابخانه نسخه‌ثابت و آزمون مرزهای تقویم.
- آزمون پایش Save و OneDrive با Debounce، Stability Probe، Coalescing و Follow-up Import مصوب.
- ساخت مدل نسخه‌بندی، Relation و Opening Balance.
- تعیین تکلیف 103 نام خارج از «لیست کسبه» در Alias/Resolution بدون اتصال خودکار فازی و بدون ورود شماره موبایل واقعی.
- تکمیل Import اتمیک و Idempotent.

معیار خروج G1: واردات تکرارشونده بدون گم‌شدن یا تکثیر و با گزارش مغایرت.

### فاز 2 — موتور دفتر و محاسبات

- پیاده‌سازی قواعد پول C/D/RS/H/HA/HS.
- ساخت Resolver ایمن RS تاریخی و صف رفع ابهام بدون جفت‌سازی تصادفی.
- پیاده‌سازی خرید، فروش، ورود، خروج و Item Master مصوب همراه توقف محدود کالای ناشناخته.
- تثبیت ذخیره و نمایش پول به‌صورت عدد صحیح تومان بدون تبدیل.
- پیاده‌سازی Decimal برای وزن/عیار و اثبات نقطه ROUND_HALF_UP سه‌رقمی وزن 750 از روی اکسل.
- ساخت Canonical Ledger و Query/Projectionهای نسخه‌دار و قابل بازتولید از Raw با `calculation_version`.
- محاسبه مانده تاریخی پول و کالا.
- مدیریت مانده افتتاحیه 1405 و کنترل انتقال 1406.

معیار خروج G2: تطبیق کامل و مستند مانده دیتابیس با اکسل.

### فاز 3 — موتور گزارش و اصلاحیه

- ساخت مدل گزارش منطقی، شناسه و Snapshot.
- تولید گزارش روزانه متنی.
- تشخیص تغییر و تولید اصلاحیه تاریخی به‌علاوه آخرین مانده.
- تجمیع اصلاحیه پس از Commit کامل Backlog با کلید «شخص + تاریخ مالی» و حفظ Revisionهای میانی.
- ساخت Diff خوانای اصلاحیه و قواعد نمایش فیلدهای صفر/خالی.
- نهایی‌سازی ترتیب متن، بخش‌های اختیاری، Bullet/Separator و قطعه‌بندی «بخش X از N».
- تقسیم پیام‌های بلند و حفظ ترتیب.
- ساخت گزارش‌های PDF درخواستی با شیت «ریزحساب» به‌عنوان مرجع ساختار و ترتیب اطلاعات و افزودن محدود `report_id`، زمان تولید ایران و شماره صفحه.
- تقسیم PDF چندساله/پرحجم و صف پایدار یک Active/سه Pending با Dedup.

معیار خروج G3: نمونه‌های گزارش و PDF با قواعد مصوب، Golden Sample و بازبینی Codex مدیر پروژه تطبیق کامل داشته باشند؛ آزمون پذیرش مالک در فاز 7 انجام می‌شود.

### فاز 4 — همگام‌سازی، صف و زیرساخت قطعی

- عامل پایش Save اکسل.
- ورود آفلاین و بدون تأیید ویرایش‌ها و حذف‌های تکی یا گروهی به SQLite و Outbox محلی.
- محدودکردن پایش به فایل عملیاتی سال جاری و جلوگیری از Import فایل‌های آرشیوی.
- Staging، اعتبارسنجی و Commit اتمیک.
- Event Ledger/Outbox محلی بدون تکثیر Payload، Sequence و ACK پیوسته.
- ساخت ثبت اولیه یک‌بارمصرف دستگاه، تولید و نگهداری امن کلید Ed25519، ثبت کلید عمومی، لغو و چرخش کلید.
- API یک‌طرفه HTTPS با Challenge یک‌بارمصرف، امضای Canonical Batch، Idempotency و Retry با Backoff.
- تنظیم دامنه/زیردامنه، DNS، Reverse Proxy و گواهی عمومی ACME؛ mTLS و CA خصوصی وجود ندارند.
- Catch-up پس از قطعی.
- Scheduler چندصفی با اولویت مصوب، Cursor حساب/گیرنده و نمایش پیشرفت Backlog.
- آزمون Backlog سه‌ماهه، سقف دیسک/RAM، پرشدن دیسک و رویداد خراب.
- پیاده‌سازی Backup و Restore اولیه بدون الزام رمزگذاری برنامه‌ای و با محدودیت دسترسی سیستم‌عامل/فیزیکی.

معیار خروج G4: سناریوهای قطع و اتصال بدون از دست رفتن یا تکرار داده موفق باشند.

### فاز 5 — هویت و کنترل دسترسی با داده آزمایشی

- ساخت اشخاص هویتی با یک نقش مصرف‌کننده ثابت در هر ثبت‌نام فعال و بدون عضویت هم‌زمان صاحب حساب/حسابدار.
- جریان فعال‌سازی صاحب حساب و دعوت حسابدار.
- جریان اتمیک بازیابی Telegram User ID و خروج دومرحله‌ای حسابدار.
- رد کامل Telegram User ID جدید تا اجرای تعویض دستی و ممیزی‌شده O-22، حتی اگر شماره Contact یکسان باشد.
- Reply Keyboard نقش‌ها، Inline Calendar شمسی و ورودی تایپی/بازه با Back و Cancel.
- اعمال سقف سه حسابدار و یکتایی عضویت حسابدار.
- اعمال دعوت یک‌بارمصرف دقیقاً 24ساعته با لغو/صدور مجدد اتمیک.
- ساخت مدیر ارشد اصلی با اسکریپت Bootstrap، ایجاد مدیر ارشد پشتیبان جدا پیش از کاربران واقعی و فعال‌سازی مدیران با Contact منطبق، بدون OTP.
- پنل مدیر میانی و مدیر ارشد.
- ممیزی و تست نفوذ منطقی مجوزها.

معیار خروج G5: تمام آزمون‌های ماتریس دسترسی با هویت آزمایشی موفق باشند.

### فاز 6 — بات تلگرام و تصمیم مشترک

- منوهای نقش‌ها و درخواست PDF.
- ارسال دسته‌ای بدون پیش‌نمایش.
- تحویل چندگیرنده‌ای، طبقه‌بندی خطا و سیاست پنج تلاش/Retry مصوب بدون تکرار تحویل موفق.
- تأیید/رد سه‌ساعته و داوری 100 میلی‌ثانیه‌ای.
- ابطال اداری مستقل مدیر ارشد بدون Override رأی یا نتیجه تاریخی.
- اعلان نتیجه به همه اعضا و مدیر ارشد.

معیار خروج G6: آزمون انتهابه‌انتهای گزارش، تصمیم و ارسال ناموفق موفق باشد.

### فاز 7 — استقرار آزمایشی و آزمون پذیرش

- استقرار مطابق معماری نهایی.
- اجرای موازی با اکسل بدون ارسال به کاربران واقعی.
- مقایسه روزانه مانده‌ها و گزارش‌ها.
- آزمون بار، طول پیام، PDF، قطعی، PITR و Restore ماهانه با RPO/RTO مصوب.
- آزمون چرخه حذف لاگ‌های فنی/امنیتی و ناشناس‌سازی شناسه‌های هویتی طبق O-44.
- رفع همه مغایرت‌های بحرانی.
- اجرای آزمون پذیرش انتهابه‌انتهای مالک محصول؛ هر اصلاح اعلام‌شده توسط مالک به Work Package قابل‌ردیابی تبدیل و دوباره آزموده می‌شود.

معیار خروج G7: دوره آزمایشی و آزمون پذیرش مالک بدون اختلاف حسابداری یا اصلاح اعلام‌شده حل‌نشده پایان یابد؛ Codex مدیر پروژه شواهد را ثبت و Gate را می‌بندد.

### فاز 8 — ورود شماره‌های واقعی و فعال‌سازی نهایی

وضعیت: طبق دستور کاربر، آخرین مرحله پیش از راه‌اندازی

- تکمیل شماره موبایل صاحبان حساب.
- استانداردسازی و کنترل یکتایی شماره‌ها.
- اتصال قطعی شماره‌ها به party_idهای تأییدشده.
- ثبت شماره مدیران و حسابداران اولیه.
- ارسال دستورالعمل Start و فعال‌سازی مرحله‌ای.
- کنترل فهرست فعال، ناموفق و در انتظار فعال‌سازی.

معیار خروج G8: کاربران واقعی مجاز با شماره تأییدشده فعال و هیچ اتصال مبهم یا تکراری باقی نماند.

### فاز 9 — راه‌اندازی و عملیات

- اجرای اولین ارسال واقعی تحت نظارت.
- کنترل خلاصه موفق/ناموفق و Retry.
- پایش Sync، صف، سرور، Backup و خطاها.
- ثبت بازخورد و اصلاح تجربه مکالمه بدون تغییر بی‌مجوز قواعد مالی.

## 21. دفتر موارد باز

وضعیت هر مورد در همان مدخل ثبت می‌شود. پیشنهاد بدون تأیید صریح همچنان باز می‌ماند. در نسخه 0.21 از 66 مورد، 65 مورد ✅ بسته و فقط O-25 در وضعیت 🧪 نیازمند شاهد فنی است؛ هیچ مورد 🟨 باقی نمانده است.

### O-01 — محل نهایی اجرا

- **وضعیت:** ✅ بسته؛ کاربر معماری ترکیبی را تأیید کرد.
- **موضوع:** اجرای کامل روی سیستم ایران، اجرای کامل روی سرور یا معماری ترکیبی.
- **تصمیم:** عامل خواندن اکسل، SQLite آینه و Outbox روی سیستم محلی می‌مانند و بات، API، دیتابیس مرکزی و موتور ارسال روی سرور هتزنر فنلاند اجرا می‌شوند.
- **دلیل:** ذخیره اکسل در قطعی اینترنت از بین نمی‌رود، بات به VPN سیستم حسابداری وابسته نیست و پس از اتصال، داده‌ها قابل Sync هستند. قطعی سراسری همچنان دسترسی کاربران ایرانی به تلگرام را مختل می‌کند، اما باعث ازدست‌رفتن داده نمی‌شود.
- **تاریخ تصمیم:** 2026-08-28.

### O-02 — دیتابیس مرکزی

- **وضعیت:** ✅ بسته؛ PostgreSQL برای دیتابیس مرکزی تأیید شد.
- **موضوع:** استفاده از SQLite یا PostgreSQL برای دیتابیس عملیاتی سرور.
- **تصمیم:** SQLite برای آینه، Staging و صف محلی؛ PostgreSQL برای دیتابیس مرکزی.
- **دلیل:** SQLite برای یک Writer محلی ساده و مطمئن است، اما PostgreSQL برای دسترسی هم‌زمان بات، Worker گزارش، Worker ارسال، قفل تصمیم 100 میلی‌ثانیه‌ای، Backup و محدودیت‌های یکتا مناسب‌تر است.
- **تاریخ تصمیم:** 2026-08-28.

### O-03 — شناسه پایدار رکوردهای اکسل

- **وضعیت:** ✅ بسته؛ کاربر طراحی شناسه، Hash و رفتار Edit/Delete/Insert را تأیید کرد.
- **موضوع:** روش تشخیص یک رکورد پس از مرتب‌سازی، جابه‌جایی یا ویرایش.
- **تصمیم:** UUIDv7 ثابت در ستون‌های خالی `Z/P/P/D` به‌ترتیب چهار شیت، بدون Insert و بدون تغییر فرمول‌های موجود؛ سه ستون `record_id` و یک ستون `party_id` مخفی می‌شوند.
- **Hash:** SHA-256 نسخه‌دار برای `source_hash`، `ledger_hash` و `sheet_snapshot_hash` روی Payload استاندارد Canonical و خارج از Excel. Hash شیت بر جفت‌های مرتب‌شده UUID و Hash ساخته می‌شود تا Sort بی‌اثر باشد.
- **جزئیات:** عامل Sync برای ردیف جدید در رویداد `WorkbookBeforeSave` UUIDv7 می‌سازد تا شناسه همراه همان Save در فایل بماند؛ دست‌کاری خود UUID در ردیف باقی‌مانده، خالی‌کردن آن یا تکرار مبهم شناسه می‌تواند Import را متوقف کند، اما حذف کامل ردیف و ناپدیدشدن UUID آن یک Soft Delete معتبر است. محدوده Sort/Filter شناسه را پوشش می‌دهد. فایل `.xlsx` باقی می‌ماند و هیچ VBA یا فرمول Hash داخل آن قرار نمی‌گیرد.
- **اثر عملی:** ویرایش = Revision جدید همان شناسه؛ حذف = Soft Delete شناسه مفقود؛ Insert = شناسه کاملاً جدید. تغییر تاریخ، هم تاریخ قدیم و هم تاریخ جدید را برای اصلاحیه متأثر می‌کند و Sort خالص هیچ رویداد مالی نمی‌سازد.
- **محدودیت اختیار:** این تصمیم فقط طراحی را تصویب می‌کند؛ هرگونه آزمایش یا اعمال روی فایل اصلی یا کپی آن نیازمند اجازه صریح و جداگانه کاربر است.
- **دلیل:** تنها راه مطمئن برای تشخیص ویرایش همان رکورد است؛ شماره ردیف با Sort و Insert تغییر می‌کند و هش محتوا هنگام ویرایش عوض می‌شود.
- **تاریخ تصمیم:** 2026-08-28.

### O-04 — تفاوت H، HA و HS

- **وضعیت:** ✅ بسته؛ کاربر یکسان‌بودن محاسبه و تفاوت صرفاً برای فیلتر را تأیید کرد.
- **موضوع:** اثر محاسباتی و نمایشی H، HA و HS.
- **تصمیم:** کد خام هر ردیف بدون تغییر نگهداری می‌شود، اما اثر حسابداری و متن گزارش هر سه یکسان است: بدهکار = طلب ما و بستانکار = طلب شما. نام متفاوت فقط برای فیلتر است.
- **دلیل:** اطلاعات خام از بین نمی‌رود و اگر بعداً تفاوتی مشخص شد، بدون مهاجرت دشوار قابل تفکیک خواهد بود.
- **تاریخ تصمیم:** 2026-08-28.

### O-05 — تشخیص ردیف انتقال مانده ابتدای سال

- **وضعیت:** ✅ بسته؛ قاعده ترکیبی با تضمین تاریخ 01/01 و کنترل مانده سال قبل تصویب شد؛ آزمون 1406 همچنان معیار پذیرش است.
- **موضوع:** تشخیص مطمئن انتقال مانده برای پول و کالا، به‌ویژه از سال 1406 به بعد.
- **تصمیم:** تاریخ 01/01 شرط لازم است و همراه کد/جهت/شرح/مقدار و نوع کالا امضای اولیه را می‌سازد. انتقال 1405 یک‌بار Opening Balance است؛ انتقال 1406 و بعد فقط پس از تطبیق با مانده بسته‌شده دیتابیس در Reconciliation شناسایی و از دفتر عملیاتی حذف می‌شود.
- **جزئیات:** `entry_origin` در دیتابیس مشتق می‌شود و ستون جدیدی برای Opening Balance در اکسل لازم نیست. هر عدم تطبیق متوقف و به مدیر ارشد اعلام می‌شود.
- **دلیل:** تاریخ به‌تنهایی ممکن است شامل تراکنش واقعی روز اول سال باشد؛ تطبیق مانده قبلی مانع دوباره‌شماری یا طبقه‌بندی اشتباه می‌شود.
- **تاریخ تصمیم:** 2026-08-28.

### O-06 — حذف ردیف اکسل

- **وضعیت:** ✅ بسته؛ کاربر هرگونه کنترل، تأیید یا محدودیت برای حذف و ویرایش را رد کرد.
- **موضوع:** حذف یک ردیف فوراً معتبر باشد یا به تأیید مدیر نیاز داشته باشد.
- **تصمیم:** حذف یا ویرایش هر تعداد ردیف در فایل عملیاتی سال جاری آزاد است. Save کامل و معتبر اکسل دستور نهایی محسوب می‌شود و هیچ تأیید در بات، پنجره محلی، آستانه حجمی یا استثنای ویژه برای «لیست کسبه» وجود ندارد.
- **اثر داده:** ویرایش با UUID ثابت Revision جدید همان رکورد را می‌سازد؛ حذف، رکورد را Soft Delete/`void` می‌کند و نسخه قبلی را برای ممیزی نگه می‌دارد. تغییرات تاریخی طبق قواعد مصوب اصلاحیه و آخرین مانده را ایجاد می‌کنند.
- **کنترل فنی باقیمانده:** فقط Snapshot ناقص/خراب، ساختار نامعتبر یا شناسه مبهم می‌تواند Import را متوقف کند؛ تعداد حذف یا ویرایش به‌تنهایی هرگز دلیل توقف نیست. ثبت Audit یا اعلان پس از وقوع نیز مانع اعمال تغییر نمی‌شود.
- **سابقه تصمیم:** پیشنهادهای قبلی تأیید از طریق بات، تأیید محلی و آستانه حذف پرخطر توسط کاربر رد شدند.
- **دلیل:** اکسل منبع عملیاتی و کاربر آن مرجع مجاز ایجاد، ویرایش و حذف است؛ سامانه باید تغییر معتبر منبع را بدون مداخله اجرایی منعکس کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-07 — ویرایش فایل سال آرشیوی

- **وضعیت:** ✅ بسته؛ کاربر تضمین کرد داده سال‌های گذشته تغییر نمی‌کند.
- **موضوع:** آیا عامل Sync باید همه فایل‌های آرشیوی را دائماً پایش کند؟
- **تصمیم:** فایل و داده هر سال پس از ورود نهایی و آرشیو تغییرناپذیر هستند. عامل Sync فقط فایل عملیاتی سال جاری را پایش می‌کند و هیچ مسیر عادی برای Import اصلاحیه از فایل آرشیوی وجود ندارد.
- **جزئیات:** Hash نسخه نهایی آرشیو برای کنترل تمامیت و Backup نگهداری می‌شود؛ تغییر احتمالی فایل آرشیوی هشدار تمامیت ایجاد می‌کند ولی دفتر مرکزی را تغییر نمی‌دهد.
- **دلیل:** طبق تضمین کاربر، اصلاحات فقط در فایل سال جاری رخ می‌دهند؛ حذف پایش آرشیو خطر ورود ناخواسته نسخه قدیمی یا کپی موقت را از بین می‌برد.
- **تاریخ تصمیم:** 2026-08-28.

### O-08 — صاحب حسابی که حسابدار شخص دیگری است

- **وضعیت:** ✅ بسته؛ نقش هم‌زمان برای کاربران مصرف‌کننده ممنوع شد.
- **موضوع:** امکان داشتن هم‌زمان نقش صاحب حساب و حسابدار یک حساب دیگر.
- **تصمیم:** هر کاربر مصرف‌کننده فقط نخستین نقش فعال خود را دارد: صاحب حساب یا حسابدار. افزودن نقش دوم، عضویت هم‌زمان یا تغییر مستقیم نقش مجاز نیست.
- **جزئیات:** برای استفاده با نقش جدید، دسترسی/عضویت قبلی ابتدا به‌طور معتبر حذف و سپس ثبت‌نام یا دعوت تازه انجام می‌شود. شناسه هویتی، سابقه نقش قبلی، دسترسی‌ها و رویدادهای ممیزی پاک نمی‌شوند.
- **دلیل:** مدل تک‌نقشی مسیرهای بات و کنترل دسترسی را روشن نگه می‌دارد و با قاعده «یک حسابدار برای بیش از یک صاحب حساب ممنوع» سازگار است.
- **تاریخ تصمیم:** 2026-08-28.

### O-09 — احراز هویت مدیران

- **وضعیت:** ✅ بسته؛ احراز مدیران با شماره ازپیش‌ثبت‌شده و Contact تلگرام، بدون OTP، تعیین شد.
- **موضوع:** امنیت بیشتر از احراز شماره و Telegram User ID برای مدیران پرقدرت.
- **تصمیم:** اولین مدیر ارشد با اسکریپت Bootstrap خارج از بات و شماره موبایل مشخص ساخته می‌شود. مدیران بعدی را مدیر ارشد با نقش و شماره موبایل اضافه می‌کند. فعال‌سازی هر مدیر فقط با اشتراک Contact همان شخص و تطبیق شماره انجام می‌شود؛ PIN، OTP و TOTP استفاده نمی‌شود.
- **کنترل:** بات فقط Contact متعلق به فرستنده را می‌پذیرد، `contact.user_id` را با فرستنده تطبیق می‌دهد، شماره را نرمال و با رکورد ازپیش‌ثبت‌شده مقایسه می‌کند و سپس Telegram User ID را یکتا متصل می‌سازد. تلاش‌های موفق و ناموفق ممیزی می‌شوند.
- **دلیل:** این مسیر طبق تصمیم کاربر ساده و کافی است و همچنان مانع ثبت‌نام مدیری می‌شود که شماره‌اش قبلاً توسط اسکریپت یا مدیر ارشد مجاز نشده باشد.
- **تاریخ تصمیم:** 2026-08-28.

### O-10 — مرز روز مالی و زمان ارسال

- **وضعیت:** ✅ بسته؛ روز هدف و ملاک انتساب رکورد قطعی شد.
- **موضوع:** تعریف روز هدف و زمان اجرای گزارش روزانه.
- **تصمیم:** گزارش رسمی روزانه همیشه برای تاریخ شمسی روز قبل ساخته می‌شود و ملاک حضور رکورد فقط تاریخ ثبت‌شده در اکسل است؛ زمان Save، Import یا ساعت ورود داده نقشی در روز مالی ندارد. گزارش رسمی روز جاری ارسال نمی‌شود.
- **جزئیات:** داده روز قبل می‌تواند صبح روز جاری تکمیل شود. مدیر ارشد پس از تکمیل و Sync موفق فرمان ارسال را اجرا می‌کند و ارسال زمان‌بندی‌شده خودکار در نسخه اول وجود ندارد. رکورد روز قبل که پس از ارسال ثبت یا اصلاح شود، اصلاحیه همان تاریخ و آخرین مانده را ایجاد می‌کند.
- **کنترل فنی:** هر فرمان از یک Snapshot سازگار از آخرین Sync موفق استفاده می‌کند؛ این Snapshot فقط مرز فنی ثبات داده است و Cut-off مالی جدیدی ایجاد نمی‌کند.
- **دلیل:** روز جاری هنوز باز است و امکان اضافه‌شدن تراکنش دارد؛ تاریخ صریح اکسل تنها معیار پایدار و قابل ممیزی برای گزارش روزانه است.
- **تاریخ تصمیم:** 2026-08-28.

### O-11 — شروع مهلت سه‌ساعته در ارسال ناقص

- **وضعیت:** ✅ بسته؛ قاعده اولین تحویل کامل و deadline مشترک تأیید شد.
- **موضوع:** اگر برخی گیرندگان پیام را دریافت کنند و برخی ناموفق باشند، سه ساعت از چه زمانی آغاز شود؟
- **تصمیم:** وقتی اولین گیرنده تمام قطعات گزارش و دکمه‌ها را دریافت کند، یک `published_at` مشترک ثبت و سه ساعت از همان لحظه آغاز می‌شود. اگر هیچ‌کس گزارش کامل را نگرفته باشد، مهلت شروع نمی‌شود.
- **جزئیات:** گیرنده Retryشده پیش از انقضا فقط زمان باقی‌مانده را دارد؛ اگر نتیجه پیش‌تر با رأی یا انقضا نهایی شده باشد، گزارش بدون دکمه و همراه نتیجه قطعی ارسال می‌شود.
- **دلیل:** گزارش مشترک باید فقط یک موعد داشته باشد و یک گیرنده ناموفق نباید تصمیم همه را نامحدود متوقف کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-12 — دکمه تصمیم برای PDF درخواستی

- **وضعیت:** ✅ بسته؛ PDF درخواستی از چرخه تصمیم رسمی خارج شد.
- **موضوع:** آیا PDFی که کاربر خودش درخواست کرده نیز تأیید/رد شود؟
- **تصمیم:** خیر. PDF فقط برای درخواست‌کننده تولید می‌شود و دکمه، مهلت سه‌ساعته یا نتیجه مشترک ندارد؛ فقط گزارش رسمی روزانه و اصلاحیه وارد چرخه تأیید/رد می‌شوند.
- **دلیل:** PDF درخواستی پاسخ لحظه‌ای به درخواست کاربر است و واردکردن آن به چرخه تأیید، تعداد تصمیم‌ها را زیاد و معنای تأیید رسمی را مبهم می‌کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-13 — علت اختیاری رد

- **وضعیت:** ✅ بسته؛ جریان ثبت رأی و علت اختیاری تأیید شد.
- **موضوع:** زمان و شیوه دریافت علت مغایرت.
- **تصمیم:** کلیک «رد» بلافاصله رأی را ثبت می‌کند و سپس دو گزینه «ارسال علت» و «بدون توضیح» نمایش می‌دهد. هر ردکننده برای ارسال یک علت 10 دقیقه فرصت دارد؛ عدم ارسال علت، رأی را باطل نمی‌کند.
- **جزئیات:** علت هر فرد جداگانه و با هویت او برای مدیر ارشد/Audit حفظ می‌شود و خودکار برای همه اعضا بازنشر نمی‌شود. اگر تأیید در پنجره 100 میلی‌ثانیه‌ای برنده شود، علت مخالفت باقی ولی نتیجه مشترک تأیید است.
- **دلیل:** تصمیم مالی نباید به تکمیل متن اختیاری وابسته باشد، اما نظر مخالف نیز نباید گم شود.
- **تاریخ تصمیم:** 2026-08-28.

### O-14 — تجمیع ردیف‌های مشابه در متن

- **وضعیت:** ✅ بسته؛ الگوی نسخه اول براساس پروژه قدیمی و تفکیک نوع تراکنش تعیین شد.
- **موضوع:** نمایش جداگانه یا جمع‌کردن چند تراکنش مشابه.
- **تصمیم:** نسخه اول مشابه گزارش پروژه قدیمی است. خرید/فروش و ورود/خروج هر تراکنش را در یک سطر مستقل نشان می‌دهند. دریافت/پرداخت برای هر صاحب حساب درج‌شده در توضیحات، در هر روز و جهت مالی، یک سطر دارد و مبالغ هم‌گروه جمع می‌شوند.
- **شاهد پروژه قدیمی:** شیت `sum money query name` مبلغ را با `SUMIFS` و کلید تاریخ، بدهکار/بستانکار و صاحب حساب جمع و از آن یک خط پیام می‌ساخت.
- **کنترل معنایی:** کلید تجمیع جدید باید جهت و معنای C/D/RS/H/HA/HS را حفظ کند؛ به‌ویژه رابطه‌های متفاوت RS فقط به دلیل یکسان‌بودن صاحب حساب ادغام نمی‌شوند. شرح خالی H/HA/HS طبق تصمیم قبلی بدون توضیح و با مبلغ طلب/بدهی گزارش می‌شود.
- **دلیل:** این رفتار با گزارش آشنای فعلی سازگار است و در عین کاهش خطوط تکراری دریافت/پرداخت، جزئیات هر معامله و حرکت کالا را نگه می‌دارد.
- **تاریخ تصمیم:** 2026-08-28.

### O-15 — قالب PDF

- **وضعیت:** ✅ بسته؛ مرجع ساختاری و عناصر تکمیلی PDF تأیید شدند.
- **موضوع:** شکل نهایی گزارش‌های درخواستی.
- **مرجع قطعی:** ساختار پایه از شیت «ریزحساب» فایل `new.xlsx` گرفته می‌شود. بررسی خواندنی نشان داد محدوده نمایشی A:J شامل ردیف، تاریخ، شرح، کالا، مقدار، فی، مبلغ، مانده انتهای روز، بدهکار/بستانکار و توضیحات است؛ بازه و نام شخص در بالا، سطر عنوان در ردیف 4 و ردیف‌های `:مانده` پس از هر روز قرار دارند. صفحه A4 عمودی، راست‌به‌چپ و افقی‌وسط‌چین است و ردیف‌های 1 تا 4 به‌عنوان عنوان چاپ تکرار می‌شوند.
- **تصمیم:** هسته جدول، ترتیب اطلاعات و منطق نمایش شیت حفظ می‌شود، ولی بازتولید پیکسلی رنگ، فونت و فاصله‌ها الزام نیست. فقط `report_id`، زمان تولید به وقت ایران و شماره صفحه به سربرگ/پاورقی افزوده می‌شوند. شماره موبایل، Telegram User ID و واترمارک نمایش داده نمی‌شوند و PDF در نسخه اول رمز ندارد.
- **چرخه فایل:** نام فنی فایل بر پایه `report_id` و بدون داده هویتی حساس ساخته می‌شود. فایل موقت پس از تحویل موفق حذف می‌شود، ولی داده و Snapshot منطقی گزارش در دیتابیس می‌ماند تا گزارش دوباره قابل تولید باشد.
- **دلیل:** استفاده از فرم آشنای «ریزحساب» نیاز به طراحی مجدد گزارش را کم می‌کند؛ عناصر فنی فقط برای پیگیری و ممیزی لازم‌اند و نباید جدول حسابداری را عوض کنند.
- **تاریخ تصمیم:** 2026-08-28.

### O-16 — Item Master و واحد کالاها

- **وضعیت:** ✅ بسته؛ ساختار مرجع کالا و رفتار کالای ناشناخته تأیید شد.
- **موضوع:** حذف فهرست سخت‌کدشده کالاها و تعریف قواعد هر کالا.
- **تصمیم:** جدول قابل مدیریت Item Master با `item_id` پایدار، نام معیار، نام‌های جایگزین، واحد پایه، تعداد اعشار، نوع وزنی/تعدادی، نیاز به عیار، روش وزن 750 و وضعیت فعال ساخته می‌شود.
- **رفتار کالای ناشناخته:** نام و ردیف خام وارد Staging/دیتابیس و برای تعیین تکلیف علامت‌گذاری می‌شود، اما مانده و گزارش کالایی وابسته تا تعیین واحد و قاعده صحیح منتشر نمی‌شود. دامنه توقف فقط همان کالا و حساب‌های متأثر است؛ داده پولی و حساب‌های نامرتبط ادامه پیدا می‌کنند.
- **دلیل:** اضافه‌شدن کالای جدید نباید نیازمند تغییر کد یا ایجاد مانده با واحد اشتباه باشد.
- **تاریخ تصمیم:** 2026-08-28.

### O-17 — نام‌های خارج از لیست کسبه

- **وضعیت:** ✅ بسته؛ مدل چهارگروهی و ممنوعیت اتصال خودکار تأیید شد.
- **موضوع:** 103 نام متمایز تراکنشی که در لیست کسبه نیستند.
- **تصمیم:** نام خام همیشه حفظ و در Alias/Resolution به یکی از چهار گروه شخص موجود، شخص جدید، حساب/طرف داخلی غیرکاربر یا غلط تایپی/نام جایگزین نگاشت می‌شود.
- **کنترل:** تطبیق فازی فقط پیشنهاد می‌دهد و هیچ اتصال مالی یا هویتی را خودکار قطعی نمی‌کند. ردیف مالی حل‌نشده حذف نمی‌شود، ولی هویت مبهم امکان فعال‌سازی تلگرام ندارد.
- **دلیل:** شباهت نام برای اتصال اطلاعات مالی کافی نیست و تطبیق اشتباه خطر افشای حساب دارد.
- **تاریخ تصمیم:** 2026-08-28.

### O-18 — واحد پول ریال یا تومان

- **وضعیت:** ✅ بسته؛ واحد منبع و نمایش «تومان» تأیید شد.
- **موضوع:** تفسیر عدد موجود در اکسل و عنوان نمایش گزارش.
- **تصمیم:** اعداد پولی فایل تومان هستند. مقدار منبع دقیقاً بدون ضرب یا تقسیم و به‌صورت عدد صحیح ذخیره می‌شود، Metadata واحد `TOMAN` خواهد بود و گزارش نیز «تومان» نمایش می‌دهد؛ هیچ تبدیل ضمنی ریال/تومان مجاز نیست.
- **دلیل:** تبدیل ضمنی بین ریال و تومان یکی از پرخطرترین خطاهای حسابداری است.
- **تاریخ تصمیم:** 2026-08-28.

### O-19 — سیاست Retry ارسال

- **وضعیت:** ✅ بسته؛ تعداد، زمان‌بندی و ارجاع خطا تأیید شد.
- **موضوع:** تعداد و فاصله تلاش‌های خودکار و زمان ارجاع به مدیر.
- **تصمیم:** حداکثر پنج تلاش خودکار شامل تلاش اولیه و Retry تقریباً در 30 ثانیه، 2 دقیقه، 10 دقیقه و 30 دقیقه پس از تلاش اولیه انجام می‌شود؛ در Rate Limit، `retry_after` تلگرام مقدم است.
- **جزئیات:** خطای موقت Retry می‌شود؛ خطای قطعی مانند Block، Chat Not Found یا حساب غیرفعال مستقیماً نیازمند اقدام مدیر است. پس از سقف تلاش، «ارسال مجدد همه ناموفق‌ها» دور جدید را فقط برای گیرندگان/قطعات ناقص آغاز و هیچ تحویل موفقی را تکرار نمی‌کند.
- **دلیل:** خطاهای موقت خودکار رفع می‌شوند ولی خطای دائمی مانند Blockکردن بات بی‌نهایت تکرار نمی‌شود.
- **تاریخ تصمیم:** 2026-08-28.

### O-20 — نگهداری Backup و لاگ

- **وضعیت:** ✅ بسته؛ تناوب، محل دوم، نگهداری و اهداف بازیابی تأیید و الزام رمزگذاری بعداً با O-43 برداشته شد.
- **موضوع:** مدت نگهداری و اهداف بازیابی.
- **تصمیم:** دیتابیس و Outbox محلی هر ساعت روی محل فیزیکی دوم Backup می‌شوند. سرور مرکزی PITR با RPO حداکثر 15 دقیقه و نسخه‌ای در فضای ذخیره‌سازی جدا از خود سرور دارد. نگهداری برابر 30 روزانه، 12 هفتگی و 12 ماهانه است. طبق تصمیم جدید O-43 رمزگذاری در سطح برنامه برای این نسخه‌ها الزام نیست؛ دسترسی فیزیکی و سیستم‌عامل محدود می‌ماند.
- **بازیابی و نگهداری:** Restore ماهانه با RTO حداکثر 4 ساعت آزموده می‌شود. داده مالی، Snapshot گزارش و ممیزی اصلی حذف خودکار ندارند؛ PDF موقت پس از تحویل موفق حذف می‌شود.
- **دلیل:** وجود Backup بدون مدت نگهداری و آزمون Restore تضمین بازیابی ایجاد نمی‌کند.
- **تاریخ تصمیم:** 2026-08-28؛ **اصلاح تصمیم رمزگذاری:** 2026-08-28 در O-43.

### O-21 — چند اصلاحیه هم‌زمان یا پس از قطعی

- **وضعیت:** ✅ بسته؛ مرز تجمیع و ترتیب ارسال اصلاحیه‌ها تأیید شد.
- **موضوع:** جلوگیری از ارسال چند اصلاحیه غیرضروری برای تغییرات پی‌درپی یک روز.
- **تصمیم:** پیش از تولید اصلاحیه، تمام Backlog تا نقطه سازگار Sync و Commit می‌شود. تغییرات ارسال‌نشده با کلید «شخص + تاریخ مالی» از وضعیت نهایی یک اصلاحیه می‌سازند و همه Revisionهای میانی در ممیزی می‌مانند.
- **جزئیات:** فقط تاریخ دارای گزارش رسمی قبلی اصلاحیه می‌شود؛ تاریخ گزارش‌نشده از مسیر روزانه/Catch-up می‌رود. تاریخ‌های مختلف جدا و به ترتیب ارسال می‌شوند و سپس فقط آخرین روز مشمول Catch-up فرستاده می‌شود. تغییر پس از انتشار اصلاحیه، اصلاحیه مستقل جدید می‌سازد.
- **دلیل:** کاربر پیام تکراری نمی‌گیرد و تاریخچه تغییرات همچنان کامل می‌ماند.
- **تاریخ تصمیم:** 2026-08-28.

### O-22 — بازیابی یا تعویض Telegram User ID

- **وضعیت:** ✅ بسته؛ اتصال یکتا و بازیابی تحت کنترل مدیر ارشد تأیید شد.
- **موضوع:** تغییر حساب تلگرام، سرقت حساب یا تعویض شماره.
- **تصمیم:** هر هویت فقط یک Telegram User ID فعال دارد. حساب جدید باید Contact متعلق به خودش با شماره ثبت‌شده بفرستد و مدیر ارشد تعویض را تأیید کند؛ فعال‌شدن جدید و لغو قبلی اتمیک و Callbackهای قبلی فوراً باطل می‌شوند.
- **جزئیات:** Telegram User ID جدید، حتی با همان شماره، پیش از پایان کامل همین فرایند هیچ دسترسی ندارد. به حساب قبلی در صورت امکان و به صاحب حساب/مدیر ارشد اعلان امنیتی داده می‌شود. تغییر شماره موبایل فقط با احراز خارج از بات، تأیید مدیر ارشد و دلیل ثبت‌شده انجام و همه اتصال‌های قبلی در Audit حفظ می‌شوند.
- **دلیل:** چند اتصال فعال کنترل لغو دسترسی و تشخیص گیرنده معتبر را پیچیده می‌کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-23 — لغو دسترسی توسط خود حسابدار

- **وضعیت:** ✅ بسته؛ خروج اختیاری و محدود به عضویت خود حسابدار تأیید شد.
- **موضوع:** آیا حسابدار بتواند عضویت خودش را پایان دهد؟
- **تصمیم:** بله؛ جریان دومرحله‌ای «خروج از این حساب» فقط عضویت خود حسابدار را فوراً لغو، دسترسی آینده و Callbackهای او را باطل و صاحب حساب و مدیر ارشد را مطلع می‌کند. رأی‌ها، تحویل‌ها و Audit قبلی باقی می‌مانند و بازگشت دعوت تازه می‌خواهد؛ حذف پیام‌های قدیمی دستگاه تضمین نمی‌شود.
- **دلیل:** هر فرد باید بتواند دریافت اطلاعات مالی شخص دیگر را متوقف کند، بدون اینکه اختیار مدیریت سایر حسابداران را داشته باشد.
- **تاریخ تصمیم:** 2026-08-28.

### O-24 — Override نتیجه توسط مدیر ارشد

- **وضعیت:** ✅ بسته؛ Override مستقیم ممنوع و اقدام مدیریتی مستقل تأیید شد.
- **موضوع:** امکان تغییر مستقیم نتیجه تأیید یا رد مشترک توسط مدیر.
- **تصمیم:** Override مستقیم وجود ندارد. مدیر ارشد می‌تواند با دلیل اجباری گزارش را در Administrative Resolution مستقل باطل کند یا پس از اصلاح داده، اصلاحیه تازه بسازد؛ نام مدیر، زمان، دلیل و report_id ثبت و نتیجه به همه اعضای حساب اعلام می‌شود.
- **کنترل:** رأی‌ها و نتیجه تاریخی تغییر نمی‌کنند و اقدام مدیریتی یک لایه مستقل و قابل ممیزی است.
- **دلیل:** تغییر رأی ثبت‌شده اعتبار ممیزی را از بین می‌برد، درحالی‌که ابطال مستقل امکان اصلاح عملی را حفظ می‌کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-25 — گردکردن پول و کالا

- **وضعیت:** 🧪 قاعده طراحی تأیید شده؛ ارتقا به ✅ منوط به اثبات نقطه دقیق Round از روی اکسل است.
- **موضوع:** جلوگیری از اختلاف ناشی از Float و گردکردن نامشخص.
- **قاعده تأییدشده:** پول عدد صحیح تومان و بدون Float است. وزن، عیار و مقدار کالا با Decimal و دقت کامل منبع ذخیره می‌شوند، مقدار خام جایگزین نمی‌شود و Round تکراری مانده ممنوع است. وزن 750 با Decimal و `ROUND_HALF_UP` Excel-compatible تا سه رقم اعشار تولید می‌شود.
- **شاهد لازم:** با بررسی خواندنی فرمول‌های واقعی و نمونه‌های `.xxx4`، `.xxx5` و `.xxx6` مثبت/منفی باید تعیین شود Round در سطح هر تراکنش یا پس از تجمیع اعمال می‌شود. خروجی سطر و مانده نهایی باید دقیقاً با اکسل برابر باشد؛ سپس وضعیت به ✅ ارتقا می‌یابد.
- **دلیل:** گردکردن در چند مرحله یا استفاده از Float می‌تواند در مانده تجمعی اختلاف بسازد.
- **تاریخ تأیید قاعده:** 2026-08-28؛ **زمان تکمیل شاهد:** پیش از G2.

### O-26 — فرمول اکسل بدون Cached Value معتبر

- **وضعیت:** ✅ بسته؛ مرز Sync به داده خام چهار شیت ورودی محدود شد.
- **موضوع:** سلول فرمولی خطادار، محاسبه‌نشده یا دارای مقدار قدیمی.
- **تصمیم:** فقط ستون‌های ورودی Whitelistشده چهار شیت، همراه UUID، Revision، مقدار خام و Metadata منبع، وارد Raw Immutable می‌شوند. فرمول‌ها، توابع، Cached Valueها، شیت‌های کمکی و خروجی Query/گزارش اکسل وارد دفتر مالی نمی‌شوند.
- **معماری:** Raw Immutable → Canonical Ledger/Calculation Engine → Query، View یا Materialized Projection نسخه‌دار برای مانده و گزارش. خروجی ذخیره‌شده فقط Cache قابل بازتولید با `calculation_version` است و منبع حقیقت جداگانه نیست.
- **رفتار خطا:** Formula/Function/Cached Value خطادار یا قدیمی با Raw معتبر Import را متوقف نمی‌کند و تغییر صرفاً فرمولی Event، Revision یا اصلاحیه نمی‌سازد. Raw ناقص/نامعتبر، UUID مبهم یا RS جدیدِ دارای ناسازگاری ساختاری کل Snapshot را پیش از Commit رد می‌کند؛ ابهام نام/کالا و RS تاریخی چندکاندیدایی Raw و سایر Importها را حفظ و فقط انتشار دامنه متأثر را متوقف می‌کنند.
- **دلیل:** SQLite/PostgreSQL برای Query، Index، فیلتر، تجمیع، کنترل نسخه و ممیزی از فرمول‌های گسترده اکسل مناسب‌ترند و این جداسازی محاسبات را متمرکز، سریع، آزمون‌پذیر و قابل بازتولید می‌کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-27 — تعداد و روش ساخت اولین مدیران

- **وضعیت:** ✅ بسته؛ یک مدیر اصلی و یک مدیر ارشد پشتیبان جدا تأیید شدند.
- **موضوع:** Bootstrap امن مدیر ارشد و تعداد مدیران اولیه.
- **تصمیم:** اولین مدیر ارشد اصلی با اسکریپت یک‌بارمصرف خارج از بات و شماره ازپیش‌ثبت‌شده ساخته و با Contact فعال می‌شود. او پیش از کاربران واقعی یک مدیر ارشد پشتیبان با هویت جدا می‌سازد؛ OTP/TOTP و حساب مشترک وجود ندارد.
- **کنترل:** Backend حذف یا تعلیق آخرین مدیر ارشد فعال را رد می‌کند. مدیر میانی اولیه الزامی نیست و مدیر ارشد بعداً به تعداد لازم اضافه می‌کند.
- **دلیل:** ساخت اولین مدیر از داخل بات حلقه اعتماد ایجاد می‌کند و داشتن تنها یک مدیر ارشد خطر قفل‌شدن کامل سامانه را بالا می‌برد.
- **تاریخ تصمیم:** 2026-08-28.

### O-28 — پروتکل دقیق همگام‌سازی محلی و سرور

- **وضعیت:** ✅ بسته؛ کاربر طرح تفصیلی بخش 15.3 را تأیید کرد.
- **موضوع:** روش امن، تکرارپذیر و قابل بازیابی انتقال تغییرات از سیستم ایران به سرور هتزنر.
- **تصمیم:** Event Ledger/Outbox پایدار در SQLite، Sync یک‌طرفه با Sequence و Revision، Batch حداکثر 500تایی، Gzip، HTTPS/TLS 1.3، امضای Ed25519 اختصاصی دستگاه، ACK پیوسته و Idempotency در PostgreSQL.
- **جزئیات:** Save بدون تغییر رویداد تولید نمی‌کند؛ رویداد تأییدنشده هرگز حذف نمی‌شود؛ خطای ساختاری Stream را متوقف می‌کند؛ پس از تخلیه کامل Backlog، اصلاحیه‌ها و Catch-up ساخته می‌شوند.
- **دلیل:** این روش در قطعی طولانی، Crash، ACK گم‌شده و ارسال تکراری، از گم‌شدن، دوباره‌شماری و خارج‌شدن ترتیب جلوگیری می‌کند و به IP ثابت ایران نیاز ندارد.
- **تاریخ تصمیم اولیه:** 2026-08-28؛ **اصلاح احراز عامل:** mTLS طبق O-41 در 2026-08-28 با امضای دستگاه جایگزین شد.

### O-29 — دامنه، DNS و گواهی عمومی سرور

- **وضعیت:** ✅ معماری بسته؛ نام دامنه هنوز انتخاب نشده و ورودی اجرایی مؤجل تا پیش از فاز 4 است.
- **موضوع:** انتخاب دامنه/زیردامنه API، DNS و زیرساخت صدور و تمدید گواهی عمومی سرور.
- **تصمیم:** زیردامنه `sync.<domain>` با رکورد A مستقیم به `${SYNC_SERVER_HOST}` از فایل `.env` محلی و غیرقابل‌Commit، اتصال Direct/DNS-only، فقط پورت عمومی 443 و گواهی عمومی ACME با تمدید خودکار استفاده می‌شود. طبق O-41 گواهی Client، mTLS و CA خصوصی وجود ندارند و احراز عامل در لایه برنامه با امضای Ed25519 انجام می‌شود.
- **تعویق مجاز:** کاربر فعلاً دامنه‌ای ندارد؛ نام دامنه و سرویس DNS هنگام آماده‌سازی فاز 4 انتخاب/خرید می‌شوند و این تعویق مانع فاز 1 تا 3 نیست.
- **دلیل:** دامنه امکان اعتبارسنجی پایدار هویت سرور و تمدید خودکار TLS را می‌دهد؛ کلید عمومی مستقل هر دستگاه نیز لغو دسترسی عامل را بدون تغییر دامنه یا گواهی سرور ممکن می‌کند.
- **سابقه:** طرح قبلی CA خصوصی/mTLS با تأیید O-41 جای خود را به امضای دستگاه داد؛ بخش دامنه، DNS مستقیم و ACME بدون تغییر ماند.
- **تاریخ تصمیم معماری:** 2026-08-28؛ **اصلاح O-41:** 2026-08-28؛ **موعد انتخاب نام:** پیش از فاز 4.

### O-30 — اعتبار دعوت حسابدار

- **وضعیت:** ✅ بسته؛ دعوت یک‌بارمصرف 24ساعته و چرخه لغو/صدور مجدد تأیید شد.
- **موضوع:** جلوگیری از باقی‌ماندن لینک دعوت قابل استفاده برای مدت نامحدود.
- **تصمیم:** دعوت دقیقاً 24 ساعت، یک‌بارمصرف و جزو سقف سه حسابدار است. صاحب حساب می‌تواند آن را لغو یا برای همان حساب/شماره دوباره صادر کند و صدور جدید، Token قبلی را فوراً باطل می‌کند.
- **کنترل:** Token غیرقابل حدس و بدون شماره موبایل است؛ مصرف، انقضا، لغو و صدور مجدد در Audit ثبت می‌شوند و پذیرش به Contact خود فرد با شماره منطبق نیاز دارد.
- **دلیل:** پنجره کوتاه، خطر استفاده از لینک قدیمی یا فورواردشده را کم می‌کند و صدور مجدد مسیر ساده بازیابی را نگه می‌دارد.
- **تاریخ تصمیم:** 2026-08-28.

### O-31 — تشخیص پایان Save و OneDrive

- **وضعیت:** ✅ بسته؛ Debounce، Stability Probe و Coalescing تأیید شدند.
- **موضوع:** جلوگیری از خواندن فایل نیمه‌نوشته یا ساخت Importهای تکراری براثر چند رویداد Excel/OneDrive.
- **تصمیم:** عامل فقط فایل عملیاتی تنظیم‌شده را می‌پاید و فایل موقت/Conflict را نادیده می‌گیرد؛ پس از آخرین Event دو ثانیه Debounce و دو بررسی پیاپی Size/mtime، آزادبودن فایل و سلامت ZIP/XLSX انجام می‌دهد.
- **جزئیات:** چند Save متوالی یک Import از آخرین وضعیت می‌سازند؛ Save هنگام Import دقیقاً یک Follow-up ایجاد و Hash یکسان صفر Event می‌سازد. رفتار Rename/Replace و فاصله Probe در آزمون محیط واقعی OneDrive تنظیم می‌شود.
- **تفکیک اجرا:** هسته زمان‌بندی اعلان و تلاش خواندن در O-72/ADR-0010 و WP-07 با شواهد ساختگی تعریف شده است؛ دریافت اعلان واقعی و نگه‌داشتن مالکیت تلاش تا Commit پایدار Import در بسته‌های بعدی اثبات می‌شوند. قاعده دوثانیه‌ای و رفتار کسب‌وکار تغییر نکرده‌اند.
- **دلیل:** Event فایل به‌تنهایی پایان نوشتن را تضمین نمی‌کند و OneDrive ممکن است چند Event یا Replace ایجاد کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-32 — تطبیق RSهای تاریخی کاملاً مشابه

- **وضعیت:** ✅ بسته؛ تطبیق یکتا و منع جفت‌سازی تصادفی تأیید شد.
- **موضوع:** چند RS قدیمی با تاریخ، مبلغ و توضیحات یکسان ممکن است بیش از یک زوج کاندیدا داشته باشند.
- **تصمیم:** کاندیدا با اشخاص دو طرف، تاریخ، مبلغ، صاحب حساب/توضیحات و جهت مالی ساخته می‌شود؛ فقط تطبیق یکتای یک‌به‌یک relation_id قطعی می‌گیرد. چند کاندیدای یکسان به گروه ابهام می‌روند و هرگز با مجاورت یا انتخاب تصادفی جفت نمی‌شوند.
- **دامنه توقف:** Raw و Import سایر ردیف‌ها حفظ می‌شوند؛ فقط Ledger، مانده و گزارش RS اشخاص متأثر تا رفع ابهام منتشر نمی‌شود. RS جدید با UUID و relation_id پایدار دیگر به تطبیق اسمی وابسته نیست.
- **دلیل:** جفت‌سازی اشتباه اثر مالی دو شخص را تغییر می‌دهد و از توقف محدود پرخطرتر است.
- **تاریخ تصمیم:** 2026-08-28.

### O-33 — نمایش فیلدهای صفر یا خالی

- **وضعیت:** ✅ بسته؛ قواعد متن و PDF تأیید شدند.
- **موضوع:** نمایش قیمت، تخفیف صفر، توضیحات و شماره فاکتور خالی.
- **تصمیم:** تخفیف صفر و فیلد اختیاری خالی از متن حذف می‌شوند و «بدون توضیح» درج نمی‌شود؛ قیمت، مبلغ، کالا و مقدار الزامی باقی می‌مانند، صفر واقعی با Null یکی نیست و مبلغ/قیمت با جداکننده هزارگان و واحد تومان نمایش داده می‌شوند. PDF ستون را نگه می‌دارد و سلول خالی نشان می‌دهد.
- **دلیل:** متن کوتاه‌تر می‌شود، بدون آنکه صفر معنادار یا اطلاعات اجباری از بین برود.
- **تاریخ تصمیم:** 2026-08-28.

### O-34 — نمایش تغییرات در اصلاحیه

- **وضعیت:** ✅ بسته؛ خلاصه تغییر، Diff مالی و گزارش کامل تأیید شدند.
- **موضوع:** نحوه نمایش مقدار قبلی/جدید بدون افشای جزئیات فنی.
- **تصمیم:** اصلاحیه تعداد Added/Edited/Deleted را در بالا، فیلدهای مالی تغییرکرده را به صورت «قبلی ← جدید» و ردیف افزوده/حذف‌شده را با عنوان روشن نشان می‌دهد؛ سپس گزارش کامل همان تاریخ و آخرین مانده می‌آید.
- **کنترل:** UUID، شماره ردیف و Metadata فنی برای کاربر پنهان و Diff خام کامل برای مدیر ارشد/Audit حفظ می‌شود.
- **دلیل:** گیرنده علت اثر مالی اصلاحیه را می‌بیند و درعین‌حال متن با داده فنی شلوغ نمی‌شود.
- **تاریخ تصمیم:** 2026-08-28.

### O-35 — PDFهای بسیار بزرگ و صف تولید

- **وضعیت:** ✅ بسته؛ تقسیم زمانی، صف پایدار و Dedup تأیید شدند.
- **موضوع:** حفظ امکان گزارش بازه طولانی بدون مصرف کنترل‌نشده منابع.
- **تصمیم:** بازه منطقی محدود نمی‌شود، اما هر PDF حداکثر یک سال شمسی است؛ بازه چندساله به فایل‌های مرتب با parent_report_id مشترک تقسیم و سال بزرگ‌تر از سقف ردیف آزمون‌شده نیز قطعه‌بندی می‌شود.
- **صف:** هر درخواست‌کننده یک Job فعال و حداکثر سه Pending دارد؛ درخواست یکسان Deduplicate و صف در دیتابیس پایدار نگهداری می‌شود.
- **شاهد فنی:** سقف ردیف هر فایل در G3/G7 براساس حافظه، زمان، فونت فارسی و اندازه خروجی تعیین می‌شود.
- **دلیل:** کاربر به بازه کامل دسترسی دارد، ولی یک PDF یا درخواست تکراری منابع سیستم را اشباع نمی‌کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-36 — ترتیب متن، ایموجی و جداکننده

- **وضعیت:** ✅ بسته؛ ساختار خوانا و کم‌تزئین تأیید شد.
- **موضوع:** حفظ شباهت خروجی قدیمی بدون شلوغی یا وابستگی به شماره سطر.
- **تصمیم:** ترتیب نوع/تاریخ، شخص، پول، خریدوفروش، ورودوخروج، مانده، report_id و مهلت تصمیم ثابت است. بخش خالی حذف، خطوط با `•`، جداکننده ساده و ایموجی فقط در عنوان بخش استفاده می‌شوند؛ شماره سطر یا شماره‌گذاری 100به‌بعد نمایش داده نمی‌شود.
- **پیام بلند:** هر قطعه «بخش X از N» و report_id مشترک دارد و دکمه‌ها فقط زیر قطعه آخر هستند.
- **شاهد:** واژه‌پردازی نهایی روی نمونه‌های واقعی در G3 تأیید می‌شود.
- **تاریخ تصمیم:** 2026-08-28.

### O-37 — اولویت و نرخ تخلیه Backlog

- **وضعیت:** ✅ بسته؛ اولویت صف و منع VIP تأیید شد.
- **موضوع:** ترتیب پردازش هنگام بازگشت از قطعی طولانی.
- **تصمیم:** ترتیب Sync/Commit کامل → اصلاحیه رسمی → گزارش روزانه Catch-up → PDF درخواستی/عملیات عادی است. VIP یا اولویت دستی مشتری وجود ندارد؛ در هر سطح تاریخ قدیمی‌تر و سپس party_id ملاک است.
- **کنترل:** صف‌های Sync، تولید و ارسال جدا و پایدارند، Rate Limit و `retry_after` مقدم است و مدیر پیشرفت تخلیه را می‌بیند. تعداد Worker و Batch در آزمون بار تنظیم می‌شوند.
- **دلیل:** داده ناتمام نباید گزارش بسازد و اولویت پایدار از تبعیض یا بی‌نظمی جلوگیری می‌کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-38 — تعریف موفقیت در تحویل ناقص

- **وضعیت:** ✅ بسته؛ Cursor حساب و گیرنده تفکیک شدند.
- **موضوع:** حالت دریافت مالک و شکست حسابدار یا برعکس.
- **تصمیم:** `published` با اولین تحویل کامل، Cursor تولید حساب را جلو می‌برد؛ `fully_delivered` فقط با تحویل کامل همه گیرندگان است. هر گیرنده Cursor تحویل مستقل دارد.
- **رفتار:** گیرنده ناموفق همان report_id را Retry می‌گیرد و گیرنده موفق تکرار نمی‌شود. اگر هیچ گیرنده‌ای تحویل کامل نداشته باشد، published و Cursor تولید جلو نمی‌روند.
- **دلیل:** شکست یک گیرنده نباید گزارش جدید یا پیام تکراری برای دیگران بسازد و درعین‌حال نباید از پیگیری تحویل او حذف شود.
- **تاریخ تصمیم:** 2026-08-28.

### O-39 — منو و نام دکمه‌های بات

- **وضعیت:** ✅ بسته؛ منوهای نقش‌ها و الگوی Keyboard تأیید شدند.
- **موضوع:** نام و ترتیب مسیرهای اصلی بات.
- **تصمیم:** صاحب حساب منوهای «مانده فعلی»، «تراکنش‌های امروز»، «گزارش یک روز»، «گزارش بازه زمانی»، «سوابق گزارش‌ها»، «حسابداران من» و «راهنما» دارد؛ حسابدار همان منو بدون مدیریت حسابدار می‌بیند. مدیر میانی ابتدا مشتری را انتخاب و مدیر ارشد علاوه بر آن ارسال روز قبل، ناموفق‌ها، نتایج و مدیریت کاربران را می‌بیند.
- **رابط:** سطح اول Reply Keyboard ثابت و عملیات درون مسیر Inline Button است.
- **دلیل:** مسیرهای پرتکرار کوتاه و محدودیت نقش از همان سطح رابط روشن می‌ماند، درحالی‌که Backend همچنان مجوز را مستقل کنترل می‌کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-40 — ورود تاریخ و تقویم شمسی تلگرام

- **وضعیت:** ✅ بسته؛ تقویم شمسی Inline و ورودی تایپی تأیید شدند.
- **موضوع:** انتخاب روز یا بازه بدون وابستگی به Date Picker میلادی تلگرام.
- **تصمیم:** تقویم شمسی سفارشی با Inline Keyboard، حرکت ماه قبل/بعد و انتخاب روز روش اصلی است. تایپ `YYYY/MM/DD` نیز با نرمال‌سازی ارقام فارسی/عربی/لاتین و `/` یا `-` پشتیبانی می‌شود.
- **بازه و خطا:** شروع سپس پایان و خلاصه تأیید می‌آید؛ تاریخ نامعتبر/آینده یا پایان کوچک‌تر بی‌صدا اصلاح نمی‌شود و Back، Cancel و ورود مجدد همیشه در دسترس‌اند. بازه بزرگ طبق O-35 تقسیم می‌شود.
- **شاهد:** کتابخانه و Prototype نهایی در G5/G6 با Callback منقضی و همه نقش‌ها آزموده می‌شوند.
- **دلیل:** کاربر فارسی‌زبان بدون تبدیل دستی تاریخ، هم مسیر دکمه‌ای و هم ورود سریع متنی دارد.
- **تاریخ تصمیم:** 2026-08-28.

### O-41 — احراز هویت عامل محلی در ارتباط با سرور

- **وضعیت:** ✅ بسته؛ HTTPS عمومی همراه کلید و امضای اختصاصی Ed25519 دستگاه تأیید شد.
- **موضوع:** احراز امن عامل ویندوز بدون سرور CA، نشست ثابت یا وابستگی به IP ایران.
- **تصمیم:** عامل جفت‌کلید Ed25519 را روی همان میزبان می‌سازد و کلید خصوصی را در مخزن امن Windows نگه می‌دارد. ثبت اولیه با username/password قوی و یک‌بارمصرف مخصوص Enrollment انجام، Credential پس از نخستین ثبت معتبر غیرفعال و فقط کلید عمومی، اثرانگشت، `device_id` و وضعیت فعال/لغوشده در سرور نگهداری می‌شود.
- **امضای درخواست:** هر Batch روی نمایش Canonical شامل `device_id`، روش و مسیر، Challenge یک‌بارمصرف سرور، `batch_id`، بازه Sequence و `body_hash` امضا می‌شود. سرور پیش از Commit امضا، Challenge، وضعیت دستگاه، Hash و ترتیب را بررسی می‌کند؛ Retry با Challenge تازه و همان Idempotency Key انجام می‌شود.
- **چرخه عمر:** مدیر ارشد می‌تواند دستگاه را فوراً لغو کند. چرخش عادی با کلید قبلی امضا و اتمیک فعال می‌شود؛ در فقدان/خرابی کلید، Enrollment تازه و لغو کلید قبلی لازم است. Secret خصوصی هرگز به سرور یا Backup داده مالی منتقل نمی‌شود.
- **گزینه‌های کنارگذاشته‌شده:** mTLS/CA خصوصی به‌دلیل هزینه عملیاتی غیرضروری برای این پروژه و Bearer Session ثابت یک‌ماهه به‌دلیل انقضا در قطعی طولانی و خطر Replay انتخاب نشدند. طرح CA به سرور جدا نیاز نداشت، اما با این تصمیم اصولاً CA خصوصی حذف شد.
- **شاهد اجرایی:** نگهداری کلید در مخزن مناسب Stack روی Windows، Canonicalization ثابت، بردار آزمون Ed25519، Replay، تغییر بدنه، لغو، چرخش و بازیابی دستگاه باید پیش از G4 اثبات شوند.
- **تاریخ تصمیم:** 2026-08-28.

### O-42 — مدل تاریخ شمسی و کتابخانه تبدیل

- **وضعیت:** ✅ بسته؛ قواعد ذخیره، تبدیل و آزمون تاریخ تأیید شد.
- **موضوع:** جلوگیری از تبدیل ناسازگار تاریخ میان Excel، دیتابیس و تلگرام.
- **تصمیم:** مقدار خام تاریخ Excel حفظ و یک تاریخ Canonical قابل Query به‌همراه سال/ماه/روز شمسی و کلید سال مالی ذخیره می‌شود. تبدیل دست‌نویس ممنوع، نسخه کتابخانه و `calculation_version` ثابت و مرجع زمانی `Asia/Tehran` است.
- **شاهد اجرایی:** نام بسته پس از انتخاب Stack تعیین و در Lockfile قفل می‌شود؛ نوروز، کبیسه، پایان ماه، رفت‌وبرگشت و ورودی نامعتبر باید پیش از G2/G5 آزمون شوند.
- **تاریخ تصمیم:** 2026-08-28.

### O-43 — رمزگذاری داده و Backup

- **وضعیت:** ✅ بسته؛ کاربر الزام رمزگذاری داده و Backup را نپذیرفت.
- **موضوع:** آیا داده مالی، دیتابیس و نسخه پشتیبان در سطح برنامه رمزگذاری شوند؟
- **تصمیم:** رمزگذاری در سطح برنامه در نسخه اول الزام نیست، چون منبع Excel نیز بدون این لایه نگهداری می‌شود. این تصمیم عبارت «Backup رمزگذاری‌شده» در O-20 را جایگزین می‌کند؛ رمزگذاری شفاف موجود در دیسک یا سرویس ذخیره‌سازی نه الزامی است و نه غیرفعال می‌شود.
- **مرز تصمیم:** HTTPS/TLS، Hash یک‌طرفه گذرواژه، محافظت Token/کلید خصوصی در مخزن امن سیستم‌عامل، عدم درج Secret در کد/لاگ و ACL محدود همچنان الزامی‌اند؛ اینها حفاظت اسرار احراز هویت‌اند، نه رمزگذاری داده مالی.
- **ریسک پذیرفته‌شده:** سرقت فیزیکی رسانه یا دسترسی سطح سیستم‌عامل می‌تواند Backup را افشا کند؛ کنترل باقی‌مانده جداسازی فیزیکی، حداقل دسترسی و ممیزی است.
- **تاریخ تصمیم:** 2026-08-28.

### O-44 — نگهداری لاگ و ناشناس‌سازی هویت

- **وضعیت:** ✅ بسته؛ دوره‌ها و استثناها تأیید شدند.
- **موضوع:** مدت نگهداری داده عملیاتی و هویتی پس از پایان رابطه.
- **تصمیم:** لاگ فنی/Debug تا 90 روز و لاگ امنیتی تا یک سال می‌مانند؛ دفتر مالی، Snapshot گزارش و ممیزی اصلی در نسخه اول حذف خودکار ندارند و PDF موقت پس از تحویل موفق حذف می‌شود. شماره موبایل و Telegram User ID یک سال پس از پایان دسترسی نگهداری و سپس ناشناس‌سازی می‌شوند، مگر اختلاف باز یا الزام قانونی.
- **هماهنگی با O-43:** نگهداری یک‌ساله شناسه‌ها الزام رمزگذاری برنامه‌ای ندارد و با ACL و ممیزی دسترسی محافظت می‌شود.
- **تاریخ تصمیم:** 2026-08-28.

### O-45 — تغییر مالکیت شماره یا حساب تلگرام

- **وضعیت:** ✅ بسته؛ فرایند انتقال خودکار لازم نیست و تطبیق دقیق Telegram User ID ملاک است.
- **موضوع:** رفتار سامانه در صورت تغییر Telegram User ID یا مالکیت شماره.
- **تصمیم:** شماره موبایل به‌تنهایی دسترسی را به Telegram User ID تازه منتقل نمی‌کند. هر ID متفاوت کاملاً غیرمجاز است و داده، PDF، پیام گزارش و Callback دریافت نمی‌کند؛ تنها استثنای فعلی، تعویض دستی، اتمیک و ممیزی‌شده O-22 با تأیید مدیر ارشد است.
- **دلیل:** تغییر شماره یا Contact نباید اتصال Telegram را خودکار و قابل تصاحب کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-46 — نقش‌ها و اختیار در اجرای چندایجنتی

- **وضعیت:** ✅ بسته؛ مدل مالک محصول/مدیر پروژه/مجری با تفکیک خودتأییدی تأیید شد.
- **تصمیم:** کاربر مالک محصول، تعیین‌کننده چشم‌انداز و آزمون‌کننده پذیرش نهایی است. Codex مدیر پروژه و بازبین مستقل، مسئول Roadmap، اولویت‌بندی، Scope بسته‌ها، تصمیم ADR، پذیرش Handoff، Gate و Merge است. Google Antigravity صرفاً مجری است و حق خودتأییدی، بستن Gate، Merge یا تغییر Roadmap را ندارد.
- **گردش کار:** Antigravity بسته را اجرا و متوقف می‌شود؛ Codex شواهد را بازبینی و در صورت نقص چرخه اصلاح را ادامه می‌دهد یا در صورت قبولی Merge/Gate را پیش می‌برد. مالک در پایان محصول را آزمون و اصلاحات را به مدیر پروژه اعلام می‌کند؛ دستور تازه مالک بر تصمیم مدیریتی مقدم است.
- **مرز اختیار:** این واگذاری، مجوز ضمنی برای تغییر Excel اصلی/کپی واقعی، داده یا Secret واقعی، Production، هزینه تازه یا عملیات مخرب نیست؛ این موارد مطابق O-47/O-50 اختیار صریح و هدف‌دار مالک می‌خواهند.
- **تاریخ تصمیم:** 2026-08-30؛ جایگزین مدل تأیید روزمره مالک در 2026-08-28.

### O-47 — Baseline، Branch و حفاظت Git

- **وضعیت:** ✅ بسته؛ سیاست Git مرحله‌ای و محافظت‌شده تأیید شد.
- **تصمیم:** پیش از کدنویسی Baseline پاک، `.gitignore` و `main` محافظت‌شده ساخته می‌شود. هر Work Package در Branch/Worktree مانند `antigravity/phase-XX-...` اجرا و فقط پس از Handoff، آزمون و بازبینی/تصمیم Codex مدیر پروژه ادغام می‌شود؛ Commit مستقیم و Force Push روی شاخه مشترک ممنوع است.
- **دارایی محافظت‌شده:** Roadmap فقط توسط Codex مدیر پروژه و از مسیر Branch/PR قابل تغییر است و Antigravity حق ویرایش آن را ندارد. Excel مرجع/کپی واقعی، داده یا Secret واقعی، Telegram/سرور/DNS تولید، هزینه تازه و Migration مخرب فقط با اختیار صریح و هدف‌دار مالک قابل تغییرند.
- **تاریخ تصمیم:** 2026-08-28.

### O-48 — قرارداد Handoff مجری

- **وضعیت:** ✅ بسته؛ قالب استاندارد تحویل تأیید شد.
- **تصمیم:** هر بسته در `handoffs/phase-XX/work-package/` حداقل `handoff.md`، `acceptance-matrix.md` و `test-results.txt` دارد و هدف/Scope، ارجاع Roadmap، فایل‌های تغییرکرده، Schema/Migration، فرمان و Exit Code، آزمون و شاهد، فرض/مورد باز، ریسک، Rollback و تأیید عدم تغییر دارایی محافظت‌شده را ثبت می‌کند.
- **قاعده پذیرش:** ادعای مجری بدون Artifact و شاهد کافی نیست و مجری پس از تحویل برای بازبینی متوقف می‌شود.
- **تاریخ تصمیم:** 2026-08-28.

### O-49 — ADR و مرز تصمیم فنی

- **وضعیت:** ✅ بسته؛ ثبت تصمیم معماری و مرز اختیار تأیید شد.
- **تصمیم:** تصمیم‌های معماری در `docs/adr/ADR-xxxx-...md` با گزینه‌ها، پیشنهاد، Trade-off، هزینه تغییر و اثر Roadmap ثبت می‌شوند. Codex مدیر پروژه تصمیم‌های فنی، مالی و رفتار قابل‌مشاهده داخل چشم‌انداز و قواعد قطعی Roadmap را تصویب می‌کند؛ Antigravity فقط پیشنهاد می‌دهد و متوقف می‌شود.
- **مرز ارجاع مالک:** تغییر چشم‌انداز یا قاعده صریح مالک، هزینه/سرویس تازه، استفاده از داده واقعی، دامنه/Production، تغییر ناسازگار یا اقدام روی دارایی محافظت‌شده فقط با اختیار هدف‌دار مالک انجام می‌شود.
- **تاریخ تصمیم:** 2026-08-28.

### O-50 — معنای وضعیت‌ها و Stop Condition

- **وضعیت:** ✅ بسته؛ قواعد اجرایی نشان‌های Roadmap تأیید شد.
- **تصمیم:** `✅` اجرای متن مصوب، `🧪` فقط تولید شاهد لازم، `🟦` توقف مجری و مسیر ADR/تصمیم Codex مدیر پروژه، `🟨` توقف تا تصمیم مدیر پروژه یا فراهم‌شدن وابستگی و `⛔` منع اجرا است. هیچ Gate دارای شاهد `🧪` ناموفق یا ناقص بسته نمی‌شود.
- **توقف سخت:** Antigravity حق تغییر Roadmap/Gate را ندارد و باید آن را به Codex مدیر پروژه ارجاع دهد. تغییر Excel اصلی/کپی واقعی، داده یا شماره واقعی، Telegram تولید، سرور/DNS تولید، Secret، هزینه تازه و Migration مخرب بدون اختیار صریح و هدف‌دار مالک ممنوع است.
- **تاریخ تصمیم:** 2026-08-28.

### O-51 — معماری مخزن عملیاتی عمومی و آرشیو خصوصی

- **وضعیت:** ✅ بسته؛ مخزن عمومی به‌عنوان منبع عملیاتی انتخاب شد و تصمیم قبلی Private عملیاتی را جایگزین می‌کند.
- **تصمیم:** `mohsenbarari/accounting-bot-public` منبع عملیاتی پروژه، کد، Roadmap و Skill است. مخزن Private پیشین فقط آرشیو تاریخچه قبلی باقی می‌ماند؛ روی سرور Clone نمی‌شود و تغییر محصول تازه دریافت نمی‌کند. تاریخچه Private به Public منتقل یا عمومی نمی‌شود.
- **کنترل:** هر PR پیش از Merge باید Secret/Data Scan و بازبینی Codex را بگذراند. Excel/SQLite/PDF واقعی، شماره موبایل، Telegram ID، Credential، Token، کلید، Host/IP واقعی، Dump، Backup و Artifact تولید هرگز Commit نمی‌شوند؛ فقط Fixture ساختگی مجاز است.
- **شاهد اجرا:** مخزن عمومی از Snapshot پاک با Root Commit مستقل ساخته شد؛ IP و شناسه‌های عملیاتی با Placeholder جایگزین و `main` آن با PR اجباری، Enforce Admin و منع Force Push/Deletion محافظت شد. انتخاب صریح کاربر در 2026-08-29 این مخزن را از Snapshot به مرجع عملیاتی ارتقا داد.
- **مرز پیکربندی:** ایمیل Commit بنا بر تصمیم کاربر محرمانه محسوب نمی‌شود. تنظیمات واقعی فقط در Secret store یا `.env` محلیِ غیرقابل‌Commit قرار می‌گیرند و فایل نمونه صرفاً نام متغیر و Placeholder امن دارد.
- **تاریخ تصمیم:** 2026-08-29؛ جایگزین تصمیم 2026-08-28.

### O-52 — سبک معماری و زبان پایه

- **وضعیت:** ✅ بسته؛ Modular Monolith، Monorepo و Python 3.13 تأیید شدند.
- **موضوع:** ساختار کد و زبان مشترک Agent و سرور.
- **تصمیم:** Monorepo، Modular Monolith در سرور، Agent مستقل ویندوز و Python 3.13 برای هر دو؛ Domain حسابداری Python خالص و مستقل از Framework/Telegram/DB باشد و API/Webhook/Worker Processهای جدا از همان Packageها استفاده کنند.
- **دلیل:** بیشترین بازاستفاده از تحلیل و Prototype موجود با کمترین پیچیدگی توزیع‌شده؛ امکان جداسازی Service در آینده بدون شکستن Domain.
- **شاهد لازم:** ADR و Spike سازگاری COM/PyInstaller/Python پیش از G1.
- **تاریخ تصمیم:** 2026-08-28.

### O-53 — Stack عامل محلی ویندوز

- **وضعیت:** ✅ بسته؛ Stack عامل محلی و مدل اجرای تعاملی تأیید شدند.
- **تصمیم:** `pywin32` برای Excel COM و DPAPI، `watchdog`، `zipfile` + `lxml.iterparse`، `sqlite3`، `httpx`، `cryptography`، `Pydantic`/`pydantic-settings` و `PyInstaller`. اجرا در Session تعاملی با Task Scheduler/Startup، نه Windows Service.
- **دلیل:** UUID باید در رویداد BeforeSave حافظه زنده Excel درج شود، ولی استخراج خام سریع و مستقل از Recalculation بماند؛ SQLite صف دیسکی بدون Server جدا می‌دهد.
- **Fallback:** اگر Spike رویداد COM یا بسته‌بندی را غیرقابل‌اعتماد نشان داد، فقط Bridge کوچک C#/.NET برای رویداد Excel با ADR پیشنهاد می‌شود و هسته حسابداری Python می‌ماند.
- **تاریخ تصمیم:** 2026-08-28.

### O-54 — Stack API، دیتابیس و صف مرکزی

- **وضعیت:** ✅ بسته؛ Stack سرور و صف PostgreSQL تأیید شدند.
- **تصمیم:** `FastAPI`، `Pydantic v2`، `SQLAlchemy 2.0`، `Alembic`، `psycopg 3` و PostgreSQL 18. صف نسخه اول جدول PostgreSQL با Lease/Attempt/`due_at`/Idempotency و `FOR UPDATE SKIP LOCKED` باشد و Redis/Celery/RabbitMQ اضافه نشوند.
- **دلیل:** حجم پروژه به Broker جدا نیاز ندارد؛ صف داخل همان تراکنش گزارش/تحویل، ممیزی و بازیابی را ساده‌تر و دقیق‌تر می‌کند.
- **شرط بازنگری:** فقط Benchmark یا نیاز عملی چندسروری می‌تواند با ADR افزودن Broker را توجیه کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-55 — Stack تلگرام، تاریخ و PDF فارسی

- **وضعیت:** ✅ بسته؛ Stack تلگرام، تاریخ شمسی و PDF فارسی تأیید شدند.
- **تصمیم:** `aiogram 3` با Webhook/Secret Token، `persiantools` + `zoneinfo` برای شمسی/تهران، `Jinja2` برای Template و `Playwright + Chromium` با فونت Vazirmatn برای PDF RTL/A4.
- **دلیل:** Chromium رندر دوجهته فارسی را بهتر از گزینه‌ای که آن را رسماً محدود اعلام می‌کند پوشش می‌دهد؛ PDF Worker جدا هزینه Browser را از API/Telegram جدا نگه می‌دارد.
- **شاهد لازم:** Golden Test تقویم و Visual Regression/استخراج متن PDF نمونه «ریزحساب» پیش از G3/G6.
- **تاریخ تصمیم:** 2026-08-28.

### O-56 — استقرار، وابستگی‌ها و کیفیت

- **وضعیت:** ✅ بسته؛ Stack استقرار و کنترل کیفیت تأیید شدند.
- **تصمیم:** Docker Compose تک‌سرور با Caddy، API/Webhook، Worker و PostgreSQL؛ `uv` + `uv.lock`؛ `pytest`، `pytest-asyncio`، `Hypothesis`، Testcontainers، `Ruff`، `mypy` و Structured JSON Logging.
- **دلیل:** برای یک سرور هتزنر، Compose قابل‌بازیابی و کم‌هزینه‌تر از Kubernetes است؛ Lockfile و آزمون‌های Property/Integration برای قواعد مالی و Idempotency ضروری‌اند.
- **خارج از نسخه اول مگر با شاهد:** Kubernetes، Service Mesh، Redis، Celery، RabbitMQ و سامانه مانیتورینگ خارجی پولی.
- **تاریخ تصمیم:** 2026-08-28.

### O-57 — جداسازی شخص هویتی از شخص حسابداری

- **وضعیت:** ✅ بسته؛ مدل هویتی جدا تأیید شد.
- **تصمیم:** فرد دارای موبایل/Telegram یک شخص هویتی مستقل از `party_id` مالی است. صاحب حساب از طریق عضویت به یک حساب مالی متصل می‌شود و حسابدار می‌تواند بدون حضور در «لیست کسبه» هویت معتبر داشته باشد؛ نبود Telegram برای شخص حسابداری، داده مالی او را حذف یا ادغام نمی‌کند.
- **دلیل:** هویت دسترسی و طرف حساب مالی چرخه عمر و قواعد متفاوت دارند و ادغام آنها می‌تواند سابقه مالی یا مجوز را به فرد نادرست منتقل کند.
- **شاهد لازم:** آزمون ماتریس هویت و نقش در G5.
- **تاریخ تصمیم:** 2026-08-28.

### O-58 — هویت پایدار رویداد مالی

- **وضعیت:** ✅ بسته؛ مدل Financial Event تأیید شد.
- **تصمیم:** هر واقعیت دریافت، پرداخت، خرید، فروش یا جابه‌جایی کالا یک Financial Event پایدار دارد. ویرایش Raw با شناسه ثابت Revision همان رویداد را تغییر می‌دهد و Void آن را ابطال می‌کند؛ ردیف Excel یا اجرای مجدد Import هویت تازه نمی‌سازد.
- **دلیل:** اصلاحیه و Audit باید تغییر یک واقعیت را از ایجاد تراکنش تازه تشخیص دهند.
- **شاهد لازم:** آزمون Rebuild/Revision/Void و Idempotency در G1/G2.
- **تاریخ تصمیم:** 2026-08-28.

### O-59 — پایه‌های دفتر نسخه‌دار

- **وضعیت:** ✅ بسته؛ `ledger_entries` به‌عنوان Projection مالی مصوب شدند.
- **تصمیم:** هر نسخه معتبر Financial Event یک یا چند پایه نسخه‌دار بدهکار/بستانکار تولید می‌کند و مانده‌ها با Query روی نسخه فعال آنها محاسبه می‌شوند. پایه‌ها از Raw و `calculation_version` قابل بازتولیدند و Formula/خروجی Excel منبع عملیاتی نیست.
- **دلیل:** محاسبه مستقل، Reconciliation، RS چندطرفه و بازسازی تاریخی بدون وابستگی به Formula اکسل ممکن می‌شود.
- **شاهد لازم:** حذف Projection آزمایشی و Rebuild کامل باید همان مانده و گزارش را در G2 تولید کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-60 — توضیح خالی در C و D

- **وضعیت:** ✅ بسته؛ حفظ مبلغ و هشدار نقص تأیید شد.
- **تصمیم:** توضیح خالی C/D مبلغ را از دفتر، مانده یا گزارش حذف نمی‌کند. گزارش متن عمومی دریافت/پرداخت بدون ساخت نام حساب نشان می‌دهد و نقص یک هشدار قابل‌پیگیری برای مدیر ارشد/Audit می‌سازد.
- **دلیل:** نبود شرح نباید واقعیت مبلغ را از بین ببرد، ولی نقص اطلاعات نیز نباید پنهان بماند.
- **شاهد لازم:** نمونه‌های C/D با شرح پر/خالی در G2/G3.
- **تاریخ تصمیم:** 2026-08-28.

### O-61 — الگوریتم قطعی رقابت 100 میلی‌ثانیه‌ای

- **وضعیت:** ✅ بسته؛ پنجره رد و تقدم تأیید تأیید شد.
- **تصمیم:** تأیید اول فوراً نتیجه را تأیید می‌کند. رد اول تا 100 میلی‌ثانیه Pending می‌ماند؛ تأیید معتبر با زمان دریافت سرور `<= reject_received_at + 100ms` برنده است، وگرنه رد نهایی می‌شود. تراکنش/قفل ردیف و Idempotency باید فقط یک نتیجه نهایی بسازند.
- **مرز:** مهلت سه‌ساعته و تأیید خودکار بدون رأی مستقل از این پنجره باقی می‌مانند.
- **شاهد لازم:** آزمون کمتر/برابر/بیشتر از مرز، Callback هم‌زمان و Retry در G6.
- **تاریخ تصمیم:** 2026-08-28.

### O-62 — کاربر ثبت‌شده ولی فعال‌نشده در Telegram

- **وضعیت:** ✅ بسته؛ رفتار `pending_activation` تأیید شد.
- **موضوع:** رفتار گزارش و Retry برای فردی که شماره/هویت او ثبت شده ولی هنوز بات را Start و فعال نکرده است.
- **تصمیم:** وضعیت مستقل `pending_activation` دارد و Failure ارسال محسوب نمی‌شود؛ تا Start/احراز، API تلگرام برای او Retry نمی‌شود. مدیر ارشد او را جدا از ناموفق‌ها می‌بیند و پس از فعال‌سازی فقط آخرین گزارش رسمی مشمول به‌علاوه گزارش‌های آینده ارسال می‌شود، نه تمام تاریخچه معوق.
- **دلیل:** تلگرام پیش از Start امکان پیام ندارد و Retry مکرر هم بی‌اثر است؛ تفکیک وضعیت، آمار خطا را درست نگه می‌دارد و از هجوم گزارش‌های قدیمی جلوگیری می‌کند.
- **تاریخ تصمیم:** 2026-08-28.

### O-63 — مشاهده ممیزی امنیتی توسط نقش‌ها

- **وضعیت:** ✅ بسته؛ پیشنهاد توسعه قابلیت امنیتی رد شد.
- **موضوع:** کدام بخش از سابقه امنیتی برای صاحب حساب، حسابدار و مدیران قابل مشاهده باشد.
- **تصمیم:** هیچ صفحه، دکمه یا مسیر تازه‌ای برای مشاهده «رویدادهای امنیتی» به بات هیچ نقشی اضافه نمی‌شود و دکمه‌ها و کاربردهای بات همان تصمیم‌های قبلی باقی می‌مانند. لاگ‌ها و ممیزی داخلی ازپیش‌مصوب برای عملیات، خطا، دسترسی و بازیابی حذف نمی‌شوند، اما قابلیت کاربرمحور تازه‌ای از آنها ساخته نمی‌شود. نمایش سوابق عملیاتی و سلامت همگام‌سازی ازپیش‌مصوب مدیر ارشد نیز به همان دامنه قبلی محدود است.
- **دلیل:** کاربر پیچیدگی و کاربرد تازه امنیتی در رابط بات را لازم نمی‌داند و حفظ رفتار قبلی بات را خواسته است.
- **تاریخ تصمیم:** 2026-08-28.

### O-64 — Mask شماره موبایل

- **وضعیت:** ✅ بسته؛ Mask رد و نمایش کامل تأیید شد.
- **موضوع:** محل نمایش شماره کامل و الگوی پوشاندن آن.
- **تصمیم:** هرجا قابلیت مصوب بات شماره موبایل را نمایش می‌دهد، شماره باید کامل، واضح و بدون Mask دیده شود. این تصمیم شماره موبایل را به خروجی‌هایی مانند گزارش روزانه، اصلاحیه، PDF، Handoff یا لاگ فنی که طبق طراحی قبلی محل نمایش شماره نیستند اضافه نمی‌کند؛ Token دعوت نیز همچنان فاقد شماره است.
- **ریسک پذیرفته‌شده:** نمایش کامل نسبت به Mask حریم خصوصی کمتری دارد؛ کنترل فقط بر مجوز Backend همان مسیرهای مصوب و جلوگیری از افزودن شماره به خروجی‌های نامرتبط باقی می‌ماند.
- **تاریخ تصمیم:** 2026-08-28.

### O-65 — دامنه و فراخوانی Skill Antigravity

- **وضعیت:** ✅ بسته؛ محل قابل‌استفاده برای Antigravity تعیین و نصب اولیه انجام شد.
- **موضوع:** Skill به‌صورت Global یا Project Scope نصب و چگونه الزام‌آور شود.
- **تصمیم:** نسخه Project Scope در `.agents/skills/accounting-bot-implementer/` مخزن عملیاتی منبع یگانه است و مسیر Global رسمی Antigravity یعنی `/root/.gemini/config/skills/accounting-bot-implementer/` با Symlink مدیریت‌شده همان منبع را ارائه می‌کند، نه نسخه مستقل قابل‌ویرایش. هر Work Package نیز نام Skill را صریحاً در Prompt ذکر می‌کند.
- **شاهد نصب:** در 2026-08-30 مسیر Global به نسخه Project Scope داخل `/srv/accounting-bot/workspace/.agents/skills/accounting-bot-implementer/` پیوند داده شد؛ فایل اصلی Skill در هر دو مسیر SHA-256 یکسان دارد و Validator قالب‌ها Exit Code صفر داد. سپس خروجی نشست واقعی ارائه‌شده توسط کاربر، کشف Skill در `/skills` و رفتار صحیح Dry Run پیش از G0 را اثبات کرد.
- **دلیل:** مسیر Global برای کشف Antigravity حفظ شده ولی محتوای آن مستقیماً از مخزن عملیاتی می‌آید؛ بنابراین نسخه مستقل قابل‌ویرایش و Drift وجود ندارد.
- **تاریخ تصمیم:** 2026-08-28.

### O-66 — منبع یگانه Roadmap و Skill میان سیستم محلی و سرور

- **وضعیت:** ✅ بسته؛ مخزن عمومی عملیاتی به‌عنوان منبع یگانه تأیید شد.
- **موضوع:** جلوگیری از ایجاد دو نسخه قابل‌ویرایش متفاوت در OneDrive و پروژه عملیاتی سرور.
- **تصمیم:** مخزن Public عملیاتی Git یعنی `mohsenbarari/accounting-bot-public` منبع یگانه و Versioned برای `ROADMAP.md` و `.agents/skills/` است؛ سرور و سیستم محلی Clone/Worktree همان مخزن هستند. مخزن Private قبلی آرشیو فقط‌خواندنی است و نسخه موازیِ قابل‌ویرایش محسوب نمی‌شود.
- **دلیل:** Antigravity و Codex باید دقیقاً یک Roadmap و Skill را بخوانند؛ کپی دستی دوطرفه دیر یا زود باعث Drift و اجرای قاعده قدیمی می‌شود.
- **مرز اختیار:** تغییر سرور، Volume، DNS، Secret، داده واقعی و Merge همچنان فقط در حدود اجازه صریح مربوط انجام می‌شود.
- **شاهد اجرا:** Remote اصلی سیستم محلی به مخزن Public تغییر کرد؛ مخزن Private پیشین Archived است. Clone کنترل‌شده Public روی Volume سرور انجام، Commit با `origin/main` تطبیق و نصب Global به نسخه Project Scope همان Clone پیوند داده شد. فقط Forward Test واقعی Antigravity باقی مانده است.
- **تاریخ تصمیم:** 2026-08-29؛ جایگزین انتخاب Private در 2026-08-28.

### O-67 — حفاظت فنی شاخه `main` در GitHub Free

- **وضعیت:** ✅ بسته؛ انتقال منبع عملیاتی به Public و استفاده از حفاظت موجود تأیید شد.
- **موضوع:** اعمال فنی الزام «عدم Commit مستقیم یا Force Push روی `main`» بدون خرید GitHub Pro.
- **تصمیم:** مخزن عمومی عملیاتی می‌ماند و همه تغییرات از Branch/PR عبور می‌کنند؛ Require Pull Request و Enforce for administrators فعال و Force Push/Deletion ممنوع است. خرید GitHub Pro در محدوده فعلی لازم نیست.
- **شاهد:** Branch Protection مخزن عمومی در 2026-08-29 اعمال و راستی‌آزمایی شد. پاسخ 403 مخزن Private صرفاً سابقه علت تغییر تصمیم است و دیگر مانع عملیاتی نیست.
- **تاریخ تصمیم:** 2026-08-29.

### O-68 — قرارداد Canonical و Hash منبع

- **وضعیت:** ✅ بسته؛ قرارداد فنی نسخه 1 توسط Codex مدیر پروژه تصویب و در ADR-0006 ثبت شد.
- **موضوع:** تولید نتیجه یکسان برای تشخیص تغییر سطر/شیت روی Agent ویندوز و سرور، بدون وابستگی به ترتیب Mapping، Sort ردیف یا Serializer پلتفرم.
- **تصمیم:** `source-hash.v1` و `sheet-snapshot-hash.v1` با SHA-256 روی UTF-8 JSON آرایه‌ای تایپ‌دار، فشرده و دارای Golden Vector ساخته می‌شوند. ترتیب فیلدهای سطر دقیقاً از `raw-source-contract.v1` و ترتیب Snapshot از UUIDv7 Canonical است؛ شناسه فنی در Source Hash نیست ولی با Hash سطر در Snapshot جفت می‌شود. Null/Text صریح، Raw Text بدون Normalize، Integer/Decimal بدون Float و تاریخ شمسی Canonical با `persiantools`/`jalali-date.v1` هستند.
- **مرز:** `ledger_hash` تا تثبیت Financial Event/Resolver/Calculation Version در فاز 2 مؤجل است. WP-03 هیچ XLSX Parser، Schema/Revision/Void، قاعده مالی یا دسترسی Excel/داده واقعی ایجاد نمی‌کند.
- **نسخه‌بندی:** هر تغییر مؤثر بر bytes یا معنا، نسخه تازه و Migration/Rebuild شاهددار می‌خواهد و حق بازتفسیر خاموش Hash نسخه 1 وجود ندارد.
- **شاهد اجرا:** WP-03 با Golden bytes/digest چهار شیت و Snapshot، حساسیت تک‌تک فیلدهای Raw، Property Test ترتیب Mapping/Snapshot، اثر Insert/Edit/Delete، مرزهای Decimal/Null/Text/TypeTag، آزمون تقویم/`Asia/Tehran` و 64 آزمون موفق پذیرفته شد؛ CI نهایی Windows/Linux در Run `33329429229` و Merge Commit برابر `6be9fef` است.
- **تاریخ تصمیم:** 2026-08-30.

### O-69 — Snapshot کامل و برنامه تغییر قطعی منبع

- **وضعیت:** ✅ بسته؛ قرارداد فنی نسخه 1 توسط Codex مدیر پروژه تصویب و در ADR-0007 ثبت شد.
- **موضوع:** جلوگیری از تبدیل Snapshot ناقص به حذف کاذب و تولید نتیجه یکسان Insert/Edit/`void`/بدون‌تغییر مستقل از ترتیب شیت و ردیف.
- **تصمیم:** `source-change-plan.v1` فقط یک `ValidatedSourceWorkbookSnapshot` تایپ‌شده را می‌پذیرد که دقیقاً هر چهار شیت WP-02 را یک‌بار دارد، UUID/Raw/Hash هر ردیف را با WP-03 بازتولید کرده و UUID را در کل Workbook یکتا می‌داند؛ فلگ‌هایی مانند `allow_void`/`is_complete` و Hash مورداعتماد Caller مجاز نیستند. وضعیت قبلی همه هویت‌های Active و Voided، Home Sheet و آخرین Revision را نگه می‌دارد و Plan با ترتیب شیت رجیستری سپس bytes UUID ساخته می‌شود.
- **انتقال‌ها:** هویت تازه Insert با Revision 1؛ Active با Hash یکسان بدون‌تغییر؛ Active با Hash متفاوت Edit و Revision بعدی؛ Active غایب فقط در Snapshot کامل `void` و Tombstone Revision بعدی؛ Voided غایب بدون رویداد؛ بازگشت Voided در همان شیت Edit/فعال‌سازی دوباره و Revision بعدی است. انتقال UUID شناخته‌شده به شیت دیگر خطای هویتی سراسری است و به Void+Insert تبدیل نمی‌شود.
- **مرز:** اعتبار Source Contract شرط لازم است ولی به‌تنهایی اجازه Commit نیست؛ Requiredness/Business Validation، Parser اکسل، فایل Snapshot، SQLite، `import_id`، Commit اتمیک، Outbox و دامنه اثر مؤجل‌اند. WP-04 فقط مدل/Planner خالص و شواهد ساختگی را می‌سازد و هیچ دسترسی به Excel یا داده واقعی ندارد.
- **شاهد لازم:** جدول انتقال کامل، رد Missing/Duplicate/Unknown Sheet و UUID سراسری/Relocation، Deep Immutability، شمارش/Revision دقیق، Property Test جایگشت و Retry منطقی، Hash شیت مطابق WP-03 و شاهد نبود مقایسه درجه دو روی 15,000 ردیف ساختگی.
- **شاهد اجرا:** WP-04 در 2026-08-31 پس از بازبینی مستقل سه دور اصلاح، 89 آزمون موفق و CI Windows/Linux در Run `33360537972` پذیرفته و در PR #16 با Merge Commit `ebdbe837bf5064ad1809f7f66da8fac9ba1d6a55` ادغام شد. آزمون‌های مستقل، اعتبارسنجی سازنده‌ها، کپی دفاعی، تطابق دقیق Index/ردیف، شمارنده و Hash سخت‌گیرانه، ترتیب Raw مطابق رجیستری و برابری کامل Plan برای Mappingهای مستقل را تأیید کردند. بنچ‌مارک مستقل 15,000 ردیف ساختگی Build را `2.5017s` و Plan را `0.2897s` اندازه گرفت؛ این شاهد جایگزین معیار کارایی فایل واقعی یا Import سرتاسری G1 نیست.
- **تاریخ تصمیم:** 2026-08-30.

### O-70 — قرارداد Reader فقط‌خواندنی XLSX

- **وضعیت:** ✅ بسته؛ قرارداد فنی نسخه 1 در ADR-0008 و شواهد پیاده‌سازی مصنوعی WP-05 توسط Codex مدیر پروژه پذیرفته شدند؛ معیار فایل واقعی و G1 همچنان 🧪 هستند.
- **موضوع:** تبدیل سلول فیزیکی فایل ثابت به Snapshot معتبر چهار شیت، بدون ورود نتیجه فرمول، از دست‌رفتن دقت عدد یا حذف کاذب براثر خواندن ناقص.
- **تصمیم:** `xlsx-source-reader.v1` در `accounting-local-agent` با `zipfile` و `lxml 6.1.2` قفل‌شده، قسمت‌های XLSX را از Relationshipها پیدا و فقط چهار شیت رجیستری را Streaming می‌خواند؛ Headerهای ردیف 1 و شناسه فنی دقیق‌اند، شیت کمکی منبع نیست و Snapshot تنها پس از پایان موفق کل خواندن و Builder موجود WP-04 برگردانده می‌شود. Location سطرها جدا از Hash و به‌صورت تغییرناپذیر نگهداری می‌شود.
- **سلول و فرمول:** متن و Unicode بدون Normalize و Null جدا از متن خالی حفظ می‌شوند؛ عدد XML مستقیماً به Decimal و عدد ستون کمکی متنی به Lexeme متنی تبدیل می‌شود. تاریخ مالی/UUID فقط متن‌اند؛ تاریخ سریالی، Float، گردکردن و تبدیل تومان مجاز نیستند. نمای علمی عدد XML با حفظ دقت قابل Decode است ولی نمای علمیِ ورودی متنی همچنان طبق WP-03 رد می‌شود. Formula و Cache پیش از تبدیل کنار گذاشته می‌شوند؛ خروجی Array/Data Table بدون `f` مستقل نیز مستثنی است، اما Literal مستقل داخل محدوده Shared Formula حذف نمی‌شود.
- **فعالیت ردیف:** فقط Literal غیرخالی در `activity_columns` رجیستری ردیف را فعال می‌کند؛ صفر داده است، ولی تاریخ/UUID/Formula/Style تنها فعالیت نیست. Raw متن فعال Trim نمی‌شود. ردیف بدون فعالیت حتی با تاریخ/شناسه باقی‌مانده از مجموعه منبع حذف می‌شود؛ پاک‌کردن ورودی‌ها تأیید یا سقف تعدادی نمی‌خواهد و تبدیل آن به `void` فقط پس از اعتبارسنجی کامل مراحل بعد انجام می‌شود. هر ردیف فعال UUIDv7 معتبر می‌خواهد؛ Reader شناسه نمی‌سازد یا تعمیر نمی‌کند.
- **مرز:** تنها Fixtureهای XLSX کاملاً ساختگیِ تولیدشده هنگام آزمون مجازند؛ فایل مرجع و کپی واقعی حتی خوانده نمی‌شوند. ثبات/کپی فایل، COM/Save، نوشتن UUID، سال مالی/آرشیو، Requiredness و قواعد مالی، SQLite/Outbox و ارسال گزارش مؤجل‌اند. خروجی Reader به‌تنهایی مجوز Commit نیست.
- **شاهد لازم:** آزمون XML/ZIP واقعی ساختگی برای Header/Relationship/نوع سلول/فرمول و محدوده آن/ردیف فعال/UUID، شکست کامل فایل ناقص و نگهداری ردیف‌های پنهان، Property Test با فایل‌های مستقل و اتصال به Planner؛ سنجش 15,000 ردیف فعال ساختگی با Tail فرمولی و RSS واقعی روی Linux/Windows، حفظ 89 آزمون قبلی، کنترل‌های کیفیت و Handoff کامل. معیار کارایی فایل واقعی و G1 باز می‌مانند.
- **شاهد اجرا:** WP-05 در 2026-09-01 پس از بازبینی مستقل چندمرحله‌ای، 200 آزمون موفق و CI Windows/Linux در Run `33505486610` پذیرفته و در PR #19 با Merge Commit `638053a7968110b5d3631b74d1b155da5304cd0a` ادغام شد. CI بنچ‌مارک 15,000 ردیف فعال و 5,000 ردیف Tail را روی Linux در `2.9061s / 58.96 MiB` و روی Windows در `4.6764s / 59.34 MiB` اجرا کرد؛ Header/Relationship، متن/Decimal/Null، فرمول و محدوده خروجی، فعالیت/UUID/Location، SST انتخابی، شکست کامل، Planner/Idempotency و RSS پنجره فراخوانی با Fixture صرفاً ساختگی پوشش داده شدند. این شاهد جایگزین Stable Copy، COM/Save، UUID Writer، SQLite/Import Commit، تطبیق فایل واقعی یا معیار خروج G1 نیست.
- **تاریخ تصمیم:** 2026-08-31.

### O-71 — Acquisition و پاک‌سازی Snapshot پایدار XLSX

- **وضعیت:** ✅ بسته؛ قرارداد فنی نسخه 1 در ADR-0009 و شواهد پیاده‌سازی ساختگی WP-06 توسط Codex مدیر پروژه پذیرفته شدند؛ معیار فایل واقعی، Save/OneDrive و G1 همچنان 🧪 هستند.
- **موضوع:** جلوگیری از خواندن ترکیبی دو نسل فایل هنگام Save/Replace و تعیین مالکیت روشن برای Hash، فایل موقت و پاک‌سازی پیش از ورود به Reader.
- **تصمیم:** `xlsx-snapshot-acquisition.v1` در `accounting-local-agent` مسیر دقیق `.xlsx` را پس از Debounce آینده می‌پذیرد، دو مشاهده Size/`mtime_ns`/هویت فایل را با فاصله صریح مقایسه می‌کند، منبع را فقط‌خواندنی و جریانی به `.part` خصوصی کپی و SHA-256 می‌کند، منبع و کپی را مستقل بازخوانی و فقط پس از تطابق اندازه/Hash و سلامت Container به `.xlsx` اتمیک ارتقا می‌دهد. WP-05 فقط همین مسیر Lease را می‌خواند.
- **چرخه حیات:** Snapshot فقط داخل Context معتبر است؛ در خروج عادی، خطای مصرف‌کننده و شکست Acquisition، فقط Artifactهای دقیق همان Lease بدون حذف بازگشتی پاک می‌شوند. تغییر/حذف Snapshot هنگام مصرف خطای تمامیت است و خطای Cleanup نباید خطای اصلی را پنهان کند. بازیابی Artifact پس از Crash مؤجل است.
- **خطا و مرز:** تغییر، Replace، دسترسی موقت یا ZIP ناتمام «Source Not Ready» قابل Retry است؛ Policy نامعتبر، Storage، Integrity و Cleanup دلیل تایپ‌شده جدا دارند و پیام پیش‌فرض مسیر مطلق/داده خام را افشا نمی‌کند. این بسته Directory Watch، Debounce دوثانیه‌ای، Coalescing/Follow-up، قاعده واقعی OneDrive/File Release، COM/UUID، سال مالی، SQLite/Outbox یا داده واقعی را اجرا نمی‌کند.
- **شاهد لازم:** آزمون‌های Race تزریق‌شده در همه مرزهای مشاهده/کپی/بازخوانی، حفظ بایت و Metadata منبع، Streaming محدود، ZIP ناقص، Storage/Promotion/Cleanup، چند Lease هم‌زمان و اتصال Snapshot ساختگی چهارشیتی به WP-05/WP-04؛ Benchmark ترکیبی 15,000 ردیف زیر همان 15 ثانیه/128 MiB در CI Windows/Linux و حفظ 200 آزمون قبلی. معیار فایل واقعی، Save/OneDrive و G1 باز می‌مانند.
- **شاهد اجرا:** WP-06 در 2026-09-02 پس از بازبینی مستقل commit `503fe2c9635bf268c91c720462413ac75e568efc`، بازاجرای حفظ Candidate خارجی و منبع Hardlinkشده، پیام‌های تایپ‌شده امن و حفظ علت‌های همزمان Query/Close و جایگزینی واقعی هویت فایل پذیرفته شد. CI نهایی Run `33616797405` روی Linux دارای 272 قبولی و 2 آزمون مخصوص Windows اجرا‌نشده و روی Windows دارای 273 قبولی و فقط 1 آزمون مخصوص POSIX اجرا‌نشده بود؛ هر چهار سناریوی symlink ویندوز اجرا شدند. بنچ‌مارک مستقل ترکیبی 15,000 ردیف روی Linux برابر `12.8081s / 59.83 MiB` و روی Windows برابر `2.8819s / 56.52 MiB` بود. PR #22 با مجوز صریح مالک و Merge Commit `1346abf8b0bda329e6df452bc16a0a0f7d9ed9f7` ادغام شد. این شاهد، آزمون فایل واقعی، Save/OneDrive، COM/UUID، SQLite/Outbox یا پذیرش سرتاسری G1 نیست.
- **تاریخ تصمیم:** 2026-09-01.

### O-72 — هماهنگ‌کننده Save و تلاش خواندن منبع

- **وضعیت:** ✅ بسته؛ قرارداد فنی نسخه 1 در ADR-0010 و شواهد پیاده‌سازی ساختگی WP-07 توسط Codex مدیر پروژه پذیرفته شدند؛ معیار محیط واقعی و G1 همچنان 🧪 هستند.
- **موضوع:** جلوگیری از تلاش‌های هم‌زمان یا تکراری و گم‌شدن Save تازه هنگام acquisition، خواندن یا پاک‌سازی، با مرز روشن میان خواندن و Commit مالی.
- **تصمیم:** `save-import-coordinator.v1` در `accounting-local-agent` اعلان‌های تایپ‌شده را فقط با مسیر دقیق و قواعد واژگانی سیستم‌عامل تطبیق می‌دهد؛ دو سوی Move بررسی می‌شوند و مسیر تغییرنام‌یافته جای منبع تنظیم‌شده را نمی‌گیرد. با ساعت `monotonic_ns` و فاصله ثابت دو ثانیه، حالت‌های Idle/Waiting/Running/Faulted، حداکثر یک Token فعال متعلق به همان Coordinator و یک قصد Follow-up نگهداری می‌شوند. اعلان‌های نامرتبط تایمر را تغییر نمی‌دهند؛ I/O و Reader زیر قفل وضعیت اجرا نمی‌شوند.
- **تکمیل و خطا:** موفقیت فقط پس از خروج سالم Lease و پاک‌سازی اعلام می‌شود. فقط خطای مستقیم `source_not_ready` بدون نیاز به اعلان تازه حداقل دو ثانیه پس از پایان دوباره بررسی می‌شود؛ رد مستقیم Reader منتظر Save تازه می‌ماند و Follow-up ازقبل‌دریافت‌شده را حفظ می‌کند. خطاهای غیرقابل Retry، گروه خطاها و Cancellation به Faulted می‌روند؛ اعلان‌ها حفظ می‌شوند ولی ادامه نیازمند `resume_after_fault()` صریح برنامه است. قواعد جدیدی برای تأیید کاربر، سقف ویرایش/حذف یا UI ساخته نمی‌شود.
- **مرز:** این وضعیت موقت فقط زمان‌بندی تلاش خواندن است، نه صف مالی، Outbox یا Baseline موفق Import. هیچ Hash فایل جای Hash منبع را نمی‌گیرد؛ برابری Raw با Planner پذیرفته‌شده WP-04 آزموده می‌شود. Driver فقط WP-06 و WP-05 را ترکیب می‌کند؛ مالکیت تا Commit پایدار، Watchdog Observer، دریافت واقعی اعلان، OneDrive/COM/UUID، داده واقعی و بازیابی Crash مؤجل‌اند.
- **شاهد لازم:** ماتریس SC-01 تا SC-16 با ساعت ساختگی، مرز نانوثانیه‌ای، Move/Path، تجمیع انبوه، Token خارجی/قدیمی، رقابت Take/Finish/Notify با Barrier، همه Outcomeها، خطای خروج Lease، Retry بدون اعلان و اتصال چهارشیت ساختگی؛ حفظ 274 آزمون موجود و حدود Benchmark و موفقیت CI بومی Windows/Linux. G1 و معیار محیط واقعی باز می‌مانند.
- **شاهد اجرا:** کامیت `83a8facb6f86e7971171953e3dfe88dafe33c957` پس از بازبینی مستقل چهارمرحله‌ای، رفع خطای پایان دوباره، حفظ علت‌های مستقل Reader/finish/guard، رزرو اتمیک Token و رد mutation صفر برنده در خود رقابت پذیرفته شد. CI Run `33632031865` روی Linux دارای 312 قبولی و 2 آزمون مخصوص Windows اجرا‌نشده و روی Windows دارای 313 قبولی و فقط 1 آزمون مخصوص POSIX اجرا‌نشده بود؛ هر چهار سناریوی symlink ویندوز اجرا شدند. بنچ‌مارک ترکیبی CI برای 15,000 ردیف روی Linux برابر `2.5104s / 59.95 MiB` و روی Windows برابر `4.7539s / 59.16 MiB` بود؛ سقف 15s/128MiB تغییر نکرد. PR #25 با تأیید صریح مالک و Merge Commit `fe7e19287e66d208124487b96087a1b85f60831f` ادغام شد. [گزارش پذیرش Codex](handoffs/phase-01/wp-07-save-import-coordinator/codex-review.md) مرز این شاهد و محدودیت شکست خود guard را ثبت می‌کند؛ هیچ Baseline/Commit مالی یا آزمون محیط واقعی تأیید نشده است.
- **تاریخ تصمیم:** 2026-09-02.

### O-73 — اجرای Watcher و حلقه خواندن ترتیبی منبع

- **وضعیت:** ✅ قرارداد نسخه 1 و پیاده‌سازی ساختگی WP-08 پذیرفته شدند؛ Codex مدیر پروژه ADR-0011 و شواهد Observer بومی Windows/Linux را تأیید کرد. معیار محیط واقعی و خروج از G1 همچنان 🧪 هستند.
- **موضوع:** اتصال اعلان‌های فایل به WP-07 بدون مسدودکردن Callback، رقابت چند Reader، گم‌شدن Wake یا باقی‌ماندن Thread پس از توقف/شکست.
- **تصمیم:** `source-watch-runtime.v1` یک Observer بومی را فقط روی والد مسیر دقیق و بدون بازگشت در زیرپوشه‌ها اجرا می‌کند؛ ساخت شیء I/O ندارد و Snapshot root از درخت پوشه پایش‌شده جداست. پس از Start موفق یک اعلان اولیه از همان Debounce دوثانیه‌ای عبور می‌کند. Callback فقط رویداد را تطبیق و هماهنگ‌کننده را بیدار می‌کند؛ حلقه ترتیبی در Thread فراخواننده از Driver پذیرفته‌شده استفاده و نتیجه را فقط پس از خروج سالم Lease تحویل می‌دهد.
- **چرخه حیات:** Runtime یک‌بارمصرف با حالت‌های New/Running/Stopping/Stopped/Failed است. درخواست Stop به I/O یا Join منتظر نمی‌ماند؛ تلاش ازقبل‌پذیرفته‌شده می‌تواند تکمیل شود، تلاش تازه پذیرفته نمی‌شود و Teardown همه Workerهای متعلق را متوقف/Join می‌کند. Start ناقص، مرگ Observer/Emitter، خطای Callback/Consumer و شکست‌های همزمان Cleanup با دلیل تایپ‌شده و علت‌های مستقل حفظ می‌شوند؛ Restart یا Resume خودکار وجود ندارد.
- **مرز:** فقط پایش پوشه و XLSX کاملاً ساختگی مجاز است. صف داخلی watchdog سقف قراردادی ندارد و توقف Native/I/O/Consumer الزاماً مهلت سخت ندارد؛ اعلان اولیه تضمین بازیابی پایدار، تشخیص Overflow یا عدم اتلاف همه رویدادها نیست. داده واقعی، OneDrive/Excel/COM/UUID، نصب Agent، اعتبارسنجی مالی/سال و آرشیو، SQLite/Outbox/Commit و بازیابی Crash مؤجل‌اند. تحویل نتیجه Reader هیچ ACK یا Baseline مالی نیست.
- **شاهد لازم:** WR-01 تا WR-18 با Fake Clock/Barrier و Backend تزریقی خصوصی برای Wake/Stop/Start/Failure، آزمون Observer بومی Linux/Windows برای فایل موجود، Create/Save/Replace/Move/Delete و Cleanup، اتصال به Oracle مستقل WP-04، حفظ 314 آزمون موجود و سقف Benchmark 15s/128MiB، Handoff کامل و بازبینی مستقل Codex. G1 باز می‌ماند.
- **شاهد ثبت‌شده:** PR #28 با HEAD بازبینی‌شده `2f51f2e` و Merge `0d1df85` و CI موفق Run `33666088469`؛ سپس اصلاح پایداری WR-08 پس از شکست Windows Run `33666981958` در PR #30 با HEAD `a593e93` و Merge `fdaa98b` پذیرفته شد. CI نهایی اصلاحیه Run `33670197344` دارای 348 قبولی Linux و 349 قبولی Windows، اجرای همه سناریوهای symlink ویندوز و بنچمارک‌های زیر سقف است. [ثبت پذیرش مستقل](handoffs/phase-01/wp-08-source-watch-runtime/codex-review.md) سابقه شکست و رفع آن، شاهد تأخیر کنترل‌شده در هر چهار فاز و مرز ساختگی و ریسک‌های باقی‌مانده را مستند می‌کند.
- **تاریخ تصمیم:** 2026-09-02.

### O-74 — پیش‌بررسی فیلدهای الزامی منبع

- **وضعیت:** ✅ قرارداد فنی نسخه 1 در ADR-0012 و پیاده‌سازی ساختگی WP-09 توسط Codex مدیر پروژه پذیرفته شدند؛ معیار محیط واقعی و خروج از G1 همچنان 🧪 هستند.
- **موضوع:** Snapshot معتبر WP-04/WP-05، کامل‌بودن ساختار و نوع/Hash را اثبات می‌کند ولی ممکن است مقدار ضروری ردیف فعال Null یا متن خالی باشد؛ این کمبود باید پیش از واردات عملیاتی مشخص شود.
- **تصمیم:** `source-requiredness.v1` یک بررسی خالص در `accounting-contracts` است که جدول Requiredness چهار شیت در ADR-0012 را اعمال می‌کند؛ `None` را Missing و متن الزامی با `str.strip()` خالی را Blank گزارش می‌کند، همه موارد را به ترتیب شیت رجیستری/UUID/فیلد گرد می‌آورد و Snapshot اصلی را بدون حذف ردیف یا تغییر مقدار/Hash نگه می‌دارد. نتیجه و شمارنده‌ها از ورودی محاسبه می‌شوند و وضعیت قبولی ادعایی از Caller پذیرفته نمی‌شود.
- **مرز:** صفر و نمایش‌های عددی معتبر حفظ می‌شوند؛ توضیحات، تماس و فیلدهای اختیاری طبق جدول الزامی نمی‌شوند. عیار وابسته به Item Master، قواعد کد/جهت، هویت و کالای ناشناخته، RS، سال/آرشیو و اثر مالی بررسی جداگانه می‌خواهند. `passes_requiredness` مجوز Commit نیست؛ SQLite/Outbox، فایل واقعی، COM/UUID، نصب و انتشار خارج از WP-09 هستند.
- **شاهد لازم:** SR-01 تا SR-14 شامل جدول مستقل همه فیلدها، Null/Blank/Zero، سازنده‌های تغییرناپذیر، گزارش کامل/قطعی، جایگشت، حفظ Raw و اتصال Reader ساختگی؛ مقیاس 15,000 ردیف، حفظ 350 آزمون موجود، کنترل‌های کیفیت و CI Windows/Linux با سقف‌های قبلی. G1 باز می‌ماند.
- **شاهد پذیرفته‌شده:** 53 آزمون اختصاصی، اوراکل مستقل همه 16 فیلد الزامی و 9 فیلد متنی، کنترل‌های حذف عمدی قاعده و آزمون بی‌اثری Import ماژول با کنترل‌های منفی، جایگشت واقعی سطر/کلید و ارزیابی کامل 15,000 ردیف؛ بازبینی جداگانه اصلاحات Codex، CI دو سکویی PR #32 و پس از ادغام، با جزئیات در [ثبت پذیرش](handoffs/phase-01/wp-09-source-requiredness/codex-review.md). هویت کد محصول پذیرفته‌شده و سقف‌های قبلی Reader/Acquisition حفظ شدند.
- **تاریخ تصمیم:** 2026-09-02.
- **تاریخ پذیرش شاهد ساختگی:** 2026-09-03.

### O-75 — شواهد سال مالی از تاریخ‌های خام منبع

- **وضعیت:** ✅ قرارداد فنی نسخه 1 در ADR-0013 و شواهد ساختگی WP-10 پس از بازبینی غیرنویسنده و CI دو سکویی توسط Codex مدیر پروژه پذیرفته و در PR #35 ادغام شدند؛ سیاست اتصال عملیاتی منبع/سال و G1 همچنان جدا و باز هستند.
- **موضوع:** نام یکسان فایل سالانه، تاریخ خالی یا چند سال مشاهده‌شده نمی‌تواند به‌تنهایی زمینه عملیاتی و وضعیت آرشیو را تعیین کند؛ پیش از آن تصمیم، شواهد خام باید بدون تغییر داده و مستقل از محیط استخراج شوند.
- **تصمیم:** `source-fiscal-evidence.v1` در `accounting-contracts` از `date_raw` تمام ردیف‌های سه شیت تراکنشی و `jalali-date.v1`، سال هر ردیف یا None را به ترتیب شیت رجیستری/UUID، سال‌های مشاهده‌شده و شمارش مرتب آنها می‌سازد. ردیف‌های «لیست کسبه» فقط جداگانه شمرده می‌شوند؛ Snapshot، مقدار و Hash بدون تغییر می‌مانند و هیچ سال/شمارش ادعایی از Caller پذیرفته نمی‌شود.
- **مرز:** گزارش هیچ سال واحد عملیاتی، وضعیت آرشیو، مجوز Commit یا رد خودکار برای فایل خالی/چندساله ندارد. O-76/ADR-0014 معنای منطقی کلید منبع/سال، وضعیت ثبت‌شده و انتخاب رجیستری را تعیین می‌کند؛ اتصال به فایل واقعی، تصویر صحیح هویت‌های قبلی و اجرای پایدار فعال‌سازی/آرشیو همچنان پیش‌نیاز جداگانه Import هستند. مانده افتتاحیه، قواعد مالی/RS، SQLite/Outbox، فایل واقعی، مشخصه تازه در اکسل و تغییر Reader/Watcher خارج از WP-10 هستند.
- **شاهد لازم:** FE-01 تا FE-14 با اوراکل مستقل سال/ردیف، Null/Empty/Mixed، سازنده و پیام امن، جایگشت واقعی، بی‌اثری زمان/UUID/شرح و Formula، اتصال Reader/Requiredness/Planner ساختگی و 15,000 ردیف؛ حفظ 403 آزمون موجود، CI بومی Windows/Linux و سقف‌های قبلی Reader/Acquisition. G1 باز می‌ماند.
- **شاهد پذیرفته‌شده:** 56 آزمون اختصاصی و اوراکل مستقل کامل 15,000 ردیف، بی‌اثری Import/ارزیابی با کنترل منفی مشخص، جایگشت واقعی و نسل‌های XLSX با مقایسه مستقل Planner؛ بازبینی عامل جداگانه بدون یافته قابل‌اقدام و CI دو سکویی PR #35 و پس از ادغام با 458 قبولی Windows و 457 قبولی Linux. چهار سناریوی Symlink ویندوز اجرا شدند؛ جزئیات هویت کد، شمارش‌ها، محدودیت‌ها و بازگشت در [ثبت پذیرش](handoffs/phase-01/wp-10-source-fiscal-evidence/codex-review.md) آمده است.
- **دلیل انتخاب:** استخراج شاهد قابل‌آزمون بدون اختراع قانون رد داده یا افزودن قرارداد هویت/ذخیره‌سازی پیش از طراحی آن؛ هزینه گزارش حداکثر O(n log n) زمان و O(n) متادیتا است.
- **تاریخ تصمیم:** 2026-09-03.
- **تاریخ پذیرش شاهد ساختگی:** 2026-09-03.

### O-76 — اتصال منطقی منبع سالانه و انتخاب وضعیت قبلی

- **وضعیت:** ✅ قرارداد فنی نسخه 1 در ADR-0014 توسط Codex مدیر پروژه تصویب و WP-11 صادر شد؛ 🧪 پیاده‌سازی و شواهد آن، اتصال فیزیکی/پایدار و G1 همچنان باز هستند.
- **موضوع:** نام یکسان فایل سالانه یا رجیستری سال قبل نباید منبع تازه را به حذف کاذب سال دیگر تبدیل کند؛ تاریخ‌های مشاهده‌شده، به‌ویژه فایل خالی/چندساله، هویت منبع را اثبات نمی‌کنند.
- **تصمیم:** `source-binding.v1` کلید UUIDv7 مستقل منبع و سال اعلام‌شده، رکوردهای ثبت‌شده ACTIVE/ARCHIVED با رجیستری قبلی تغییرناپذیر و Hash نهایی فقط برای آرشیو را مدل می‌کند. رجیستری برای هر شناسه/سال یک رکورد و حداکثر یک منبع فعال دارد. Resolver فقط برای تطابق کامل کلید فعال، همان شیء رجیستری قبلی را برمی‌گرداند؛ آرشیو و منبع ثبت‌نشده خروجی صریح بدون رجیستری عملیاتی دارند و شناسه شناخته‌شده با سال متناقض خطای تایپ‌شده است. هیچ انتخاب جایگزین براساس سال، ثبت خودکار یا فراخوانی Planner انجام نمی‌شود.
- **هویت و سال:** کلید، معنای منطقی مشخصه داخلی آینده است و از نام فایل/زمان/اکثریت تاریخ‌ها استنتاج نمی‌شود؛ سال تاریخ تراکنش می‌تواند با سال اعلام‌شده منبع تفاوت داشته باشد. شناسه دائمی شخص/رکورد عوض نمی‌شود و حضور همان party_id در نماهای تاریخی سال‌های مختلف مجاز است؛ قرارداد رجیستری‌ها را ادغام و Revision سراسری را بازشماری نمی‌کند.
- **مرز اعتماد:** رکوردها بازنمایی داده قابل‌اعتماد Caller هستند، نه گواه Commit یا تعلق مشخصه به بایت فایل. پیش از Import عملیاتی باید خواندن مشخصه و Raw از همان Acquisition، ثبت اولیه، تصویر عضویت هر منبع همراه با یکتایی/Home Sheet و پیوستگی Revision سراسری، و ورود نهایی/آرشیو/فعال‌سازی اتمیک با بازیابی خطا تعریف و اثبات شوند. مشخصه فیزیکی اکسل، نوشتن فایل واقعی/کپی، SQLite/Outbox، انتقال مانده و تغییر Reader/Watcher در WP-11 اجرا نمی‌شوند.
- **شاهد لازم:** SB-01 تا SB-14 با جدول مستقل مسیرها، نوع/تغییرناپذیری/پیام امن، Property و کنترل منفی I/O، اتصال صریح ساختگی به Planner برای عدم حذف کاذب منبع دیگر، حفظ party_id مشترک و عدم پیمایش مجدد رجیستری 15,000 ردیفی؛ حفظ 459 آزمون پایه و CI بومی Windows/Linux با سقف‌های Reader/Acquisition. قبول نتیجه ACTIVE مجوز Commit یا بستن G1 نیست.
- **دلیل انتخاب:** اتصال صریح و قابل‌آزمون بدون اختراع سال از داده ناقص یا تغییر خاموش قراردادهای پذیرفته‌شده؛ هزینه ساخت رجیستری O(s log s) برای تعداد منابع و جست‌وجو O(1)، بدون کپی/پیمایش ردیف‌های قبلی.
- **تاریخ تصمیم:** 2026-09-03.

## 22. ریسک‌های اصلی

| ریسک | اثر | اقدام کنترلی پیشنهادی |
|---|---|---|
| اشتباه‌گرفتن تکمیل Reader با Commit Import یا گم‌شدن Save حین کار | جاافتادن تغییر یا اجرای هم‌زمان | Token اتمیک و Follow-up قطعی O-72، تکمیل پس از Lease، و تعویق Baseline/Commit پایدار به قرارداد Import |
| نبود شناسه پایدار در اکسل | اشتباه‌گرفتن ویرایش با حذف/ایجاد | record_id مخفی و نسخه‌بندی |
| ادغام شخص هویتی با شخص حسابداری | انتقال اشتباه مجوز یا سابقه مالی | موجودیت‌های مستقل، عضویت صریح و آزمون O-57 |
| تولید غیرقطعی Event یا Ledger | مانده/اصلاحیه تکراری یا غیرقابل‌بازسازی | شناسه و Revision پایدار، calculation_version، Idempotency و Rebuild آزموده O-58/O-59 |
| نام‌های ناسازگار | اتصال تراکنش به شخص اشتباه | party_id، جدول Alias و صف رفع ابهام |
| کالای ناشناخته یا واحد اشتباه | مانده کالایی نادرست | Item Master، حفظ نام خام و توقف محدود انتشار دامنه متأثر |
| تبدیل ضمنی ریال/تومان | اختلاف ده‌برابری مانده | ذخیره عدد صحیح منبع با Metadata صریح `TOMAN` و بدون تبدیل |
| فرمول یا Cached Value قدیمی | مانده نادرست | محاسبه مستقل و اعتبارسنجی |
| ورود Formula/Query به‌عنوان منبع | وابستگی به Recalculation اکسل و Revision کاذب | Raw Whitelist، Ledger مستقل و Projection نسخه‌دار قابل بازتولید |
| تشخیص Cache خروجی Array/Data Table به‌عنوان Literal یا حذف Literal داخل Shared Range | اثر مالی نادرست یا حذف کاذب منبع | طبقه‌بندی پیش از Decode، پوشش محدوده فرمول و آزمون ZIP/XML مستقل طبق O-70 |
| Float یا Round در نقطه نادرست | اختلاف تجمعی مانده پول/کالا | Integer تومان، Decimal کامل، ROUND_HALF_UP و تطبیق مرزی با اکسل |
| انتقال سالانه | دوباره‌شماری مانده | Opening Balance فقط برای 1405 و Reconciliation بعدی |
| استنتاج سال عملیاتی از نام فایل، بیشترین تاریخ یا فایل خالی | انتخاب سال نادرست یا حذف کاذب داده سال دیگر | مشاهده خالص O-75 و انتخاب رجیستری با کلید صریح O-76؛ اتصال کلید به همان بایت Snapshot و تصویر هویت/Revision پایدار پیش از Import عملیاتی، بدون حداقل تعداد ردیف |
| تلقی متادیتای اتصال به‌عنوان گواه ثبت نهایی یا تعویض سال | ورود آرشیو یا از دست‌رفتن پیوستگی هویت مشترک | مرز اعتماد صریح O-76؛ خواندن مشخصه/Raw از یک Acquisition و پروتکل پایدار عضویت/Revision/آرشیو با آزمون Crash پیش از اتصال عملیاتی |
| زوج ناقص RS | گزارش اشتباه برای دو شخص | relation_id و توقف رویداد ناسازگار |
| RS تاریخی چندکاندیدایی | جفت‌شدن اشتباه و اثر مالی دو شخص | منع تطبیق تصادفی، گروه ابهام و توقف محدود دامنه متأثر |
| Eventهای متعدد یا فایل نیمه‌نوشته OneDrive | Import ناقص یا تکراری | Debounce، Stability Probe، Hash و یک Follow-up Import |
| تغییر منبع حین کپی یا باقی‌ماندن Snapshot کنترل‌شده | ترکیب دو نسل فایل، حذف کاذب یا افشای Artifact موقت | Lease نسخه‌دار O-71، بازخوانی مستقل منبع/کپی، ارتقای اتمیک، پاک‌سازی دقیق و بازیابی Crash در بسته بعدی |
| طبقه‌بندی نادرست خطای Import | رد بی‌دلیل کل فایل یا انتشار مانده مبهم | ماتریس دو سطحی: Rollback خطای فنی سراسری و توقف محدود ابهام نام/کالا/RS تاریخی؛ آزمون Failure Injection |
| قطعی اینترنت/VPN | تأخیر در Sync و ارسال | Outbox محلی پایدار، Catch-up و Retry امن |
| پرشدن دیسک در قطعی طولانی | توقف Import یا از دست‌رفتن قابلیت ثبت | بودجه 4 KiB/رویداد، رزرو 2 GiB، هشدار ظرفیت و Backup محلی دوم |
| Batch تکراری یا خارج از ترتیب | دوباره‌شماری یا نسخه نادرست | Sequence، Revision، Hash، ACK پیوسته و Idempotency |
| انقضای گواهی عمومی یا افشای کلید خصوصی دستگاه | توقف Sync یا اتصال عامل غیرمجاز | ACME خودکار، مخزن امن Windows، کلید مجزای هر دستگاه و لغو/چرخش فوری مطابق O-41 |
| Replay یا تغییر درخواست امضاشده | تکرار عملیات یا پذیرش بدنه جعلی | Challenge یک‌بارمصرف، امضای Canonical روش/مسیر/Batch/Sequence/Body Hash و Idempotency |
| تکرار عملیات پس از Crash | تراکنش یا پیام تکراری | Idempotency Key و Commit اتمیک |
| دسترسی با Callback جعلی | افشای حساب دیگران | مجوزسنجی Backend برای هر درخواست |
| کاربر ثبت‌شده ولی فعال‌نشده | Retry بی‌اثر، آمار خطای کاذب یا هجوم Backlog پس از Start | `pending_activation`، حذف از Retry و ارسال فقط آخرین گزارش مشمول مطابق O-62 |
| Race در تأیید/رد | دو نتیجه نهایی یا رعایت‌نشدن تقدم تأیید | ساعت دریافت سرور، پنجره `<=100ms`، قفل ردیف و Idempotency مطابق O-61 |
| باقی‌ماندن Telegram User ID قبلی | دسترسی حساب سرقت‌شده یا قدیمی | اتصال یکتا، تعویض اتمیک، ابطال Callback و اعلان امنیتی |
| Override رأی تاریخی توسط مدیر | ازبین‌رفتن اعتبار ممیزی | ممنوعیت Override و Administrative Resolution مستقل با دلیل |
| شماره موبایل اشتباه یا تکراری | اتصال هویت نادرست | ورود در مرحله آخر، استانداردسازی و بازبینی |
| نمایش کامل شماره موبایل | مشاهده شماره توسط فردی خارج از مسیر مجاز | ریسک Mask‌نکردن طبق O-64 پذیرفته شده؛ مجوز Backend، نمایش فقط در مسیر مصوب و عدم افزودن به گزارش/PDF/Handoff/لاگ نامرتبط |
| محدودیت طول و نرخ تلگرام | گزارش ناقص | قطعه‌بندی و صف ارسال |
| Cursor مشترک برای تحویل ناقص | تکرار برای گیرنده موفق یا فراموشی ناموفق | تفکیک published/fully_delivered و Cursor حساب/گیرنده |
| تاریخ میلادی/نامعتبر در رابط فارسی | گزارش بازه اشتباه | Inline Calendar شمسی، نرمال‌سازی ورودی و تأیید بازه |
| تخلیه بی‌اولویت Backlog | گزارش از داده ناقص یا گرسنگی صف | اولویت ثابت، صف‌های جدا و ترتیب تاریخ/party_id |
| بسته‌شدن زودهنگام Checkpoint یا Job تکراری | اصلاحیه از داده ناقص یا پیام تکراری | High-watermark پیوسته و امضاشده، کلید Idempotency نسخه‌دار و منع Job روی ACK میانی |
| اعلان زیاد یا پنهان‌ماندن شکست Import | بی‌توجهی مدیر یا ارسال از داده ناقص | عدم اعلان Save موفق، هشدار فوری خطا/صف غیرعادی و نمایش اجباری وضعیت Sync پیش از ارسال رسمی |
| PDF بازه بسیار بزرگ یا درخواست تکراری | مصرف زیاد RAM/CPU و تأخیر صف | تقسیم سالانه/ردیفی، یک Active، سه Pending و Dedup پایدار |
| Writerهای هم‌زمان مرکزی | رقابت و ناسازگاری تراکنش | PostgreSQL تأییدشده، قید یکتا و تراکنش اتمیک |
| نبود Restore آزموده‌شده | از دست رفتن خدمت پس از حادثه | آزمون دوره‌ای بازیابی |
| خرابی دیسک محلی یا سرور اصلی | از دست‌رفتن صف یا نسخه پشتیبان هم‌مکان | Backup ساعتی روی محل فیزیکی دوم، فضای مرکزی جدا و PITR |
| Backup بدون رمزگذاری برنامه‌ای | افشای مالی در سرقت رسانه یا دسترسی سطح سیستم‌عامل | ریسک پذیرفته‌شده O-43؛ جداسازی فیزیکی، ACL حداقلی، ممیزی و عدم نگهداری Secret در Backup |
| خودتأییدی یا تغییر Scope توسط Agent مجری | عبور نقص از Gate یا انحراف از Roadmap | جداسازی Owner/Project Manager/Implementer، Handoff شاهددار و تصمیم مستقل Codex مطابق O-46 تا O-50 |
| کشف‌نشدن یا رعایت‌نشدن Skill در Antigravity | اجرای مستقیم بدون Gate/Handoff | نصب Global فعلی، Skill پروژه‌ای نهایی `.agents/skills`، فراخوانی صریح در Work Package و Forward Test واقعی `/skills`/Dry Run پیش از G0 |
| Drift میان Roadmap/Skill محلی و سرور | اجرای قاعده قدیمی یا بازبینی فایل متفاوت | مخزن Public عملیاتی به‌عنوان منبع یگانه نسخه‌دار طبق O-66؛ Global فقط استقرار مدیریت‌شده همان منبع |
| نبود Baseline و Branch محافظت‌شده | اختلاط تغییرات، Rollback دشوار یا آسیب به دارایی کاربر | Baseline پاک، Worktree/Branch هر بسته، منع Commit مستقیم/Force Push و Merge فقط پس از بازبینی Codex مدیر پروژه |
| ورود ناخواسته داده یا Secret به مخزن Public | افشای اطلاعات مالی یا دسترسی تولید | `.gitignore` سخت‌گیرانه، Fixture صرفاً ساختگی، Secret/Data Scan اجباری هر PR و بازبینی O-51 |
| باقی‌ماندن دو منبع فعال Public/Private | Drift و اجرای Roadmap قدیمی | Public تنها منبع فعال؛ Private فقط آرشیو بدون Clone سرور یا تغییر تازه طبق O-51/O-66 |
| ناسازگاری Stack مصوب با Excel COM، بسته‌بندی یا RTL | بازنویسی پرهزینه Agent/PDF | Spike COM/PyInstaller پیش از G1، Golden/Visual Test PDF پیش از G3 و امکان Bridge محدود C# فقط با ADR |

## 23. تعریف پایان پروژه نسخه اول

نسخه اول زمانی آماده راه‌اندازی است که:

- همه تصمیم‌های بحرانی دفتر موارد باز بسته شده باشند.
- چهار منبع اکسل بدون تکثیر و با نسخه‌بندی همگام شوند.
- مانده پول و کالا با اکسل مرجع تطبیق کامل داشته باشد.
- انتقال سال جدید مانده را دوباره نشمارد.
- گزارش‌های روزانه و اصلاحیه متن و گزارش‌های درخواستی PDF باشند.
- تصمیم مشترک سه‌ساعته و اولویت تأیید در 100 میلی‌ثانیه درست عمل کند.
- نقش‌ها و ممنوعیت‌ها در Backend آزموده شده باشند.
- قطعی، Retry، Backup و Restore آزمون موفق داشته باشند.
- شماره‌های واقعی فقط در مرحله نهایی وارد و یکتا شده باشند.
- اولین ارسال واقعی با گزارش تحویل کامل و امکان Retry انجام شود.
- آزمون پذیرش نهایی مالک انجام و تمام اصلاحات اعلام‌شده او حل و دوباره آزموده شده باشند.

## 24. دفترچه تغییرات Roadmap

| نسخه | تاریخ | تغییر |
|---|---|---|
| 0.50 | 2026-09-03 | ثبت تصمیم فنی O-76 و ADR-0014 و صدور WP-11 برای کلید منطقی منبع سالانه، رجیستری تغییرناپذیر فعال/آرشیو و انتخاب وضعیت قبلی همان منبع؛ تعیین SB-01 تا SB-14، مرز صریح متادیتای Caller با تعلق به فایل/ثبت نهایی، حفظ شناسه مشترک اشخاص و تعویق تصویر پایدار Revision و انتقال سال اتمیک؛ حفظ 459 آزمون پایه، بدون تغییر کد محصول یا بستن G1 |
| 0.49 | 2026-09-03 | ثبت پذیرش WP-10/O-75 پس از پیاده‌سازی Codex و بازبینی عامل غیرنویسنده بدون یافته؛ 56 آزمون اختصاصی و حفظ 403 آزمون پایه، CI دو سکویی PR #35 در Run `33728526844` و ادغام با Commit `0573775`؛ CI نهایی main در Run `33729096159` با 458/457 قبولی Windows/Linux و اجرای چهار سناریوی Symlink ویندوز؛ حفظ مرز مشاهده خالص و داده ساختگی، G1 باز و عدم صدور بسته بعدی |
| 0.48 | 2026-09-03 | ثبت تصمیم فنی O-75 و ADR-0013 و صدور WP-10 برای استخراج خالص شواهد سال مالی از تاریخ‌های Raw، تفکیک ردیف بدون تاریخ و غیرتراکنشی، شمارش/ترتیب قطعی و حفظ کامل Snapshot؛ تعیین FE-01 تا FE-14 و مرز صریح گزارش با انتخاب سال عملیاتی، آرشیو، حذف و Commit؛ حفظ 403 آزمون پایه و سقف‌های کارایی، بدون تغییر کد محصول یا بستن G1 |
| 0.47 | 2026-09-03 | ثبت پذیرش WP-09/O-74 پس از اصلاحات، ادامه محدود آزمون‌ها با مجوز مالک و بازبینی جداگانه Codex؛ 53 آزمون اختصاصی و حفظ 350 آزمون پایه، CI دو سکویی PR #32 در Run `33721864845` و ادغام با Commit `fb077a7`؛ CI نهایی main در Run `33722627358` با 402/401 قبولی Windows/Linux و چهار سناریوی symlink ویندوز، پس از تکرار Job لینوکس به علت timeout دریافت manifest ابزار uv؛ حفظ مرز ساختگی و G1 باز، بدون صدور بسته بعدی |
| 0.46 | 2026-09-02 | ثبت تصمیم فنی O-74 و ADR-0012 و صدور WP-09 برای پیش‌بررسی خالص فیلدهای الزامی چهار شیت، تفکیک Null/Blank/Zero، گزارش قطعی و حفظ کامل Raw؛ تعیین SR-01 تا SR-14 و مرز روشن قبولی Requiredness با اعتبار مالی/RS/سال و Commit پایدار؛ بدون تغییر کد محصول، داده واقعی یا بستن G1 |
| 0.45 | 2026-09-02 | ثبت پذیرش WP-08 پس از هشت دور بازبینی مستقل و ادغام PR #28 با CI موفق `33666088469` و مجوز مالک؛ ثبت شکست بعدی WR-08 در Windows Run `33666981958` و رفع آن با ACK/تحویل دوم و تأخیر کنترل‌شده در PR #30 (Merge `fdaa98b`)؛ CI نهایی اصلاحیه `33670197344` با 349/348 قبولی Windows/Linux و چهار سناریوی symlink ویندوز؛ حفظ کد محصول، مرز ساختگی، G1 باز و عدم صدور بسته بعدی |
| 0.44 | 2026-09-02 | ثبت تصمیم فنی O-73 و ADR-0011 برای Runtime یک‌بارمصرف Watcher بومی، اعلان اولیه پس از Start، حلقه ترتیبی خواندن، توقف غیرمسدودکننده و حفظ خطا/Thread cleanup؛ صدور WP-08 با 18 معیار قطعی و بومی فقط روی فایل ساختگی Windows/Linux، بدون تغییر قراردادهای پذیرفته‌شده، داده واقعی، SQLite/Commit یا بستن G1 |
| 0.43 | 2026-09-02 | ثبت پذیرش شاهددار WP-07 پس از بازبینی مستقل چهارمرحله‌ای، رفع خطا/مالکیت و سخت‌گیری oracle رقابت؛ CI Windows/Linux Run `33632031865` با 313/312 قبولی و اجرای چهار سناریوی symlink ویندوز؛ ادغام PR #25 با تأیید مالک و Commit `fe7e192`؛ حفظ مرز فایل ساختگی و بازماندن G1، بدون صدور WP-08 |
| 0.42 | 2026-09-02 | بستن تصمیم فنی O-72 و ثبت ADR-0010 برای هماهنگ‌کننده Save با مسیر دقیق، ساعت یکنواخت و Debounce ثابت دوثانیه‌ای، یک Token فعال و یک Follow-up، طبقه‌بندی Retry/Reader rejection/Faulted و Driver کامل WP-06/WP-05؛ صدور WP-07 با 16 معیار آزمون قطعی و شواهد فقط ساختگی، بدون Observer واقعی، Excel/OneDrive، UUID، Commit/SQLite/Outbox یا بستن G1 |
| 0.41 | 2026-09-02 | ثبت پذیرش شاهددار WP-06 توسط Codex مدیر پروژه پس از بازبینی مستقل و رفع ایجاد غیرانحصاری Candidate، حفظ منبع Hardlinkشده، پیام‌های امن و علت‌های مستقل Windows و آزمون جایگزینی واقعی؛ CI Windows/Linux Run `33616797405` با 273/272 قبولی و اجرای چهار سناریوی symlink ویندوز؛ ادغام PR #22 با مجوز مالک و Commit `1346abf`؛ حفظ مرز فایل ساختگی و بازماندن G1، بدون صدور بسته بعدی |
| 0.40 | 2026-09-01 | بستن تصمیم فنی O-71 و ثبت ADR-0009 برای Acquisition نسخه‌دار Snapshot ثابت با دو مشاهده، کپی/Hash جریانی، بازخوانی منبع و کپی، ZIP Container، ارتقای اتمیک و Cleanup دقیق؛ صدور WP-06 با Race/Property/Benchmark ترکیبی ساختگی و منع صریح Watcher/OneDrive/COM/UUID/SQLite/فایل واقعی، بدون بستن G1 |
| 0.39 | 2026-09-01 | ثبت پذیرش شاهددار WP-05 توسط Codex مدیر پروژه پس از بازبینی مستقل چندمرحله‌ای Reader، Raw/SST/Constructor/Property/Lifecycle و ابزارسنجی RSS؛ موفقیت 200 آزمون و CI نهایی Windows/Linux Run `33505486610` با بنچ‌مارک 15,000 ردیف `2.9061s / 58.96 MiB` و `4.6764s / 59.34 MiB`؛ ادغام PR #19 با Commit `638053a`، بدون دسترسی فایل واقعی، تغییر Excel، SQLite/Import Commit یا بستن G1 |
| 0.38 | 2026-08-31 | بستن O-70 و ثبت ADR-0008 برای Reader فقط‌خواندنی Streaming XLSX با Relationship/Header از رجیستری، متن/Decimal دقیق، تفکیک Formula/Cache و محدوده خروجی فرمول، فعالیت ردیف و Location مستقل از Hash؛ صدور WP-05 با Fixtureهای ZIP/XML کاملاً ساختگی، اتصال به WP-04 و سنجش زمان/RSS، بدون وابستگی تازه، دسترسی Excel واقعی، نوشتن UUID، SQLite یا بستن G1 |
| 0.37 | 2026-08-31 | ثبت پذیرش شاهددار WP-04 توسط Codex مدیر پروژه پس از سه دور اصلاح: اعتبارسنجی سازنده‌ها و جلوگیری از حذف کاذب، کپی دفاعی عمیق، تطابق دقیق Index/ردیف، شمارنده و Hash سخت‌گیرانه، ترتیب Raw از رجیستری و Property Test با Mappingهای مستقل و مقایسه کامل Plan؛ موفقیت 89 آزمون، بنچ‌مارک مستقل 15,000 ردیف با Build `2.5017s` و Plan `0.2897s` و CI Windows/Linux Run `33360537972`؛ ادغام PR #16 با Commit `ebdbe83`، بدون تغییر قرارداد Hash معتبر، Excel، داده واقعی یا بستن G1 |
| 0.36 | 2026-08-30 | بستن O-69 و ثبت ADR-0007 برای Snapshot تایپ‌شده کامل چهار شیت، یکتایی سراسری UUID/Home Sheet دائمی، رجیستری Active/Voided و Plan قطعی Insert/Edit/`void`/بدون‌تغییر با Revision بعدی، بازفعال‌سازی Voided به‌عنوان Edit و منع انتقال میان شیت‌ها؛ صدور WP-04 با Property/Idempotency/Immutability و شاهد کارایی 15,000 ردیف ساختگی، بدون Excel، SQLite یا بستن G1 |
| 0.35 | 2026-08-30 | ثبت پذیرش شاهددار WP-03 توسط Codex مدیر پروژه پس از بازبینی مستقل و یک دور اصلاح: سخت‌گیری Grammar رقم/جداکننده، خطای تایپ‌شده TypeTag، حذف فهرست تکراری شیت و تغییر خارج Scope mypy، Fixture کاملاً ساختگی و اسکن شکست‌پذیر، حساسیت همه فیلدهای Raw و Property Test permutation؛ تثبیت `persiantools 6.2.0`، Golden Vectorهای چهار شیت/Snapshot و `source-hash.v1`/`sheet-snapshot-hash.v1`؛ موفقیت 64 آزمون و CI Windows/Linux Run `33329429229`، ادغام PR #13 با Commit `6be9fef` و حفظ G1 در وضعیت باز |
| 0.34 | 2026-08-30 | بستن O-68 و ثبت ADR-0006 برای Canonical UTF-8 JSON آرایه‌ای تایپ‌دار، نسخه‌های `jalali-date.v1`/`source-hash.v1`/`sheet-snapshot-hash.v1`، قواعد صریح Null/Text/Integer/Decimal/تاریخ شمسی و UUIDv7 مرتب؛ تعویق آگاهانه `ledger_hash` تا فاز 2؛ صدور WP-03 با Golden Vector، Property Test، آزمون تقویم/`Asia/Tehran` و حفاظت کامل Excel/داده واقعی، بدون بستن G1 |
| 0.33 | 2026-08-30 | ثبت پذیرش شاهددار WP-02 توسط Codex مدیر پروژه: بازبینی مستقل و یک دور اصلاح Antigravity برای Deep Immutability، یکتایی سراسری نام فیلد و مرز واقعی `A:XFD`؛ تطبیق قرارداد دقیق چهار شیت و شناسه‌های `Z/P/P/D`، حذف Formula/Cached/Derived/Unlisted از Raw، نبود Float و حفاظت کامل Excel/داده واقعی؛ موفقیت 36 آزمون، Handoff/Acceptance Matrix/Test Results، اسکن دارایی/Secret و CI نهایی Linux/Windows در Run `33326970537`؛ ادغام PR #10 با Commit `5c99749` بدون بستن G1 |
| 0.32 | 2026-08-30 | ثبت پذیرش شاهددار WP-01 توسط Codex مدیر پروژه: دو چرخه Review/اصلاح Antigravity، Handoff/Acceptance Matrix/Test Results معتبر، قفل `uv 0.12.7` و Sync Frozen همه Workspaceها/Groupها، مرز خودکار Domain، اسکن پاک دارایی/Secret، موفقیت 21 آزمون و CI نهایی Linux/Windows در Run `33324657599`، ادغام PR #7 با Commit `4f9ad3e`؛ تبدیل شاهد نخستین Handoff و Baseline تکرارپذیر به ✅ بدون بستن G1 |
| 0.31 | 2026-08-30 | اجرای تصمیم تازه مالک درباره حاکمیت: Codex به‌عنوان مدیر پروژه/بازبین مسئول Roadmap، Work Package، ADR، Gate و Merge شد؛ Antigravity مجری بدون خودتأییدی ماند؛ مالک چشم‌انداز و آزمون پذیرش نهایی/اصلاحات را برعهده گرفت؛ مجوزهای ویژه Excel/داده/Secret/Production/هزینه/عملیات مخرب حفظ شدند؛ شواهد فاز صفر بررسی، G0 توسط مدیر پروژه بسته و فاز 1 مجاز شد؛ معیار G3/G7، O-46/O-47/O-49/O-50، ریسک‌ها و تعریف پایان هماهنگ شدند |
| 0.30 | 2026-08-30 | ثبت شاهد واقعی `/skills` و Dry Run ارائه‌شده توسط کاربر: کشف Skill پروژه‌ای، تشخیص تصویب‌نشدن G0، رد کدنویسی و حفاظت صحیح Roadmap/Excel/داده/`main`/Production؛ تغییر فاز صفر به «آماده تصویب» بدون بستن G0؛ تفکیک شاهد موفق پیش از G0 از آزمون Handoff کامل که در نخستین Work Package پس از G0 انجام می‌شود |
| 0.29 | 2026-08-30 | ثبت اصلاح کاربر مبنی بر وجود فقط یک Volume متصل و تکمیل شاهد فاز صفر: ساخت دایرکتوری مستقل و Bind Mount پایدار `/srv/accounting-bot` بدون Format یا دست‌زدن به داده موجود؛ Clone عمومی و بدون Credential در `workspace` و تطبیق Commit/Worktree؛ تبدیل نصب Global مستقل Skill به Symlink مدیریت‌شده Project Scope همراه Backup، Hash و Validator موفق؛ باقی‌ماندن فقط `/skills` و Dry Run واقعی به‌صورت 🧪 و عدم تغییر G0 |
| 0.28 | 2026-08-29 | اجرای تصمیم جدید کاربر برای حذف نیاز به GitHub Pro: ارتقای مخزن پاک `accounting-bot-public` به منبع عملیاتی و مرجع یگانه Roadmap/Skill؛ نگهداری مخزن Private صرفاً به‌عنوان آرشیو بدون انتقال تاریخچه یا Clone سرور؛ بستن O-67 با حفاظت فنی موجود Public؛ تقویت منع Commit داده/Secret/Host واقعی و به‌روزرسانی فاز صفر و ریسک‌ها |
| 0.27 | 2026-08-29 | اجرای انتخاب کاربر برای مخزن عمومی جدا: ساخت `accounting-bot-public` از Snapshot پاک با یک Root Commit و بدون تاریخ Private؛ حذف IP/شناسه‌های عملیاتی و استفاده از `SYNC_SERVER_HOST` در `.env` غیرقابل‌Commit؛ موفقیت Secret/Data/History Scan؛ Push عمومی و حفاظت `main` با PR اجباری، Enforce Admin و منع Force Push/Deletion؛ حفظ مخزن عملیاتی Private و بازماندن O-67 فقط برای حفاظت همان مخزن |
| 0.26 | 2026-08-29 | احراز مجدد GitHub CLI برای حساب `mohsenbarari`، ساخت مخزن عملیاتی Private، ثبت `origin` و Push/تطبیق Baseline روی `main`؛ تفکیک Clone و Forward Test باقی‌مانده؛ ثبت مانع 403 حفاظت شاخه Private روی GitHub Free و افزودن O-67 برای انتخاب ارتقا به Pro یا پذیرش موقت کنترل فرایندی، بدون عمومی‌کردن مخزن یا تغییر Billing |
| 0.25 | 2026-08-28 | آماده‌سازی Baseline محلی پاک روی `main` با 15 فایل حاکمیتی، `.gitignore`/`.gitattributes` و README؛ خارج‌کردن SQLite واقعی، Excel/PDF، work، Prototypeهای قدیمی و Skill تکراری از Git بدون حذف فایل‌ها؛ موفقیت اسکن Secret/شماره، Validator و Diff check؛ باقی‌ماندن Remote Private، Push، Clone سرور، حفاظت `main` و Forward Test به‌عنوان کارهای شاهددار پیش از G0 |
| 0.24 | 2026-08-28 | ثبت پنج ADR پذیرفته‌شده برای O-52 تا O-56: Monorepo/Modular Monolith/Python، Agent تعاملی Windows/Excel، FastAPI/PostgreSQL/صف پایدار، Telegram/شمسی/PDF فارسی و Docker Compose/وابستگی/کیفیت؛ اتصال هر ADR به Gate، شاهد، Rollback و شرط بازنگری؛ تکمیل وظیفه ADR فاز صفر بدون کدنویسی، تغییر Excel، ساخت Remote یا تصویب G0 |
| 0.23 | 2026-08-28 | تأیید دو مرحله پایانی Import: `change_events` به‌عنوان Outbox پایدار بدون داده تکراری، Checkpoint امضاشده/High-watermark و Jobهای Idempotent با ترتیب Sync→اصلاحیه→Catch-up→PDF؛ ثبت گزارش ساختاریافته Import در مسیر سلامت همگام‌سازی بدون اعلان Save موفق و با هشدار فوری خطا و منع ارسال رسمی پیش از Commit کامل؛ تکمیل ممیزی همه `🟦`ها، افزودن پذیرش/ریسک‌ها و حفظ G0 در وضعیت تصویب‌نشده |
| 0.22 | 2026-08-28 | تأیید پنج مرحله از ممیزی Import: Snapshot ثابت و پاک‌سازی‌شونده، اعتبارسنجی دو سطحی با رفع تناقض رد کامل/توقف محدود، مقایسه UUID/Hash با Revision فعال، Commit اتمیک SQLite/Outbox و محاسبه دامنه اثر قدیم و جدید با محاسبه مرجع پس از Commit سرور؛ افزودن معیارهای آزمون و ریسک مرتبط؛ مراحل 7 و 8 همچنان 🟦 و G0 تصویب نشده است |
| 0.21 | 2026-08-28 | بستن O-62 تا O-66: تثبیت `pending_activation` و عدم Retry/Backlog کامل، رد قابلیت تازه رویدادهای امنیتی در بات با حفظ کاربردهای قبلی، نمایش کامل و بدون Mask شماره در مسیرهای مصوب، نصب Global موقت Skill روی سرور و تطبیق SHA-256/Validator، و تأیید مخزن Private به‌عنوان منبع یگانه Roadmap/Skill پس از Baseline؛ Forward Test واقعی `/skills` و O-25 همچنان 🧪 و G0 هنوز تصویب نشده است |
| 0.20 | 2026-08-28 | بستن O-57 تا O-61 با تأیید جداسازی شخص هویتی/حسابداری، Financial Event پایدار، ledger_entries نسخه‌دار و قابل‌بازتولید، حفظ C/D بدون شرح همراه هشدار و الگوریتم قطعی رد Pending/تأیید `<=100ms`؛ ساخت Skill پروژه‌ای Antigravity با قالب ADR/Handoff و Validator آزموده‌شده و افزودن معیار Forward Test؛ افزودن O-62 تا O-66 برای فعال‌نشدن Telegram، دامنه ممیزی، Mask موبایل، دامنه Skill و منبع یگانه Git؛ هیچ G0 یا مجوز تغییر Excel/سرور صادر نشد |
| 0.19 | 2026-08-28 | بستن O-52 تا O-56 با تأیید Monorepo/Modular Monolith و Python 3.13، Stack عامل Windows، FastAPI/PostgreSQL و صف داخلی، aiogram/persiantools/Chromium PDF و Docker Compose/Caddy/uv؛ تبدیل بخش 15.6 به Stack مصوب، افزودن شواهد بسته‌بندی/صف/Lockfile/Compose و همسان‌سازی پیشنهادهای تکراری با تصمیم‌های قطعی O-05/O-28/O-30/O-41 تا O-43/O-54؛ هیچ G0 یا مجوز کدنویسی صادر نشد |
| 0.18 | 2026-08-28 | بستن O-46 تا O-51 با تثبیت نقش کاربر/Antigravity/Codex، Branch و Handoff، ADR، Stop Condition و جداسازی مخزن Private/Public؛ افزودن معیارهای حاکمیت، ریسک‌ها و کارهای G0؛ ثبت O-52 تا O-56 به‌عنوان پنج تصمیم باز Stack شامل Modular Monolith، Agent ویندوز، FastAPI/PostgreSQL، aiogram/Chromium PDF و Docker Compose/Caddy |
| 0.17 | 2026-08-28 | بستن O-41 با تأیید HTTPS عمومی و کلید Ed25519 اختصاصی دستگاه؛ جایگزینی mTLS/CA خصوصی و نشست یک‌ماهه با Enrollment یک‌بارمصرف، Challenge، امضای Canonical، لغو و چرخش کلید؛ هماهنگ‌سازی O-28، O-29، فاز 4، معیارهای پذیرش و ریسک‌ها |
| 0.16 | 2026-08-28 | افزودن O-41 تا O-45؛ بازگذاشتن انتخاب احراز عامل و پیشنهاد امضای Ed25519 به‌جای نشست یک‌ماهه، تثبیت مدل تاریخ شمسی، برداشتن الزام رمزگذاری برنامه‌ای داده/Backup، تعیین نگهداری لاگ و ناشناس‌سازی و منع دسترسی Telegram User ID جدید |
| 0.15 | 2026-08-28 | افزودن و بستن O-36 تا O-40؛ تثبیت ساختار متن کم‌تزئین، اولویت Backlog، Cursor جدا برای حساب/گیرنده، منوهای نقش‌ها و تقویم شمسی Inline همراه ورودی تایپی |
| 0.14 | 2026-08-28 | افزودن و بستن O-31 تا O-35؛ تثبیت Debounce/OneDrive، Resolver محافظه‌کار RS تاریخی، نمایش فیلدهای صفر/خالی، Diff اصلاحیه و تقسیم/صف پایدار PDFهای بزرگ |
| 0.13 | 2026-08-28 | بستن O-26، O-27، O-29 و O-30؛ محدودکردن Sync به داده خام چهار شیت و انتقال Formula/Query به Ledger و دیتابیس، تثبیت دو مدیر ارشد جدا، معماری دامنه/ACME/mTLS با تعویق نام دامنه و دعوت دقیق 24ساعته؛ رفع تناقض واحد تومان، Item Master و ملاک تاریخ مالی |
| 0.12 | 2026-08-28 | بستن O-21 تا O-24؛ تثبیت تجمیع اصلاحیه پس از Commit کامل Backlog، بازیابی اتمیک Telegram User ID، خروج خود حسابدار و ممنوعیت Override رأی؛ تأیید قاعده O-25 با Integer/Decimal و ROUND_HALF_UP و نگه‌داشتن وضعیت 🧪 تا اثبات نقطه Round از اکسل؛ شماره‌گذاری اعتبار دعوت حسابدار به‌عنوان O-30 |
| 0.11 | 2026-08-28 | بستن O-16 تا O-20؛ تثبیت Item Master و توقف محدود کالای ناشناخته، نگاشت چهارگروهی نام‌ها بدون اتصال خودکار، واحد تومان بدون تبدیل، سیاست پنج تلاش Telegram و Backup ساعتی/مرکزی با چرخه 30/12/12 و RPO/RTO آزمون‌پذیر |
| 0.10 | 2026-08-28 | بستن O-15؛ تثبیت شیت «ریزحساب» به‌عنوان مرجع ساختاری PDF، محدودکردن افزوده‌ها به report_id، زمان ایران و شماره صفحه، حذف شناسه‌های شخصی/واترمارک/رمز و حذف فایل موقت پس از تحویل موفق با حفظ امکان بازتولید |
| 0.9 | 2026-08-28 | بستن O-11 تا O-14؛ تثبیت published_at مشترک، حذف تصمیم از PDF، علت اختیاری 10دقیقه‌ای و الگوی تجمیع پروژه قدیمی؛ ثبت شیت «ریزحساب» به‌عنوان مرجع O-15 و بازنگه‌داشتن عناصر تکمیلی PDF |
| 0.8 | 2026-08-28 | بستن O-06؛ حذف کامل تأیید، آستانه و قرنطینه مبتنی بر حجم برای ویرایش و حذف، با حفظ Soft Delete، Revision، Audit و توقف صرفاً برای Snapshot فنی نامعتبر |
| 0.7 | 2026-08-28 | اصلاح O-06 پس از شناسایی محدودیت قطعی اینترنت؛ حذف تأیید از طریق بات و جایگزینی آن با تأیید آفلاین پیش از Save و قرنطینه محلی پشتیبان، بدون تغییر وضعیت باز تصمیم |
| 0.6 | 2026-08-28 | بستن O-07 تا O-10؛ تثبیت تغییرناپذیری آرشیو، تک‌نقشی‌بودن کاربران مصرف‌کننده، Bootstrap مدیر و احراز صرفاً با Contact، و ارسال دستی گزارش روز قبل براساس تاریخ اکسل؛ تشریح محل تأیید حذف پرخطر در O-06 |
| 0.5 | 2026-08-28 | تأیید هر پنج مورد نخست و بستن O-03؛ تثبیت UUIDv7 و سه Hash مبتنی بر SHA-256؛ افزودن معیارهای عملکرد، ثبات شناسه و ممنوعیت تغییر Excel بدون اجازه جداگانه |
| 0.4 | 2026-08-28 | تأیید پروتکل Sync و بستن O-28؛ افزودن الزام دامنه/TLS و O-29؛ تشریح رفتار ویرایش، حذف و Insert با شناسه پایدار و ثبت ممنوعیت تغییر اکسل بدون اجازه جداگانه |
| 0.3 | 2026-08-28 | تأیید معماری ترکیبی و PostgreSQL؛ تثبیت H/HA/HS و قاعده انتقال 01/01؛ افزودن طرح تفصیلی Sync، برآورد صف سه‌ماهه و اثر دقیق شناسه‌ها بر اکسل |
| 0.2 | 2026-08-26 | افزودن پیشنهاد اجرایی، دلیل و زمان تصمیم زیر هر 27 مورد باز |
| 0.1 | 2026-08-26 | ایجاد نخستین نسخه جامع شامل همه موارد قطعی، پیشنهادی و باز گفتگو |
